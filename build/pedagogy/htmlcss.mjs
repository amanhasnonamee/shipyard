// htmlcss pedagogy — authored from htmlcss-curriculum.md (current 2025)
export default {
  title: 'The HTML/CSS Shipyard - HTML & CSS: A First-Principles Curriculum',
  brand: 'THE HTML/CSS SHIPYARD',
  tagline: 'HTML & CSS · the document tree · the cascade · layout',
  storageKey: 'shipyard:htmlcss:v1',
  legacyStorageKeys: [],
  stackLabel: 'THE STACK — your document tree',
  ranks: ['SWABBIE', 'DECKHAND', 'BOATSWAIN', 'FIRST MATE', 'CASCADE CAPTAIN', 'HARBOR MASTER'],
  accent: '#ff6b6b',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum htmlcss-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0–14 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: the cascade decides who wins — and it has a fixed order, every time.</span></div>',
    '<div class="bl">AWAITING CREW — 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">▮</span></div>',
  ],
  h1: 'HTML & CSS, UNDERSTOOD.<br><span class="wh">NOT JUST PLACED.</span>',
  sub: 'This is a first-principles curriculum rebuilt as a mission. Learn the core mental model <b>deeply</b> — the document is a tree, everything is a box, and the cascade settles every argument in a fixed order — reference the rest, and physically <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The HTML/CSS Shipyard is a faithful interactive conversion of the first-principles HTML & CSS curriculum — every concept, table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file — copy it anywhere, it runs offline.',
    rule: 'everything is a box, and the cascade settles every argument in a fixed order.',
  },
  chips: { p10: ['<span class="m">★ <b>INTERVIEW GOLD</b></span>'] },
  grad: {
    title: 'LAYOUT RESOLVED 🎨',
    rule: 'everything is a box, and the cascade settles every argument in a fixed order.',
  },
  console: {
    cmd: '$ open index.html',
    line: 'painted  ·  no layout shift  ·  (yours — every layer earned)',
  },
  simPrompt: '/* .card { display: flex } — watch the cascade settle */',
  egg: { label: 'SPECIFICITY OVERRIDE' },
  deckIn: 'p13',
  milesIn: 'p14',
  simIn: 'p10',
  deckNote: 'If you can deliver these aloud — WHAT \u2192 WHY IT EXISTS \u2192 ONE CONCRETE CONSEQUENCE — you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof. When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'HTML was invented:', o: ['By Tim Berners-Lee at CERN (1989) — hypertext to share physics papers; CSS followed in 1996 to separate content from presentation', 'By Netscape in 1995', 'By the W3C in 1994', 'As part of HTTP/2'], a: 0, e: 'TBL at CERN, 1989: the document web. CSS arrived 1996 (Lie & Bos) — the separation of meaning (HTML) from appearance (CSS) is the founding idea.' },
      { q: 'HTML is:', o: ['A markup language — it describes document structure and meaning, not behaviour', 'A programming language', 'A styling language', 'A query language'], a: 0, e: 'No logic, no state. Elements describe meaning; browser + CSS decide appearance; JS adds behaviour.' },
      { q: 'The standards context that matters:', o: ['The HTML Living Standard (WHATWG) — the web keeps moving, and browsers implement to it rather than frozen specs', 'HTML is frozen at 4.01', 'Only the W3C can change HTML', 'Standards are cosmetic'], a: 0, e: 'HTML is a living standard: features ship in browsers first, get standardised after — the opposite of frozen legacy specs.' },
      { q: 'When a design is \u201cjust divs\u201d, the lost layer is:', o: ['MEANING — divs carry no semantics; screen readers, search and future you lose the document\u2019s outline', 'Speed', 'Colour', 'Cascade'], a: 0, e: 'Every div is a box until you give it meaning; semantics is the layer that survives any visual redesign.' },
    ],
    p2: [
      { q: 'The browser renders pages by:', o: ['Building a DOM TREE first, then styling it — CSS targets nodes of that tree', 'Styling the HTML text directly', 'Rendering in source order instantly', 'Asking the server for pixels'], a: 0, e: 'HTML \u2192 parsed into a tree of elements; CSS selectors match nodes; layout and paint follow. Idea 1: the tree comes first.' },
      { q: 'The box model arithmetic:', o: ['content + padding + border (+ margin) — box-sizing: border-box moves padding/border INSIDE the width', 'width includes margin by default', 'padding never affects total size', 'border is free'], a: 0, e: 'With border-box, width = everything up to the border — the only sane default for real layouts.' },
      { q: 'Normal flow is:', o: ['A real layout system — block elements stack, inline flows along the line — and it is usually right; flex/grid are refinements, not replacements', 'A bug to be worked around', 'Deprecated', 'Only for paragraphs'], a: 0, e: 'Idea 3: normal flow handles most pages correctly. Reach for positioning only when flow genuinely can\u2019t.' },
      { q: 'The cascade decides winners in this order:', o: ['Important + origin \u2192 specificity \u2192 source order — more specific selectors win; ties go to the later rule', 'File order only', 'Selector length', 'Alphabetical'], a: 0, e: 'Idea 4: importance/origin first (author !important beats normal), then specificity, then source order. Inline styles beat selectors (except !important).' },
    ],
    p3: [
      { q: 'Semantic HTML is:', o: ['Meaning over appearance — header/main/section/article/nav describe structure; the browser and assistive tech consume the OUTLINE', 'A styling preference', 'Required only for SEO', 'The same as using divs'], a: 0, e: 'The element map is the document\u2019s outline. Semantic elements also ship free behaviour (landmark roles, default margins).' },
      { q: 'Forms done right require:', o: ['label tied via for/id, a name on every field, correct type + required/pattern validation, aria-describedby for help text', 'Only a submit button', 'Tables for layout', 'JS validation exclusively'], a: 0, e: 'Labels are the click target AND the accessibility name. Validation belongs in HTML first, JS as enhancement.' },
      { q: 'Accessibility is mostly:', o: ['A byproduct — correct headings, alt text, focus styles and contrast come free from structure; the audits mostly check the structure you already should have', 'A plugin', 'A CSS class', 'Only for government sites'], a: 0, e: 'Keyboard navigation, readable outline, visible focus: all follow from semantic, well-formed documents.' },
      { q: 'alt text exists for:', o: ['The blind — describe what the image MEANS in context, not what it literally shows', 'SEO keywords', 'Designers', 'Lazy loading'], a: 0, e: 'Meaning, not description: a graph\u2019s alt summarises the finding, not the axes.' },
    ],
    p4: [
      { q: 'The selector strategy that scales:', o: ['Class-based with LOW specificity — avoid IDs for styling; :is()/:where() keep specificity sensible', 'IDs everywhere', 'Universal selectors only', 'Deep descendant chains'], a: 0, e: 'Low-specificity classes keep every later rule winnable. Deep chains (div .a .b .c) become specificity debt.' },
      { q: 'Custom properties are:', o: ['Design tokens — --x declared on :root, inherited down the tree, overridable at any level (but not inside media queries)', 'A preprocessor feature', 'Runtime variables', 'Deprecated'], a: 0, e: 'This is where modern CSS starts: one token change re-themes a whole app. They compose with calc() and can\u2019t be read by media queries — plan for that.' },
      { q: 'rem vs em:', o: ['rem = root em (stable); em compounds at every nesting level — use rem for spacing/type scale, em for element-relative sizes', 'rem is always wrong', 'They are identical', 'em is absolute'], a: 0, e: 'em compounds through nested rules and surprises; rem is the predictable scale. Together with clamp() they replace most px maths.' },
      { q: 'Two rules same specificity — who wins?', o: ['Source order: the LATER rule wins', 'The shorter rule', 'The first rule, always', 'The browser decides randomly'], a: 0, e: 'Specificity tie \u2192 later in the stylesheet wins. That is why \u201cundo\u201d rules must come after the rules they undo.' },
    ],
    p5: [
      { q: 'Choosing a layout tool:', o: ['Flow for what flow does; FLEXBOX for 1-D arrangement of content; GRID for 2-D container-driven structure; absolute positioning as the escape hatch', 'Grid for everything', 'Flexbox for everything', 'Tables were never a layout'], a: 0, e: 'Flex = content-sized, one axis, per-item control. Grid = container-sized, tracks and areas. Each has a mental model; pick by the constraint that actually binds.' },
      { q: 'The flexbox mental model:', o: ['main axis vs cross axis — justify-content places along main, align-items along cross; flex-grow/shrink/basis decide how space is claimed', 'Flex has one axis only', 'align-items controls the main axis', 'gap only works in Grid'], a: 0, e: 'If you can state the axis for each property, flexbox stops surprising you.' },
      { q: 'The grid mental model:', o: ['Tracks (columns/rows) with fr units, named template areas, and alignment — the container owns the layout, not the items', 'Grid items position themselves', 'fr is a font unit', 'Grid cannot mix sizes'], a: 0, e: 'grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)) — one line, fluid responsive layout, no media query.' },
      { q: 'The two positioning traps:', o: ['absolute removes the box from flow (its containing block is the nearest positioned ancestor); sticky needs a scroll container and a top value', 'absolute stays in flow', 'sticky works without top', 'fixed is relative to the page only'], a: 0, e: 'Both traps are \u201cworks nowhere else, works here\u201d surprises — check the containing block, check the scroll parent.' },
    ],
    p6: [
      { q: 'The responsive premise:', o: ['Respond to the VIEWPORT and the device — design systems that reflow; mobile-first makes constraints explicit', 'Detect the browser with JS', 'One desktop layout is fine', 'Media queries are the only tool'], a: 0, e: 'Intrinsic sizes, wrapping, clamp() and container queries come BEFORE media queries — media queries fill the gaps, not the plan.' },
      { q: 'The best-first toolkit:', o: ['Intrinsic sizing (min/max-content), flex-wrap, grid auto-fit/minmax, clamp() type, fluid spacing, srcset — media queries LAST', 'Fixed px widths', 'A media query per device', 'iframe resizing'], a: 0, e: 'Layout should shrink and grow on its own; breakpoints only patch where intrinsic behaviour can\u2019t.' },
      { q: 'A common responsive mistake:', o: ['Setting min-width in px on containers — breaks any viewport below it; prefer minmax(0, ...) and flexible tracks', 'Too much fluidity', 'Using rem', 'Using gap'], a: 0, e: 'min-width: 960px is how desktop-only pages happen. Grid\u2019s minmax(0, 1fr) is the anti-overflow weapon.' },
      { q: 'Images that never overflow:', o: ['max-width: 100%; height: auto — plus width/height attributes (or aspect-ratio) to reserve layout space', 'width: 100vw', 'object-fit on spans', 'img { display: none } until loaded'], a: 0, e: 'Missing dimensions cause CLS (cumulative layout shift) — reserve space, set aspect ratios, lazy-load below the fold.' },
    ],
    p7: [
      { q: 'Systems beat one-offs:', o: ['Tokens (type/colour/space) \u2192 components \u2192 pages — a small design system beats bespoke pixels every time', 'Every page bespoke', 'One-off styles are faster forever', 'Systems kill creativity'], a: 0, e: 'Consistency is a feature: users learn one scale, one rhythm, one vocabulary across the whole product.' },
      { q: 'Typography is the highest visual ROI:', o: ['A modular type scale (e.g. 1.25 ratio), 1.4\u20131.6 line-height, 45\u201375ch measure — type alone carries a design', 'More fonts = better', 'font-size: 16px everywhere', 'Line-height is a luxury'], a: 0, e: 'Type is ~90% of the page\u2019s visual weight. Get the scale, spacing and measure right and the design lands.' },
      { q: 'Colour\u2019s one DEEP rule:', o: ['CONTRAST — text must pass 4.5:1 against its background (WCAG AA); hue choice is taste, contrast is law', 'Use every colour in the palette', 'Black on white only', 'Contrast is a designer luxury'], a: 0, e: 'The audit tools check contrast, not taste. If a color pair fails, no visual argument rescues it.' },
      { q: 'Motion defaults:', o: ['150\u2013300ms, ease-out, one or two properties at a time — and honour prefers-reduced-motion', 'Longer is classier', 'Animate everything', 'Motion is forbidden'], a: 0, e: 'Motion communicates state (which element changed); slow or constant motion just annoys. 300ms of ease-out is the default taste.' },
    ],
    p8: [
      { q: 'Building the full page starts with:', o: ['The semantic STRUCTURE — outline first, then tokens, then sections, then polish; verify in DevTools constantly', 'Colours first', 'Fonts first', 'A framework'], a: 0, e: 'Structure first means the page is accessible and extensible before it is pretty.' },
      { q: 'CSS order that scales:', o: ['Mobile-first: base styles, then media queries that ADD complexity at wider widths', 'Desktop-first always', 'One giant file', 'Inline styles everywhere'], a: 0, e: 'Mobile-first means the constrained case is the default — extension beats correction.' },
      { q: 'Form validation states must be:', o: ['Visible, labelled, and matching HTML validation — :invalid/:valid plus aria-describedby help text and focus management', 'Silent until submit', 'Colour-only', 'A JS library\u2019s job alone'], a: 0, e: ':\u200bplaceholder-shown + :invalid distinguish untouched from wrong — the difference between helpful and hostile forms.' },
      { q: 'The final pass audits:', o: ['Keyboard navigation, contrast (4.5:1), zoom to 200%, Lighthouse/axe — measure, then fix the real findings', 'Only looks', 'Browser compatibility pages', 'Nothing — ship it'], a: 0, e: 'The audits are quick; the findings are honest. A keyboard-walkable, high-contrast page is the baseline, not the bonus.' },
    ],
    p9: [
      { q: 'The four DevTools panels that matter:', o: ['Elements (styles + box), Computed (what actually won), Network (what loaded), Performance (what janked) — Computed answers cascade fights instantly', 'Console only', 'Only the Elements tab exists', 'Screenshots panel'], a: 0, e: 'Never guess who won a cascade fight — the Computed panel tells you the winner and why.' },
      { q: 'The debugging decision tree:', o: ['Is it a BOX problem, a CASCADE problem, or a SOURCE problem? Check computed styles before touching code', 'Rewrite the file', 'Ask the design tool', 'Random changes until it works'], a: 0, e: 'Box: inspect the box model. Cascade: inspect computed + matched selectors. Source: is it even loaded?' },
      { q: 'An element is smaller than expected. First move:', o: ['Inspect the box model panel — border-box vs content-box and padding explain most sizing mysteries', 'Increase width', 'Add !important', 'Delete the stylesheet'], a: 0, e: 'The box panel shows content/padding/border/margin live — the arithmetic is visible, not guessed.' },
      { q: 'A style will not apply no matter what:', o: ['Check the cascade order: specificity (IDs/classes count), then source order, then inline styles and !important — the Computed panel names the culprit', 'It is a browser bug', 'CSS is broken', 'Use more !important'], a: 0, e: 'Winners are decided by importance \u2192 specificity \u2192 order. The panel lists which selector won and where it lives.' },
    ],
    p10: [
      { q: 'The rendering pipeline, in order:', o: ['HTML \u2192 DOM tree; CSS \u2192 style (cascade + resolved values) \u2192 layout (boxes measured) \u2192 paint \u2192 composite', 'Paint \u2192 layout \u2192 style', 'DOM \u2192 paint \u2192 style', 'Style is optional'], a: 0, e: 'The interview-grade narration: tokenize \u2192 parse \u2192 style \u2192 layout \u2192 paint \u2192 composite. Each stage feeds the next.' },
      { q: 'Layout thrash is:', o: ['Reading a layout value right after writing forces a synchronous relayout — batch your DOM reads and writes', 'A paint-only problem', 'A GPU issue', 'Impossible in modern browsers'], a: 0, e: 'JS reads like scrollHeight/offsetWidth mid-write-loop force reflow every time — the classic jank source.' },
      { q: 'z-index only works:', o: ['INSIDE the nearest stacking context — transform/filter/opacity create them, so z-index decisions are local, not global', 'Globally, anywhere', 'On any element', 'Only on fixed elements'], a: 0, e: 'A transformed ancestor traps its children\u2019s z-index. Two \u201cz-index: 9999\u201d fixes fight inside different contexts — the mystery behind most stacking bugs.' },
      { q: 'The parser blocks on:', o: ['CSS (render-blocking) and non-deferred JS — defer/async scripts stop blocking; <script> mid-body blocks parsing until executed', 'Images only', 'Web fonts', 'Nothing anymore'], a: 0, e: 'CSS blocks first paint; classic scripts block parsing. async = fire when ready; defer = after parsing, in order. Fonts trigger FOIT unless you plan for it.' },
    ],
    p11: [
      { q: 'The metrics actually graded:', o: ['LCP (largest content painted), INP (interaction latency), CLS (layout shift) — and each has a fix: preload, defer work, reserve space', 'Hits per second', 'FPS only', 'File size only'], a: 0, e: 'LCP: image sizes + preload. INP: keep main thread free. CLS: width/height on media + stable fonts.' },
      { q: 'CSS architecture\u2019s real trade-off:', o: ['Consistency + speed of change vs file size + constraint — utilities (Tailwind) vs BEM vs composition; the senior conversation is cost/consistency, not dogma', 'Always utilities', 'Always BEM', 'CSS files are irrelevant'], a: 0, e: 'Pick the system your team will actually keep consistent; measure the cost over a year, not a week.' },
      { q: 'The accessibility checks that catch most issues:', o: ['axe/Lighthouse scan + keyboard-only walk + focus visibility + label audit — most failures are found by these four', 'Screen-reader poetry', 'Eye-tracking', 'Colour picking'], a: 0, e: 'Automated scans catch ~30%; the keyboard walk catches the rest. Both together: most of the way.' },
      { q: 'Performance starts at:', o: ['The critical request chain — fewer, smaller, later — then images, then fonts, then JS payload', 'Spinning manufacturers\u2019 lies', 'A CDN', 'A framework'], a: 0, e: 'First paint cares about the chain: HTML \u2192 critical CSS \u2192 first image. Shipping less, shipping later, wins every time.' },
    ],
    p12: [
      { q: 'Hand-writing CSS wins when:', o: ['The surface is bespoke and small — custom UI, tight design control, few pages; the file is the system', 'The team is large', 'There are 40 components', 'Always'], a: 0, e: 'Hand-written wins on small, design-heavy surfaces where every pixel is deliberate.' },
      { q: 'A framework (or utility layer) wins when:', o: ['Rapid product iteration — consistency + speed across many screens; tokens keep brand identity anyway', 'Never', 'Only for marketing sites', 'Only with a backend'], a: 0, e: 'When screens outnumber hours, the framework\u2019s constraint is the point: your team ships consistent fast.' },
      { q: 'The honest trade-off to name:', o: ['Build size and debugging surface vs consistency and speed — and conventions matter more than either tool', 'Size is the only factor', 'Debugging is easy everywhere', 'Frameworks have no cost'], a: 0, e: 'Whatever you pick, the decision is about your team\u2019s consistency cost over a year — that is the senior framing.' },
      { q: 'What survives every framework change:', o: ['The semantic layer — real elements, a real outline, tokens as custom properties. The DOM is the contract; frameworks come and go around it', 'The utility classes', 'The build config', 'The component names'], a: 0, e: 'Structure is portable; framework-specific markup is not. Your pages\u2019 meaning outlives any stack.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 \u25b6 The box model — and why border-box is the sane default.', a: 'content + padding + border (+ margin). box-sizing: border-box puts padding/border inside the declared width — no more \u201cwidth + 32px\u201d arithmetic. (Part 2, Idea 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 \u25b6 The cascade order — who wins, and when.', a: 'Important + origin first, then specificity, then source order. Inline styles beat selectors (except !important). A tie goes to the later rule. (Part 2, Idea 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 \u25b6 Specificity in one breath.', a: 'IDs > classes/attrs/pseudo-classes > elements. :is()/:where() let you write complex selectors with LOW specificity — keep yours low so later rules can win. (Part 4.1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 \u25b6 rem vs em — and the only sane spacing rule.', a: 'rem = root em (predictable); em compounds at every nesting level. Use rem for the type/space scale, em only when something should size relative to ITSELF. (Part 4.3)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 \u25b6 Flexbox in one axis — which property controls which axis.', a: 'justify-content = MAIN axis placement; align-items = CROSS axis. flex-grow/shrink/basis decide how space is split. State the axis first, then the property. (Part 5.2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 \u25b6 Grid vs flex — the actual selector.', a: 'Flex: 1-D, content-sized, items decide. Grid: 2-D, container-sized, tracks decide. auto-fit/minmax(240px, 1fr) = fluid responsive without media queries. (Part 5.3)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 \u25b6 Custom properties — what they are and where they fail.', a: 'Design tokens: declared on :root, inherited, overridable at any level, composable with calc(). They do NOT work inside media queries — precompute what breakpoints need. (Part 4.2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 \u25b6 The semantic element set that carries the outline.', a: 'header, main, nav, section, article, aside, footer, h1\u2192h6, figure, time, address — the landmarks assistive tech and search use. Every div is a box until you give it meaning. (Part 3.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 \u25b6 Forms: the four things every field needs.', a: 'A label (for/id), a name, a correct type, and HTML validation (required/pattern) — plus aria-describedby for help text. Labels are click targets AND accessibility names. (Part 3.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 \u25b6 The colour law that is not taste.', a: 'Contrast: 4.5:1 for normal text, 3:1 for large text (WCAG AA). Hue is taste; contrast is law — every audit tool checks it. (Part 7.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 \u25b6 The rendering pipeline, named in order.', a: 'HTML \u2192 DOM tree; CSS \u2192 style (cascade + resolved values) \u2192 layout (boxes measured) \u2192 paint \u2192 composite. The interview-grade narration starts here. (Part 10.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 \u25b6 Layout thrash — the one-line explanation.', a: 'A JS read after a write forces a SYNCHRONOUS relayout. Batch your DOM reads and writes; one forced reflow a frame is fine, a loop is jank. (Part 10.2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 \u25b6 Stacking contexts — why z-index stops working.', a: 'z-index only orders elements INSIDE the same stacking context. transform/filter/opacity/position create new ones — so \u201cz-index: 9999\u201d loses to a transformed ancestor\u2019s context. (Part 10.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 \u25b6 The parser-blocks rules.', a: 'CSS blocks rendering. Classic scripts block parsing — defer runs after parsing in order, async fires when ready. Images and fonts do not block parsing, only paint. (Part 10.4)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 \u25b6 LCP / INP / CLS — what each measures and its fix.', a: 'LCP: largest content \u2192 preload + image sizes. INP: interaction latency \u2192 keep the main thread free. CLS: layout shift \u2192 reserve space (width/height, stable fonts). (Part 11.1)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 \u25b6 The CSS architecture trade-off — as a senior frames it.', a: 'Consistency + change-speed vs file size + constraint: utilities (Tailwind), BEM, or composition. The decision is your team\u2019s consistency cost over a year, not tool dogma. (Part 11.2)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 \u25b6 The responsive default that kills breakpoints.', a: 'Intrinsic sizing + flex-wrap + grid auto-fit/minmax + clamp() — layout that reflows by itself; media queries fill the residual gaps. Mobile-first makes the constrained case the default. (Part 6)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 \u25b6 When to hand-write CSS and when not to.', a: 'Hand-write: bespoke, small, design-heavy surfaces. Framework/utilities: many screens racing to consistency. Either way the semantic layer and tokens survive the choice. (Part 12)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 \u25b6 Model', pass: 'Pass: the Part 2 checkpoint aloud — DOM tree, box arithmetic, normal flow, and the cascade order from memory. <b>(Parts 1\u20132; ~days 1\u20133)</b>' },
    { id: 'm2', title: 'Milestone 2 \u25b6 Semantics', pass: 'Pass: write the full outline of a real page (landmarks, headings, forms with labels) without looking at a design. <b>(Part 3; days 4\u20136)</b>' },
    { id: 'm3', title: 'Milestone 3 \u25b6 Fundamentals', pass: 'Pass: build a token layer (type/colour/space as custom properties) and settle five specificity puzzles using the Computed panel. <b>(Part 4; days 7\u201312)</b>' },
    { id: 'm4', title: 'Milestone 4 \u25b6 Layout', pass: 'Pass: a fluid layout with flex + grid that survives 360px\u21921440px with no breakpoint below 720px. <b>(Parts 5\u20136; days 13\u201316)</b>' },
    { id: 'm5', title: 'Milestone 5 \u25b6 Craft & project', pass: 'Pass: the full responsive page — semantic structure, type scale, 4.5:1 contrast, reduced-motion honoured, keyboard-navigable. <b>(Parts 7\u20138; days 17\u201325)</b>' },
    { id: 'm6', title: 'Milestone 6 \u25b6 Depth', pass: 'Pass: narrate the rendering pipeline in order; fix a real layout-thrash and a stacking-context bug; argue when you would NOT hand-write CSS. <b>(Parts 9\u201312; days 26\u201330)</b>' },
  ],

  sim: [
    { t: 700, l: '<span class="a">cli</span> GET /index.html \u2014 1.1 MB \u2192 78 KB transferred (gzip)' },
    { t: 1000, l: '<span class="a">parse</span> HTML \u2192 DOM tree: tokens \u2192 nodes \u2014 14ms, one pass' },
    { t: 1100, l: '<span class="a">style</span> cascade + custom props resolved: 9,214 rules \u2192 1,402 matched' },
    { t: 1200, l: '<span class="a">layout</span> boxes measured: flex + grid + clamp() resolved \u2014 31ms' },
    { t: 900, l: '<span class="a">paint</span> layers rasterised \u2014 12 composited layers' },
    { t: 900, l: '<span class="a">composite</span> frame sent to GPU \u2014 60fps steady' },
    { t: 1000, l: '<span class="a">metric</span> LCP 0.9s \u00b7 INP 12ms \u00b7 CLS 0.00 — Core Web Vitals: all green' },
    { t: 800, l: '<span class="ok">OK</span> shipped. 4.5:1 everywhere. keyboard-navigable.' },
  ],
};