// python pedagogy — authored from python-curriculum.md (current 2025)
export default {
  title: 'The Python Shipyard - Python: A First-Principles Curriculum',
  brand: 'THE PYTHON SHIPYARD',
  tagline: 'Python · object model · mutability · protocols',
  storageKey: 'shipyard:python:v1',
  legacyStorageKeys: [],
  stackLabel: 'THE STACK — your object model',
  ranks: ['SWABBIE', 'DECKHAND', 'BOATSWAIN', 'FIRST MATE', 'PYTHONISTA', 'HARBOR MASTER'],
  accent: '#16b981',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum python-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0–14 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: names are labels, not boxes. Most Python bugs are Idea 1 wearing a costume.</span></div>',
    '<div class="bl">AWAITING CREW — 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">▮</span></div>',
  ],
  h1: 'PYTHON, UNDERSTOOD.<br><span class="wh">NOT JUST SNIPPED.</span>',
  sub: 'This is a first-principles curriculum rebuilt as a mission. Learn the object model <b>deeply</b> — names are labels, not boxes; mutability divides all surprising behaviour — reference the rest, and physically <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The Python Shipyard is a faithful interactive conversion of the first-principles Python curriculum — every concept, table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file — copy it anywhere, it runs offline.',
    rule: 'a name is a label, not a box. Hold that and Python never surprises you.',
  },
  chips: { p10: ['<span class="m">★ <b>INTERVIEW GOLD</b></span>'] },
  grad: {
    title: 'INTERPRETER READY 🐍',
    rule: 'a name is a label, not a box. Hold that and Python never surprises you.',
  },
  console: {
    cmd: '$ python -X dev main.py',
    line: 'running  ·  no surprises left  ·  (yours — every layer earned)',
  },
  simPrompt: '>>> a = [1, 2, 3]; b = a; b.append(4)',
  egg: { label: 'REPL MODE ENGAGED' },
  deckIn: 'p13',
  milesIn: 'p14',
  simIn: 'p10',
  deckNote: 'If you can deliver these aloud — with the WHAT → WHY IT EXISTS → ONE CONCRETE CONSEQUENCE shape — you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof, mapped to the curriculum&#39;s day plan. When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'Why does Python use indentation as syntax, where most languages use braces?', o: ['A deliberate bet that code is read more often than it is written — you cannot write badly-formatted Python', 'Because Guido preferred how ABC’s whitespace-based layout looked, and the habit simply carried over', 'A leftover compromise from Python’s early design era that later versions never got around to removing', 'Because omitting braces makes source files meaningfully smaller and quicker for the interpreter to parse'], a: 0, e: 'Readability as a language-level constraint — indentation IS syntax. That bet is why Python became the default teaching and scripting language.' },
      { q: 'Python won scientific computing because: (Part 1 says)', o: ['Cython automatically compiles ordinary Python code straight down to native machine code before it runs', 'Python is pleasant to write around fast C libraries — numpy, pandas, torch are C underneath', 'Python’s own interpreter loop is simply the fastest available implementation for numerical work', 'The GIL was removed years ago specifically so libraries like numpy could parallelize array math freely'], a: 1, e: 'Glue, not engine: the speed lives in C; Python orchestrates. The loop you write is the bottleneck; the operation you hand to pandas is not.' },
      { q: 'Why do Python docs and tutorials use spam/eggs?', o: ['After Monty Python’s Flying Circus — the language’s namesake', 'A reference to an old Unix filesystem in-joke about test data that predates Python itself', 'Supposedly Guido van Rossum’s favourite sandwich order from his university days', 'A convention borrowed from academic type-theory papers Guido happened to be reading'], a: 0, e: 'Named after Monty Python’s Flying Circus, not the snake.' },
      { q: 'The interview framing that reads senior: Python chose ___ over ___.', o: ['correctness over compatibility — and paid twelve painful years of ecosystem split', 'machine time over programmer time — optimizing raw execution speed above how pleasant the language is to write', 'compatibility over correctness — preserving every old API forever rather than ever breaking anything', 'speed over readability — favouring terse, dense syntax over code that’s easy for humans to scan'], a: 0, e: 'Python 3.0 broke backwards compatibility to fix text/bytes, division, print. Contrast with JS’s “never break the web”.' },
    ],
    p2: [
      { q: 'a = [1,2]; b = a; b.append(3) — what is a?', o: ['[1, 2] — b is a fresh copy of a, so appending to b never touches a’s contents', '[1, 2, 3] — b is another NAME for the SAME list object', 'Depends entirely on the interpreter — CPython copies but other implementations may alias', 'NameError — b was never declared as its own separate variable before the append call'], a: 1, e: 'Assignment binds a name; it never copies. Mutating the object through either name is visible through both.' },
      { q: 'def add(item, target=[]) — when is the default list created?', o: ['Never — Python rejects mutable default arguments outright and refuses to define the function', 'Once, at function definition — and shared across every call', 'On every call — Python quietly re-evaluates and rebuilds the default argument each time', 'Only on the very first call, then it is cached and frozen for every call after that'], a: 1, e: 'The default is evaluated once at def time; mutations persist across calls. Fix: target=None, then create a fresh list inside.' },
      { q: 'Why can a tuple be a dict key but a list cannot?', o: ['CPython forbids it purely for performance reasons, unrelated to anything conceptual about the types', 'Dict keys must be hashable, and hashing requires the value never change — only immutable objects qualify', 'Lists are reserved exclusively for storing dict values, never for storing its keys', 'Tuples simply take up less memory than lists, so the interpreter prefers them as keys'], a: 1, e: 'Hashing requires immutability — that is the whole reason tuples exist alongside lists.' },
      { q: 'Why does for line in open(f) stream a 10GB file while open(f).readlines() does not?', o: ['readlines() is deprecated in modern Python, so for silently falls back to a streaming mode', 'A file IS an iterator: for calls iter() then next() until StopIteration — one line in memory at a time', 'open() secretly inspects the file’s size first and switches strategy automatically for large files', 'The OS page cache keeps the whole file resident in RAM anyway, so there is no real memory difference'], a: 1, e: 'Iteration is a protocol (iter/next/StopIteration), and laziness means nothing is materialised until asked. Generators (Part 4) are how you write your own.' },
      { q: 'Why does count += 1 inside a function raise UnboundLocalError when print(count) alone would not?', o: ['count is treated as a reserved keyword inside function bodies, so it can never be reassigned', 'Assigning to a name anywhere in a function makes it local for the ENTIRE function', 'Globals can never be incremented from inside a function without an explicit return statement', 'A Python 3 regression that was later fixed in newer minor releases but still appears in old code'], a: 1, e: 'LEGB scope: assignment creates a local for the whole function, even before the assignment line. Fix: return values instead of mutating globals.' },
    ],
    p3: [
      { q: 'x in some_list vs x in some_set — the single most valuable performance instinct:', o: ['list is O(n), set is O(1) — convert before membership loops to kill accidental O(n²)', 'set membership checks are actually O(n) once Python has to walk past hash collisions', 'both list and set give O(1) membership lookups, so converting between them changes nothing', 'list is generally faster than set for small collections, so converting rarely pays off'], a: 0, e: 'The most common real speedup in data-cleaning: turn a list into a set before a loop of membership checks — a 40-minute job becomes 4 seconds.' },
      { q: 'dicts preserve insertion order since:', o: ['Python 3.7 (language guarantee)', 'never — dicts have always been fundamentally unordered and any order you see is coincidence', 'Python 2.7, though only as an accidental CPython implementation detail, not a real guarantee', '3.10, when it was finally added to the formal language specification'], a: 0, e: 'Insertion order is guaranteed since 3.7. pairs well with the workhorse table: dict = O(1) lookup by key.' },
      { q: 'Building a string with += in a loop is:', o: ['Perfectly fine for any loop size, since Python optimizes repeated string concatenation internally', 'O(n²) — use ’’.join(parts)', 'Actually faster than calling .join() because it skips building an intermediate list first', 'Simply impossible — Python raises an error the moment you try to concatenate inside a loop'], a: 1, e: 'Strings are immutable: every “modification” creates a new string, so a loop of += is quadratic. Join builds once.' },
      { q: 'A client CSV raises UnicodeDecodeError. First move:', o: ['Ignore it entirely, since pandas automatically detects and handles every possible text encoding', 'Specify encoding= explicitly; try utf-8-sig for Excel exports with a BOM', 'Open the file in raw binary mode and manually strip out anything that looks non-ASCII', 'Delete every row containing non-ASCII characters until the decode error stops appearing'], a: 1, e: 'Text is str, bytes are bytes; decode input with the right encoding. utf-8-sig absorbs Excel’s BOM. This is the most common data-work error.' },
    ],
    p4: [
      { q: 'fns = [lambda: i for i in range(3)]; [f() for f in fns] evaluates to:', o: ['[0, 1, 2] — each lambda captures the value i held at the moment it was created', '[2, 2, 2] — closures capture the NAME, bound at call time', 'NameError — i no longer exists by the time any of the lambdas actually get called', 'Independent copies of i frozen inside each lambda at definition time, like a snapshot'], a: 1, e: 'Python’s closure bug: late binding — the closure captures the name, not the value. Fix: bind at definition, lambda i=i: i.' },
      { q: 'A decorator is:', o: ['A function that takes a function and returns a replacement — @ is sugar for f = deco(f)', 'A compiler directive that tells CPython to optimize or inline the function it is attached to', 'A kind of macro that gets textually expanded into the function’s body before it runs', 'An inheritance mechanism that lets one function borrow behaviour from another automatically'], a: 0, e: 'Once you see @app.get(...), @pytest.fixture and @functools.lru_cache as “a function wrapping my function”, frameworks stop being magic — a strong interview answer.' },
      { q: 'Generators vs pandas for a huge transform — the judgment from Part 4.4:', o: ['Stream (yield) for huge files with row-independent logic; vectorize in pandas when it fits in memory', 'Always vectorize in pandas first, since it beats a generator in literally every situation', 'Always stream with generators, no matter the file size or whether pandas would fit it in memory', 'Streaming only works if numpy is installed, since generators depend on it internally'], a: 0, e: 'Know when to stream versus vectorize — that is real engineering judgment, and it shows up in data-pipeline interviews.' },
      { q: 'def fetch(table, *cols, limit=100, **filters): fetch(‘cases’, ‘id’) — what is cols?', o: ['A tuple: (‘id’,)', 'A single-element list containing just ‘id’, since extra positionals are always listy', 'A plain dictionary mapping each extra positional argument’s name to its value', 'A special keyword-only set that Python builds automatically from bare positional args'], a: 0, e: '*args collects extra positionals into a tuple; **kwargs into a dict. Keyword-only params (after *) force self-documenting call sites.' },
    ],
    p5: [
      { q: 'The data model: len(x) calls __len__, a + b calls __add__, for x in y calls __iter__. That means:', o: ['Python syntax is defined in terms of dunder PROTOCOLS your objects can implement', 'Dunders only work on Python’s own built-in types, never on classes you define yourself', 'Python is secretly built as one giant inheritance hierarchy underneath all of this syntax', 'Protocols only work if your class explicitly inherits from an abstract base class'], a: 0, e: 'The real secret: implement the protocol and your object works with the whole language — for, in, len(), list(), comprehensions: no inheritance required.' },
      { q: 'What does @dataclass generate for you?', o: ['__init__, __repr__, __eq__ — and default_factory FORCES you to avoid the mutable-default trap', 'A complete database schema definition, including table names, columns and foreign keys', 'Only a __slots__ declaration, nothing else — you still have to write __init__ by hand', 'A full object-relational mapper capable of querying and persisting instances to a database'], a: 0, e: 'For records (most data work), dataclasses remove nearly all boilerplate — and the API was shaped by how common the =[] trap is.' },
      { q: 'When is a class actually warranted?', o: ['State plus behaviour that belong together, instantiated multiple times', 'Never — Python’s philosophy discourages classes entirely in favour of plain functions', 'Every module boundary should be wrapped in its own class for proper encapsulation', 'Any group of related functions, even ones sharing no state, benefits from being a class'], a: 0, e: 'A module of functions is often better. A class as a namespace for stateless functions is a Java habit, and un-Pythonic.' },
      { q: '@property turns a method into:', o: ['Attribute access — so validation/computation can be added later without changing call sites', 'A classmethod, meaning it receives the class itself instead of an instance as its first argument', 'A value that gets computed once and then cached forever for the lifetime of the object', 'A dunder method, invoked implicitly the same way __init__ or __len__ are'], a: 0, e: 'Change the implementation without breaking the API. Composition beats inheritance for most “is-a” urges; super() follows a linearised MRO.' },
    ],
    p6: [
      { q: 'The idiomatic Python error philosophy is:', o: ['LBYL — always check every precondition before acting, to avoid triggering exceptions at all', 'EAFP — try it and handle failure; StopIteration ends every for loop', 'Never catch exceptions anywhere — let every single one propagate all the way to a crash', 'Return codes everywhere instead of raising, checked explicitly after each function call'], a: 1, e: 'Exceptions are normal control flow in Python. Checking-then-acting has race conditions and reads worse.' },
      { q: 'Why is bare except: wrong?', o: ['It runs measurably slower than a targeted except clause due to extra interpreter overhead', 'It swallows KeyboardInterrupt and every bug you have not thought of', 'It is purely a style preference that linters complain about but causes no real problems', 'It only becomes a problem once you add a logger and start inspecting exceptions'], a: 1, e: 'Catch the narrowest exception you can actually handle; never silence one without logging; raise NewError(...) from e preserves the cause.' },
      { q: 'with open(...) as f: guarantees:', o: ['The file loads eagerly the moment the with block begins, before any read() is called', 'Cleanup even on exception — hand-written .close() is a future bug', 'Exclusive access, blocking every other process from touching the file at the same time', 'Lazy reads by default, deferring every byte until something explicitly asks for it'], a: 1, e: 'Context managers guarantee cleanup for files, DB connections, transactions, locks, temp dirs. If you write .close() by hand, you are writing a future bug.' },
      { q: 'raise ValidationError(...) from e — what does from e do?', o: ['Preserves the original exception as the cause in the traceback', 'Renames the exception class to whatever e happens to be at the time it is raised', 'Deletes the traceback entirely so only the new exception message is ever shown', 'Is deprecated as of 3.11 in favour of the newer exception-grouping syntax'], a: 0, e: 'The difference between a debuggable error and a mystery. Define your own exception classes so callers can distinguish bad data from a dead network.' },
    ],
    p7: [
      { q: 'The if __name__ == "__main__" guard exists because:', o: ['Importing a module RUNS it — the guard separates “run as a script” from “imported as a library”', 'It speeds up imports by letting the interpreter skip parsing the guarded block entirely', 'It is required by PEP 8, purely a formatting convention with no runtime effect', 'It prevents circular imports by breaking the dependency cycle at import time'], a: 0, e: 'The same file can be both a script and a library if the entry code is guarded.' },
      { q: 'The #1 cause of import errors is:', o: ['Circular imports only — two modules that each try to import the other at load time', 'Where you ran python from — sys.path starts with the script’s directory; use python -m for packages', 'Missing __init__.py files, which Python 3 still silently requires in every package', 'Unicode characters in file names confusing the import machinery on some platforms'], a: 1, e: 'python src/main.py and python -m src.main produce different sys.path and different results. When imports break, check your run command first.' },
      { q: 'venv is to Python packages what ___ is to system libraries:', o: ['A container', 'A virtual machine, fully emulating separate hardware for each project’s dependencies', 'A compiler that rebuilds every dependency from source for the current environment', 'A linter that flags dependency conflicts without actually isolating anything'], a: 0, e: 'The parallel is explicit in the curriculum: neither replaces the other — Dockerized Python still uses a venv or a clean base image.' },
      { q: 'The src/ project layout exists to:', o: ['Prevent importing your package from the working directory instead of the installed copy — the “works on my machine” guard', 'Hide secrets and credentials from version control by keeping them out of the top-level directory', 'Speed up mypy by giving it a smaller, more predictable directory tree to type-check', 'Comply with PEP 517’s build-backend requirements, which mandate a src/ directory'], a: 0, e: 'src/ layout stops the accidental wrong-copy import that causes exactly the “works locally, fails in CI” failure mode.' },
    ],
    p8: [
      { q: 'The ETL definition of done includes all EXCEPT:', o: ['Runs on a 500MB file without exhausting memory', 'Uses the fastest possible single-threaded loop, tuned for raw execution speed above all else', 'A malformed row cannot kill the run — it goes to a rejects file instead', 'Re-running twice produces the same database state, with no duplicated rows'], a: 1, e: 'Idempotent, memory-safe, failures isolated — and someone else can run it from the README alone.' },
      { q: 'A malformed row must be:', o: ['Silently dropped to keep the run moving, with no record that it was ever discarded', 'Written to a rejects file with reasons — never silent', 'Allowed to crash the run entirely so nobody misses that something went wrong', 'Logged at most once per run, regardless of how many malformed rows appear'], a: 1, e: 'Bad rows to rejects with reasons is your S2T-mapping instinct expressed in code.' },
      { q: 'Where are Python type hints actually used at RUNTIME?', o: ['Never — annotations are stripped before the interpreter even sees the function body', 'FastAPI and pydantic — which read annotations deliberately for validation', 'mypy and pyright, which run as a background process alongside the interpreter', 'CPython’s own compiler, which uses annotations to generate faster bytecode'], a: 1, e: 'The runtime ignores annotations everywhere else. Project B teaches typing in anger: FastAPI converts hints into real validation and OpenAPI docs.' },
      { q: 'The pairing lesson of the two projects:', o: ['A teaches data correctness, generators, errors, stdlib; B teaches typing-in-anger, async, service shape', 'A is for beginners only, while B is the only one worth mentioning in an interview', 'Only B matters for interviews, since A doesn’t touch anything resembling production code', 'Both are essentially calculators — neither project reflects real production concerns'], a: 0, e: 'Together they are a portfolio piece that maps directly onto a technical BA / analytics-engineering conversation.' },
    ],
    p9: [
      { q: 'How do you read a Python traceback?', o: ['Top to bottom — the oldest frame at the top matters most, everything below is noise', 'Bottom up: oldest call first, error last — the last line is the exception, the line above is where it happened', 'Alphabetically, by scanning for the module name that appears earliest in the file listing', 'Only the module names matter; the actual line numbers rarely point at the real bug'], a: 1, e: 'The answer is at the bottom: exception + message, then where it happened, then the frames that led there.' },
      { q: 'AttributeError: ‘NoneType’ object has no attribute ‘x’ means:', o: ['x is simply misspelled somewhere in the object’s class definition', 'Something returned None — usually a function with no return, or a lookup that missed; inspect one step earlier', 'None is a reserved word that cannot legally have attributes accessed on it at all', 'Your class definition itself is broken and needs to be rewritten from scratch'], a: 1, e: 'Follow the data one step earlier: what produced None? That is the bug, not the AttributeError.' },
      { q: 'Mysteriously shared state across calls is:', o: ['A caching bug somewhere deep in CPython’s own interpreter internals', 'A GIL issue, since the interpreter is quietly sharing state between concurrent calls', 'A mutable default (=[]) in a signature, or an aliased object — Idea 2 in the wild', 'Normal Python behaviour that you are simply expected to work around every time'], a: 2, e: 'Search signatures for =[] / ={}; copy.deepcopy to test the hypothesis.' },
      { q: 'Replace print() debugging with:', o: ['breakpoint() (pdb: n next, s step, c continue, p expr) — six commands beat a hundred prints', 'F-strings everywhere, so at least the printed output is more readable than before', 'More print statements, placed even more densely throughout the suspect function', 'Dedicated tracing tools only — pdb is considered outdated for modern debugging'], a: 0, e: 'The habit that most speeds up debugging. And cProfile before optimizing anything — measure, never guess: Python performance intuitions are wrong more often than right.' },
    ],
    p10: [
      { q: 'Running python script.py does this in order:', o: ['Compile directly to machine code, the same way a C compiler would before execution', 'Parse → AST → compile to bytecode (.pyc in __pycache__) → execute on a stack-based VM loop', 'Bytecode → AST → source, reversing the usual compilation pipeline entirely', 'Interpret the source character by character, with no intermediate representation at all'], a: 1, e: 'There is no machine-code compilation in CPython — the .pyc bytecode runs on an interpreter loop. PyPy/Numba/Cython are where “faster Python” comes from.' },
      { q: 'Why are Python loops slow, and what do you actually do?', o: ['Use PyPy always, regardless of whether the workload is CPU-bound or I/O-bound', 'Add type hints everywhere, since the interpreter uses them to skip dynamic dispatch', 'Write cleverer Python micro-tricks that shave overhead off each individual loop iteration', 'Hand the loop to C — numpy/pandas vectorization; every op in the interpreter pays dynamic dispatch + allocation'], a: 3, e: 'The fix is not cleverness — it is pushing work to C. Exactly the Part 1 glue-language point.' },
      { q: 'CPython frees an object:', o: ['Only when the process exits, holding every allocation in memory until then', 'The moment its reference count hits zero — deterministic, unlike JS GC; a generational collector handles cycles', 'After a fixed 60 second window, regardless of how many references still point to it', 'Never — Python is documented as intentionally leaking memory by design'], a: 1, e: 'Refcount + cycle collector. “Memory not released” usually means a lingering reference: module cache, closure, or a traceback holding frames.' },
      { q: 'The accurate GIL claim is:', o: ['The GIL was fully removed starting in Python 3.12, enabling true parallel threads', 'Python threads give you I/O CONCURRENCY but not CPU parallelism — the GIL is released during I/O waits', 'Python cannot do concurrency of any kind, with or without threads or asyncio', 'Threads are consistently faster than separate processes for CPU-bound work'], a: 1, e: 'Network/DB/file work: threads or asyncio overlap. Number crunching: multiprocessing (or numpy, which releases the GIL in C). PEP 703 free-threaded builds are experimental as of 3.13 — attach a date to that claim.' },
    ],
    p11: [
      { q: 'Type hints at runtime:', o: ['Are ignored — annotations are for tooling and humans; validate external data with pydantic', 'Enforce types everywhere automatically, raising a TypeError the moment they are violated', 'Are required by PEP 8, so omitting them is treated as a hard style violation', 'Slow down CPython noticeably, since every call site now checks annotations'], a: 0, e: 'Pragmatic policy: annotate signatures at module boundaries, run mypy in CI. Types are not runtime validation.' },
      { q: 'pytest parametrize exists for:', o: ['Table-driven cases — the same instinct as a BA test plan', 'Generating coverage reports automatically without writing any additional test cases', 'Replacing fixtures entirely, since parametrize can do everything fixtures used to do', 'Running tests in parallel across multiple worker processes at once'], a: 0, e: 'Test the logic that would embarrass you if wrong: validation, transforms, money and date edge cases. Chase cases, not coverage percentages.' },
      { q: 'The performance order that works:', o: ['Rewrite the hot path in Rust first, before measuring where the actual bottleneck is', 'Add more worker processes until the workload finishes in an acceptable amount of time', 'Reach for asyncio to speed up CPU-bound math, since concurrency generally helps performance', 'Measure (cProfile) → fix the algorithm (set-vs-list!) → vectorize/push into C → parallelize per the GIL table → only then exotic compilers'], a: 3, e: 'The set-vs-list fix beats every micro-optimization. Measure first: the bottleneck is almost never where you assumed.' },
      { q: 'SQL in Python must be:', o: ['Parameterized — never string concatenation: cur.execute(“... WHERE id = %s”, (id,))', 'Pandas-only, since raw SQL strings are considered unsafe in any Python codebase', 'Built with f-strings for speed, since parameterization adds measurable query overhead', 'Prepared by hand each time, re-typing the full statement for every single call site'], a: 0, e: 'Never build SQL by concatenation; never eval/exec on input; secrets in env vars, never git; ruff ends all style debate.' },
    ],
    p12: [
      { q: 'Python is the wrong tool for:', o: ['ETL pipelines that move and clean data between a handful of systems on a schedule', 'CPU-bound hot loops that cannot vectorize — Go/Rust/C++ win by an order of magnitude', 'Automation scripts that glue together a few existing command-line tools', 'FastAPI backends serving moderate traffic behind a typical REST API'], a: 1, e: 'Also: hard real-time, browser frontends, mobile, shipping binaries to non-technical users, and very large undisciplined codebases.' },
      { q: 'Python’s honest weaknesses to name in an interview:', o: ['GIL for CPU parallelism, packaging/environment history, startup time, per-object memory', 'The whitespace-based syntax itself, which most experienced engineers still find unpleasant', 'It is too slow for essentially every kind of workload you would use it for', 'Nothing — there is no honest weakness worth naming in a technical interview'], a: 0, e: 'Naming the weaknesses with dates and versions makes your enthusiasm credible rather than naive.' },
      { q: 'How do the GIL claims stay current?', o: ['Attach versions: as of 3.13 there is an experimental free-threaded build (PEP 703)', 'Repeat whatever you originally learned about the GIL, since the fundamentals never change', 'Ignore versioning entirely and speak about the GIL only in the present tense', 'Avoid mentioning the GIL at all, since it is considered an outdated implementation detail'], a: 0, e: '“As of X it is experimental” is the difference between current and stale knowledge.' },
      { q: 'Streaming vs vectorizing a large transform — the decision:', o: ['Always stream with generators, regardless of whether the data would fit comfortably in memory', 'Always vectorize in pandas, even when the file is too large to load into memory at all', 'Stream when files are huge and logic is row-independent; vectorize when it fits in memory and math is columnar', 'Ask the database to do the transform instead, since Python is rarely fast enough either way'], a: 2, e: 'The Part 4.4 / 8.1 judgment — one of the most senior signals on this curriculum.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 ▶ list vs tuple vs dict vs set — and the complexity of a membership test in each.', a: 'list/tuple: O(n) membership, O(1) index; dict/set: O(1) by key/hash. dict preserves insertion order (3.7+). tuple = immutable records, dict keys; set = uniqueness + algebra. (Part 3.1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 ▶ Mutable vs immutable — and the mutable default argument trap.', a: 'Immutable: int, float, str, bytes, tuple, frozenset, None. Mutable: list, dict, set, instances. Defaults evaluate once at def time — use target=None and build inside. (Part 2, Idea 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 ▶ Is Python pass-by-value or pass-by-reference?', a: 'Neither — pass-by-object-reference: the name (not a copy) is passed; rebinding inside does not affect the caller, mutation does. Explain via Idea 1: names are labels. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 ▶ What is a comprehension; when do you use a generator instead?', a: 'Comprehension = materialised list/dict/set from a transform (+ optional where-clause). Generator = lazy, one item at a time, constant memory — huge files, streaming pipelines. (Parts 3.3, 4.4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 ▶ is vs ==?', a: '== compares value; is compares identity (id()). a is b can be True for interned small ints/short strings — an implementation detail, never rely on it. (Part 2, Idea 1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 ▶ How do exceptions work; why is bare except wrong?', a: 'Exceptions are normal control flow (StopIteration ends every for). Bare except swallows KeyboardInterrupt and unknown bugs — catch the narrowest thing you can handle, log, re-raise with raise ... from e. (Part 6.1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 ▶ *args / **kwargs?', a: '*args gathers extra positionals into a tuple; **kwargs extra keywords into a dict; keyword-only params after * force self-documenting call sites. (Part 4.1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 ▶ What does if __name__ == "__main__" do and why is it there?', a: 'Importing runs the module; the guard separates script-entry from library-import so one file can be both. (Part 7.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 ▶ Explain the GIL; when do you use threads vs processes vs asyncio?', a: 'GIL serialises bytecode execution (keeps refcounting cheap) but is released during I/O. I/O: threads/asyncio overlap. CPU: multiprocessing (separate GILs) or numpy (releases GIL in C). 3.13: experimental free-threaded build (PEP 703). (Part 10.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 ▶ Generators vs lists — memory and use cases?', a: 'List materialises everything (O(n) memory now); generator yields lazily, holds one item, suspends/resumes — streams, big files, pipeline composition. Trade: no len/indexing, single pass. (Part 4.4)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 ▶ Shallow vs deep copy?', a: 'Shallow (.copy(), = slicing): new top container, nested objects SHARED. Deep (copy.deepcopy): fully independent. Mutations through the shallow copy hit the original’s nested data. (Part 2, Idea 2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 ▶ What does with give you that try/finally does not?', a: 'The same cleanup guarantee with less ceremony and no ability to forget it — __enter__/__exit__ protocol behind files, connections, transactions, locks. Hand-written .close() is a future bug. (Part 6.2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 ▶ Implementing __iter__ and __len__ — what do you get?', a: 'for, len(), in, list(), comprehensions, unpacking, slicing (with __getitem__) — the language working with YOUR object via protocols, no inheritance. (Part 5.2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 ▶ LEGB scope — why does count += 1 raise UnboundLocalError?', a: 'Lookup is Local → Enclosing → Global → Builtins. Assignment anywhere in a function makes the name local for the whole function. Fix: return values, not global mutation. (Part 2, Idea 4)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 ▶ What happens when you run a .py file?', a: 'Parse → AST → compile to bytecode (cached .pyc) → stack-based VM loop. No machine-code compilation in CPython — that is PyPy, Numba, Cython. (Part 10.1)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 ▶ How does CPython manage memory, and what leaks?', a: 'Reference counting frees deterministically at zero; generational collector handles cycles. Leaks = lingering references: module caches, closures, tracebacks holding frames, weakrefs ignored. (Part 10.2)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 ▶ Why are Python loops slow — and what do you actually do about it?', a: 'Interpreter loop + dynamic dispatch + allocation per op, versus a few C instructions. Fix in order: measure (cProfile), fix the algorithm (set-vs-list), vectorize/numpy, parallelize per GIL table. (Parts 1, 10.1, 11.3)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 ▶ Streaming vs vectorizing a large transform — how do you decide?', a: 'Row-independent logic on huge files: stream with generators (constant memory). Columnar math that fits in memory: vectorize in pandas. The comparison write-up is the real lesson. (Parts 4.4, 8.1)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 ▶ Model', pass: 'Pass: the five Part 2 checkpoint questions, unaided, out loud. <b>(Parts 1–2; ~days 1–3)</b>' },
    { id: 'm2', title: 'Milestone 2 ▶ Data types', pass: 'Pass: choose the right structure for five described problems and justify each on complexity; convert a nested loop into a set-based O(n) version. <b>(Part 3; days 4–6)</b>' },
    { id: 'm3', title: 'Milestone 3 ▶ Functions', pass: 'Pass: write a @retry decorator and a streaming generator pipeline from scratch; explain what implementing __iter__ gives you. <b>(Parts 4–5; days 7–12)</b>' },
    { id: 'm4', title: 'Milestone 4 ▶ Robustness', pass: 'Pass: a script with proper exceptions, logging, a context manager, and a layout that installs in a fresh venv from pyproject.toml. <b>(Parts 6–7; days 13–16)</b>' },
    { id: 'm5', title: 'Milestone 5 ▶ Projects', pass: 'Pass: the ETL pipeline meeting every definition-of-done item plus the pandas comparison written out; the FastAPI service running in a container. <b>(Part 8; days 17–25)</b>' },
    { id: 'm6', title: 'Milestone 6 ▶ Depth', pass: 'Pass: debug an unfamiliar failing script from the traceback alone; deliver the GIL answer with the threads/processes/asyncio table from memory; argue two cases where Python is the wrong tool. <b>(Parts 9–12; days 26–30)</b>' },
  ],

  sim: [
    { t: 800, l: '<span class="a">cli</span> $ python etl.py --input huge.csv --db local' },
    { t: 1200, l: '<span class="a">generator</span> read_rows() streaming huge.csv — one row in memory' },
    { t: 1400, l: '<span class="a">validate</span> 1,204 rows ok · 3 bad → rejects file with reasons' },
    { t: 1200, l: '<span class="a">transform</span> dates → UTC · money → Decimal · ids deduped via set' },
    { t: 1300, l: '<span class="a">load</span> upsert into postgres inside <span class="c">with psycopg.connect(...)</span> — batched' },
    { t: 1000, l: '<span class="a">observe</span> logged: rows in 1207 / loaded 1204 / rejected 3 / 2.1s' },
    { t: 1200, l: '<span class="a">test</span> pytest: 42 passed — re-run again, same state (idempotent)' },
    { t: 900, l: '<span class="ok">OK</span> landed. peak memory 84 MB — not 2 GB.' },
  ],
};
