// rest-api pedagogy — authored from rest-api-curriculum.md
export default {
  title: 'The REST-API Shipyard - REST APIs: A First-Principles Curriculum',
  brand: 'THE REST-API SHIPYARD',
  tagline: 'REST \u00b7 resources & representations \u00b7 HTTP \u00b7 idempotency',
  storageKey: 'shipyard:rest-api:v1',
  accent: '#14b8a6',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum rest-api-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0\u201314 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: REST is a set of constraints, not a JSON convention. Every \u201crule\u201d is a consequence of a problem someone already hit.</span></div>',
    '<div class="bl">AWAITING CREW \u2014 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">\u258e</span></div>',
  ],
  h1: 'REST, UNDERSTOOD.<br><span class="wh">NOT JUST SNIPPED.</span>',
  sub: 'First-principles curriculum rebuilt as a mission. Learn the resource model, method semantics (safety and idempotency), statelessness, the status families, and the auth model <b>deeply</b> \u2014 reference the rest \u2014 and <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The REST-API Shipyard is a faithful interactive conversion of the first-principles REST APIs curriculum \u2014 every table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file \u2014 copy it anywhere, it runs offline.',
    rule: 'express state through representations, never through verbs in URLs. Hold that, and every \u201cwhy does this API feel wrong?\u201d answers itself.',
  },
  chips: { p2: ['<span class="m">\u2605 <b>INTERVIEW GOLD</b></span>'], p10: ['<span class="m">\u2605 <b>INTERVIEW GOLD</b></span>'] },
  simIn: 'p10',
  deckNote: 'If you can deliver these aloud \u2014 with the WHAT \u2192 WHY IT EXISTS \u2192 ONE CONCRETE CONSEQUENCE shape \u2014 you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof, mapped to the curriculum&#39;s day plan (~1 focused hour/day, days 1\u201333). When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'The RPC era was built on a bad bet. What was it?', o: ['Make network calls look exactly like local function calls \u2014 which hid latency, partial failure, and the need for retries', 'Return raw database rows', 'Use XML for everything', 'Skip versioning'], a: 0, e: 'Tight coupling (generated interface definitions broke clients on every change), a lying abstraction, enormous ceremony \u2014 and infrastructure could not help, because every call was an opaque POST.' },
      { q: 'Roy Fielding\u2019s actual contribution, stated accurately:', o: ['He invented REST and then built the web from it', 'He helped build HTTP/1.0 and 1.1, then DESCRIBED the principles that made the web scale \u2014 as an architectural style of constraints, not a spec', 'He wrote JSON', 'He invented the URL'], a: 1, e: '2000 PhD dissertation: REST = REpresentational State Transfer. Each constraint buys a property: statelessness \u2192 scaling, cacheability \u2192 fewer round trips, uniform interface \u2192 generic infrastructure.' },
      { q: 'Which two constraints do most of the work \u2014 and what does each buy?', o: ['Cacheability \u2192 no round trips; statelessness \u2192 any server can answer any request', 'Client\u2013server \u2192 pretty UIs; code-on-demand \u2192 JavaScript', 'Uniform interface \u2192 consistency; layering \u2192 proxies', 'Code-on-demand \u2192 JavaScript; cacheability \u2192 speed'], a: 1, e: 'Statelessness: any server handles any request \u2192 horizontal scaling and failover. Cacheability: \u201cthe single biggest reason the web scales\u201d \u2014 the request never happens.' },
      { q: 'Why does REST accept being chatty and verbose compared with RPC?', o: ['REST is older', 'It optimizes for evolvability and scale where you do not control the clients and cannot coordinate deployments \u2014 the public internet trade', 'It is not verbose', 'JSON is tiny'], a: 1, e: 'That is why REST wins for public APIs and is often the wrong trade for two microservices inside one cluster.' },
      { q: 'The honest thing worth saying to an interviewer about \u201cREST APIs\u201d:', o: ['All of them are RESTful', 'Most \u201cREST APIs\u201d \u2014 including famous ones \u2014 are NOT RESTful by Fielding\u2019s definition: they drop hypermedia (HATEOAS) and keep the resource model and HTTP semantics, which turned out to be the valuable part', 'Only SOAP was wrong', 'HATEOAS is mandatory everywhere'], a: 1, e: 'Fielding called this out publicly in 2012. Knowing it \u2014 and what the industry kept \u2014 is a senior answer.' },
    ],
    p2: [
      { q: 'A resource vs a representation:', o: ['The same thing', 'The resource is the abstract thing (a book, an order, today\u2019s weather); what crosses the network is a REPRESENTATION of it \u2014 JSON, CSV, HTML, a PDF of the same resource', 'The representation is the database row', 'The URL is the representation'], a: 1, e: 'The name says it: REpresentational State Transfer. One resource, many representations via the Accept header; the API is a contract deliberately decoupled from your storage.' },
      { q: 'Why is GET /deleteUser?id=5 a bug, not a style choice?', o: ['It is too long', 'GET must be SAFE: crawlers, link previews, and prefetchers issue GETs without asking \u2014 it discards caching, safe retries, and the meaning of the method', 'DELETE is faster', 'Query strings are forbidden'], a: 1, e: 'URLs name things, never actions. The verb belongs in the HTTP method \u2014 which already exists for exactly that purpose.' },
      { q: 'A POST /payments times out. What has possibly happened, and why can\u2019t the client safely retry?', o: ['Nothing, retry freely', 'The payment may have been created and only the response lost \u2014 POST is not idempotent, so retrying may charge twice. The mechanism: idempotency keys', 'POST is idempotent', 'Always retry POST'], a: 1, e: 'The client cannot tell \u201cit didn\u2019t happen\u201d from \u201cit happened but I didn\u2019t hear back.\u201d Networks fail after the server acted but before the response arrived \u2014 the actual hard part of distributed systems.' },
      { q: 'PUT vs PATCH \u2014 omit a field in each and the meaning is:', o: ['Identical', 'PUT: the field should be ABSENT \u2014 you are sending the complete new state. PATCH: leave it ALONE \u2014 you are describing changes', 'PUT patches, PATCH replaces', 'Both leave it alone'], a: 1, e: 'PUT = replace wholesale (idempotent); PATCH = partial change (not necessarily idempotent). The most confused pair in APIs.' },
      { q: 'What statelessness forbids \u2014 and what it makes possible:', o: ['Forbids databases; makes Redis', 'Forbids servers holding CLIENT SESSION state in their own memory \u2014 permits any server answering any request: load-balancing, kill-one-mid-traffic, gradual deploys, autoscaling', 'Forbids authorization; makes caching', 'Forbids cookies; makes JWTs'], a: 1, e: 'It costs: every request re-sends auth context and repeats some work. Note the precision: \u201cstateless\u201d refers to client session state, not data \u2014 shared stores like Redis are fine.' },
      { q: '401 vs 403 \u2014 the load-bearing distinction:', o: ['401 means not authenticated (\u201cI don\u2019t know who you are\u201d); 403 means not permitted (\u201cI know exactly who you are, and no\u201d)', '401 means forbidden, 403 means unauthenticated', 'They are interchangeable', '401 is for browsers, 403 for apps'], a: 1, e: 'Asked in almost every API interview. And 4xx vs 5xx tells a client whether retrying helps \u2014 retry 5xx, fix 4xx.' },
      { q: 'Which \u201charmless\u201d change quietly BREAKS clients?', o: ['Adding an optional request field', 'Adding a field to a response', 'Tightening validation \u2014 clients that were sending sloppy-but-accepted data suddenly fail; clients should tolerate unknown fields, servers should only add', 'Relaxing validation'], a: 1, e: 'The backward-compatibility discipline: servers add rather than change; clients tolerate rather than assume. Adding an enum value also breaks clients that hard-coded a switch.' },
    ],
    p3: [
      { q: 'The four parts of every HTTP request and response:', o: ['URL, headers, cookies, body', 'Start line, headers, blank line, body \u2014 that is the whole protocol shape; curl -v shows it to you', 'Method, path, version, JSON', 'Status, reason, bytes, time'], a: 1, e: 'Running `curl -v` against any API once, deliberately, is worth more than three tutorials.' },
      { q: 'Content-Type vs Accept, and the codes for rejection:', o: ['They are synonyms', 'Content-Type = format of what you SEND; Accept = format you can RECEIVE. Can\u2019t satisfy Accept \u2192 406; can\u2019t parse what was sent \u2192 415', 'Accept is deprecated', 'Both mean JSON'], a: 1, e: '406 Not Acceptable vs 415 Unsupported Media Type \u2014 the easiest pair to mix up: one is about the response, the other about the request.' },
      { q: 'Where does a piece of data belong? The rule that resolves nearly every case:', o: ['Path identifies WHICH resource; query modifies HOW a collection is returned; headers carry metadata about the request; body carries data being created or updated', 'Everything in the query string for simplicity', 'Everything in the body', 'Everything in headers'], a: 1, e: 'Never put secrets in the query string (URLs end up in a dozen logs), and never send a body on GET (intermediaries and caches ignore it).' },
      { q: 'Why is a body on GET a bad idea?', o: ['GET is slower', 'Request bodies on GET are not reliably supported \u2014 intermediaries, caches, and many clients ignore them', 'Bodies are illegal', 'GET is for browsers only'], a: 1, e: 'Same reason secrets must not live in URLs: intermediaries are part of the contract you cannot control.' },
    ],
    p4: [
      { q: 'URLs identify nouns; the verbs come from HTTP. Which pair is correct?', o: ['POST /createBook, DELETE /deleteBook?id=5', 'POST /books, DELETE /books/42', 'GET /getBook?id=42', 'PATCH /updateBook'], a: 1, e: 'The verb-shaped URL isn\u2019t merely unfashionable \u2014 it discards what HTTP gives for free: caches can\u2019t tell reads from writes, retries become unsafe, and a crawler can destroy data.' },
      { q: 'Nesting rule for resource URLs:', o: ['Nest as deep as your data model goes', 'Nest to express containment, stop at two levels \u2014 /authors/7/books fine; five levels deep encodes your data model into every client\u2019s code', 'Never nest at all', 'Nesting is for views only'], a: 1, e: 'Once a resource has its own identity, give it a top-level URL \u2014 /reviews/9/comments or /comments?review_id=9.' },
      { q: 'Cancel an order. The three legitimate approaches in order of RESTfulness:', o: ['A: PATCH /orders/42 {"status":"cancelled"} \u2014 B: POST /orders/42/cancellation (event as sub-resource) \u2014 C: POST /orders/42/cancel (explicit, pragmatic)', 'C: always', 'A: never', 'A, C, B \u2014 order is irrelevant'], a: 1, e: 'A when it\u2019s really a field; B when the action has its own history worth recording (honest, idempotency-friendly); C when the operation is a genuine command \u2014 Stripe and GitHub do C. Defending the choice beats purity.' },
      { q: 'The most common tell of an RPC-shaped API:', o: ['Too many 200s', 'Verbs in URLs \u2014 /api/getUsersList \u2014 plus POST for everything, which throws away safety, idempotency, and caching in one move', 'snake_case', 'Pagination'], a: 1, e: 'Other tells: /tbl_users database internals, inconsistent pluralization, five-level nesting, and response shapes that change per parameter.' },
    ],
    p5: [
      { q: 'Money and 64-bit IDs in JSON \u2014 the safe forms:', o: ['Numbers always', 'Money as a string or minor units (\u201c12.99\u201d or 1299) \u2014 JSON numbers are IEEE-754 doubles, 12.99 can arrive as 12.989999999999998; large IDs as strings \u2014 JS loses precision past 2^53', 'Base64 both', 'Integers for money, strings for IDs'], a: 1, e: 'Both are real bugs that have cost real money. Timestamps: ISO 8601, UTC, explicit offset \u2014 never bare \u201c11/08/2026\u201d.' },
      { q: 'A 200 response containing {"error": "not found"} is:', o: ['Fine if documented', 'A BROKEN API \u2014 the status code is for machines; every client, cache, and monitoring system treats 200 as success', 'Good for mobile', 'The RFC 9457 way'], a: 1, e: "\u201cUse the right status code; the body is for humans and debugging.\u201d One of the most common serious API design errors." },
      { q: 'A good error tells the client three things:', o: ['What kind of failure, whether retrying helps, what to fix', 'The stack trace, the line number, the library version', 'The SQL, the table name, the user ID', 'A message in prose only'], a: 1, e: 'RFC 9457 shape: type, title, status, detail, instance, request_id \u2014 plus MACHINE-READABLE codes, never prose clients must parse. Report ALL validation failures at once, and never leak internals.' },
      { q: 'Validation layers and their codes:', o: ['One layer: 400', 'Syntactic (parseable? right Content-Type?) \u2192 400/415; structural (required, types, ranges) \u2192 422; semantic/business (author exists? state legal?) \u2192 422/409/403', 'All errors are 500', 'Validation lives in the UI'], a: 1, e: 'Never trust the client \u2014 re-enforce every UI rule on the server \u2014 and validate with a schema (pydantic, zod, JSON Schema), not hand-written ifs, which drift from the docs.' },
    ],
    p6: [
      { q: 'What\u2019s wrong with offset pagination?', o: ['Nothing', 'Progressively slower as offset grows (every skipped row is generated and discarded), and it skips/duplicates items when data changes between requests \u2014 \u201crow 41\u201d isn\u2019t stable', 'It is the only way', 'It breaks caching'], a: 1, e: 'Cursor (keyset) pagination is the correct default for large or changing data: an opaque \u201cwhere I left off\u201d (sort key + ID tiebreaker) that stays fast at any depth and is stable under inserts.' },
      { q: 'The right response metadata for a cursor page:', o: ['Just an array', 'data + next_cursor + has_more, with default and MAX limits always set', 'total count only', 'An offset'], a: 1, e: 'Never return an unbounded collection \u2014 it is small in development and catastrophic in production.' },
      { q: 'The industry-recommended versioning approach:', o: ['Always date-based (Stripe-style)', '/v1/ in the path for a public API, plus a strong bias toward never needing /v2 \u2014 additive evolution is the default; versions are for genuine breaking redesigns', 'Never version', 'Custom headers only'], a: 1, e: 'Whatever you choose \u2014 path, header, Accept, date \u2014 the discipline matters more: publish a deprecation policy (Deprecation/Sunset headers), give real dates, and retire what you can.' },
      { q: 'Why does documentation drift to wrongness \u2014 and what keeps it honest?', o: ['Writers are lazy', 'Hand-maintained docs in a separate wiki are wrong within weeks; generate from code or validate code against the spec \u2014 OpenAPI — and wrong docs are worse than none, because they are trusted', 'OpenAPI is a GUI', 'Docs are optional'], a: 1, e: 'From one OpenAPI document you get interactive docs, generated clients, request validation, mock servers, and contract tests \u2014 FastAPI generates it from type annotations automatically.' },
    ],
    p7: [
      { q: 'Authentication vs authorization:', o: ['Synonyms', 'Authentication = WHO ARE YOU (401); authorization = WHAT MAY YOU DO (403)', 'Both = tokens', 'Authorization precedes authentication'], a: 1, e: 'Constantly conflated \u2014 and the 401/403 boundary is exactly where the conflation shows.' },
      { q: 'JWTs, honestly. Order the critical facts:', o: ['Signed, not encrypted \u2014 anyone holding one can READ the payload; valid until expiry, so you cannot easily revoke; validate signature, exp, iss, aud AND pin the algorithm', 'Encrypted, unreadable, revocable', 'Unsigned, safe in localStorage', 'Tiny sessions'], a: 1, e: 'Never put secrets in a JWT. Revocation problem \u2192 short-lived access tokens (5\u201315 min) plus a revocable, database-backed refresh token. Algorithm pinning stops the alg:none / RS256\u2192HS256 downgrade forgeries.' },
      { q: 'The single most common cause of \u201cmy POST doesn\u2019t work\u201d reports:', o: ['Wrong URL', 'Neither endpoint', 'Missing or wrong Content-Type \u2192 415 Unsupported Media Type', 'The server is down'], a: 1, e: 'From the Part 9 debug table for 415: \u201cthe most common cause of a \u2018my POST doesn\u2019t work\u2019 report.\u201d' },
      { q: 'CORS \u2014 what actually protects, and who enforces it?', o: ['It protects your server from attackers', 'It protects USERS from cross-origin reads; enforced by BROWSERS ONLY \u2014 curl, Postman, and servers ignore it. \u201cWorks in Postman, not in my app\u201d is the signature symptom', 'It encrypts traffic', 'It replaces auth'], a: 1, e: 'Server grants permission via headers; non-simple requests trigger an OPTIONS preflight the API must answer. Allow-Credentials cannot combine with a wildcard origin \u2014 and never reflect arbitrary origins.' },
    ],
    p8: [
      { q: 'The create endpoint\u2019s success response must include:', o: ['200 and the object', '201 Created plus a Location header pointing at the new resource', '204 No Content', '202 Accepted'], a: 1, e: '201/Location and 204-on-delete are Parts 2\u20133 basics \u2014 the first thing reviewers look for in Project A.' },
      { q: 'The single most common production hang in API clients:', o: ['A missing timeout \u2014 a request with no timeout can wait forever', 'JSON parsing', 'HTTPS', 'Retries'], a: 1, e: 'Project B\u2019s rules: credentials from env vars; explicit timeout; retry with backoff+jitter ONLY on 5xx/429/network errors \u2014 never 4xx (a bad request is still bad on the fourth attempt); honour Retry-After; paginate; cache via conditional requests; log everything except the token.' },
      { q: 'Two concurrent editors save the same record. The protection, and the code it answers with:', o: ['Last writer wins by design', 'ETag + If-Match: the GET returns an ETag; the PATCH says \u201conly if it\u2019s still this version\u201d \u2014 otherwise 412 Precondition Failed, re-fetch and retry', 'Redis locks', 'Double-checking in the app'], a: 1, e: 'A small amount of work that converts silent data loss into an explicit, handleable conflict \u2014 the same lost-update problem as the SQL curriculum, at the API layer.' },
      { q: 'The most secure choice for tokens in a browser \u2014 and why:', o: ['localStorage, always', 'HttpOnly; Secure; SameSite cookies plus CSRF defence \u2014 localStorage is readable by any XSS; cookies at least remove the token-theft path, and XSS is more common than CSRF', 'base64 in URLs', 'SessionStorage'], a: 1, e: 'JWT storage is a real trade-off, not a solved problem \u2014 the professional choice is the cookie, which needs SameSite and CSRF protection on state-changing requests.' },
    ],
    p9: [
      { q: 'The first move when an API call fails:', o: ['Rewrite the client', 'Establish WHERE the failure is before guessing why \u2014 work outward from the simplest thing that could be true: server up? (curl localhost:8000/health) DNS? right host/port/scheme? firewall? bound to 0.0.0.0? timeout set?', 'Restart the server', 'Check the body'], a: 1, e: 'The tree covers hangs, the 4xx table (400 bytes, 401 Bearer space, 404 typo or hidden object, 405 Allow header, 415 Content-Type, 422 read the body, 429 Retry-After), 5xx (read LOGS by correlation ID, never the deliberately vague response), and curl-vs-browser (CORS).' },
      { q: '\u201cIt works in curl but not in my browser app.\u201d First suspect:', o: ['The server is down', 'CORS \u2014 almost every case; check the console for the specific message and whether an OPTIONS preflight failed', 'HTTPS certificate', 'The browser caches'], a: 1, e: 'Second most likely: the browser sends different headers, or cookies aren\u2019t sent because of SameSite or a missing credentials: include.' },
      { q: 'The highest-value test level for an API:', o: ['Unit tests of business logic', 'Integration / API tests \u2014 real HTTP requests against the running app asserting status codes, bodies, and headers; this is where contract mistakes surface', 'End-to-end flows', 'Load tests'], a: 1, e: 'Keep E2E small, and write the tests everyone skips: another user\u2019s object ID (BOLA), no/malformed/expired tokens, and sending {"role":"admin"} to check mass assignment.' },
    ],
    p10: [
      { q: 'The full journey of GET https://api.example.com/v1/books/42 \u2014 in order:', o: ['App \u2192 database \u2192 response', 'DNS (cached per TTL) \u2192 TCP handshake (SYN/SYN-ACK/ACK) \u2192 TLS handshake (1\u20132 round trips) \u2192 request serialized \u2192 intermediaries (CDN edge may serve from cache; load balancer; gateway) \u2192 application (route, auth, validate, query) \u2192 compressed, cached response \u2192 connection kept alive', 'TCP \u2192 DNS \u2192 JSON', 'TLS \u2192 UDP \u2192 DNS'], a: 1, e: 'Every step is a place latency and failure originate \u2014 which is why \u201cwhy is this endpoint slow?\u201d is answered with measurement, and the answer is frequently DNS, TLS, or a proxy timeout, not your code.' },
      { q: 'Why does connection reuse matter so much?', o: ['It saves RAM', 'A new connection pays the full TCP + TLS startup cost per request \u2014 one request per connection can easily triple your latency; a persistent client (requests.Session, shared httpx.Client, keep-alive) is a cheap, common production fix', 'HTTP/3 needs it', 'Cookies expire'], a: 1, e: 'The layered-system and keep-alive facts also explain why old advice \u2014 bundling every file, domain sharding \u2014 is counterproductive on HTTP/2 and HTTP/3.' },
      { q: 'HTTP/2 and HTTP/3 \u2014 what each fixed, and what remained:', o: ['H2: multiplexing + HPACK \u2014 killed application-level head-of-line blocking, but TCP-level blocking remained; H3: QUIC over UDP \u2014 independent streams, faster handshake, connection migration', 'H2: more requests; H3: caching', 'H2: TLS; H3: DNS', 'H2: push; H3: pull'], a: 1, e: 'The through-line: each version attacked the round-trip and blocking costs of the previous one. \u201cWhich problem did each solve?\u201d is the interview question.' },
      { q: 'The two facts that shape API design more than any optimization:', o: ['CPU and memory are the bottleneck', 'Round trips dominate \u2014 1 request returning 100 items beats 100 requests returning 1; and geography is physics \u2014 a ~200ms cross-world round trip cannot be optimized away, only avoided via caching and CDNs', 'Payload size is everything', 'Compression is everything'], a: 1, e: 'This is precisely the over-fetching/under-fetching pressure that produced GraphQL \u2014 and why cacheability is described as the biggest reason the web scales.' },
    ],
    p11: [
      { q: 'Cache-Control public vs private \u2014 and the failure if you get it wrong:', o: ['No difference', 'private = only the client; public = shared proxies and CDNs may cache. Getting it wrong can LEAK one user\u2019s data to another via a shared cache \u2014 anything authenticated should be private at minimum, anything sensitive no-store', 'public is always fine', 'no-store is optional'], a: 1, e: 'A cache hit is infinitely faster than any optimization \u2014 because the request doesn\u2019t happen. Conditional requests (If-None-Match \u2192 304) save bandwidth without staleness.' },
      { q: 'Idempotency keys \u2014 how they work:', o: ['The server generates the key', 'The CLIENT generates a key per logical operation and keeps it across retries; the server stores key + result of the first success; a repeat returns the ORIGINAL response; a concurrent duplicate gets 409; keep keys ~24h', 'They expire in minutes', 'They replace authentication'], a: 1, e: 'The difference between an API that\u2019s safe to retry and one that double-charges customers \u2014 the answer to \u201cmy POST timed out, what now?\u201d' },
      { q: 'Rate limiting done right:', o: ['Per IP, always', 'Token bucket (allows controlled bursts) per API key or user \u2014 not per IP, since shared NATs punish innocent users; return 429 with Retry-After and RateLimit-* headers so good clients self-regulate', 'Fixed window only', 'Never limit auth endpoints'], a: 1, e: 'From the security checklist too: stricter limits on auth endpoints (credential stuffing), redact secrets in logs, allowlist outbound URLs (SSRF), and never serialize your database model directly (excessive data exposure).' },
      { q: 'The observability minimum that makes a bug report traceable:', o: ['Uptime monitoring', 'Structured JSON logs with a correlation/request ID on every line, propagated to downstream calls; latency as percentiles (p95/p99 \u2014 averages hide the tail); a /health endpoint; never log tokens', 'A dashboard', 'Alerting on everything'], a: 1, e: '\u201cIntermittent failures are the hard ones\u201d \u2014 correlation IDs and per-request latency are what make them findable at all.' },
    ],
    p12: [
      { q: 'REST is the strong default when:', o: ['Always', 'Public or partner-facing; clients diverse and beyond your deployment control; domain naturally resource-shaped; caching valuable; you want browsers, curl, proxies, CDNs, and OpenAPI tooling to work with no special client', 'Never', 'Only for startups'], a: 1, e: 'Being able to ARGUE AGAINST the default is a mark of real understanding.' },
      { q: 'GraphQL is the right call when \u2014 and the price you pay:', o: ['Always \u2014 it is newer', 'Clients need widely varying subsets of a complex graph; mobile clients suffer over-fetching and N+1 round trips. Price: HTTP caching largely stops working (everything is one POST to /graphql), query depth/cost limiting becomes mandatory, rate limiting gets harder', 'Never \u2014 it is old', 'When you have many servers'], a: 1, e: 'A genuine alternative for specific problems \u2014 not a replacement for REST.' },
      { q: 'gRPC is the right call when \u2014 and the price:', o: ['For public APIs', 'Internal service-to-service where you control both ends and want high throughput, strict schemas, generated clients, streaming. Price: not natively callable from browsers, binary payloads are hard to debug by hand, and generated stubs create the coupling REST was designed to avoid \u2014 fine inside a boundary you control', 'For mobile clients', 'For third-party integrations'], a: 1, e: 'The pragmatic industry pattern: REST at the public edge, gRPC or events internally \u2014 evolvability where you don\u2019t control clients, efficiency where you do.' },
      { q: 'REST\u2019s real weaknesses, stated without defending them:', o: ['None \u2014 it is perfect', 'Chatty (fetching one screen can take several round trips); structural over- and under-fetching; no standard for filtering, pagination, errors, or versioning, so every API invents a dialect; PATCH semantics underspecified; HATEOAS almost universally skipped, so clients hard-code URLs after all', 'It cannot stream', 'It is slow at parsing JSON'], a: 1, e: 'Name these openly \u2014 then the alternatives (GraphQL, gRPC, WebSockets, SSE, queues, webhooks) each buy something specific and cost something else.' },
    ],
    p13: [
      { q: '\u201cWhat is REST?\u201d \u2014 the answer that reads senior:', o: ['\u201cAn API that returns JSON\u201d', '\u201cAn architectural style with constraints \u2014 the set Roy Fielding identified as the reason the web scales: statelessness lets any server answer any request; the uniform interface lets generic infrastructure understand traffic it knows nothing about\u201d', '\u201cThe opposite of SOAP\u201d', '\u201cA standard with a compliance test\u201d'], a: 1, e: 'Tier 1 question 1 \u2014 and the shape that reads senior in every answer: WHAT it is \u2192 WHY it exists \u2192 ONE concrete consequence.' },
      { q: '\u201cA client times out on a POST and doesn\u2019t know if it succeeded. What now?\u201d', o: ['Retry immediately', 'Use an idempotency key \u2014 retry with the SAME key and the server returns the original response instead of acting again \u2014 and only retry 5xx/429/network errors, never 4xx', 'Switch to GET', 'Ask the user'], a: 1, e: 'This exact scenario is a Tier 2 differentiator question \u2014 the practical heart of the idempotency section.' },
      { q: 'Tier 3\u2019s walk-through question:', o: ['\u201cWhich JSON library do you use?\u201d', '\u201cWalk me through everything that happens when a client calls your API endpoint\u201d \u2014 DNS \u2192 TCP \u2192 TLS \u2192 intermediaries \u2192 application \u2192 compressed, cached response \u2192 keep-alive, plus measurement as the debugging habit', '\u201cWhat is your favourite status code?\u201d', '\u201cDo you use Postman?\u201d'], a: 1, e: 'Also Tier 3: HTTP/2 vs /3 and what each fixed, Fielding\u2019s constraints and what each buys, the HATEOAS honesty answer, BOLA, GraphQL vs gRPC trade-offs, and the resilient-client checklist.' },
      { q: 'The statement that separates people who used APIs from people who understand them:', o: ['\u201cI have called a thousand endpoints\u201d', 'Networks fail after the server acted but before the response arrived \u2014 so safety and idempotency are what make retries possible; that is why POST duplicates, GET-that-deletes is a bug, and payments carry keys', '\u201cI know all 70 status codes\u201d', '\u201cI never get timeouts\u201d'], a: 1, e: 'Every theory in this curriculum serves reasoning about retries, timeouts, and duplicate requests \u2014 the actual hard part of distributed systems.' },
    ],
    p14: [
      { q: 'Milestone 1 \u2014 Model \u2014 pass line:', o: ['Build a full API', 'The five checkpoint questions from Part 2, unaided; name Fielding\u2019s constraints and what statelessness buys. <b>(parts 1\u20132; days 1\u20132)</b>', 'Memorize all 70 status codes', 'Set up Postman'], a: 1, e: 'Pure understanding, no typing yet \u2014 everything later assumes the mental model.' },
      { q: 'Milestone 3 \u2014 Design \u2014 pass line:', o: ['Design a full URL surface for a non-trivial domain including non-CRUD actions with justifications; specify error format, pagination, and a versioning strategy; write valid OpenAPI for it. <b>(parts 4\u20136; days 6\u201311)</b>', 'Copy Stripe\u2019s API', 'Write one endpoint', 'Read one blog post'], a: 1, e: 'Design is where most APIs reveal whether their author understood REST \u2014 cheap to get right, expensive to change later.' },
      { q: 'Milestone 5 \u2014 Build and consume \u2014 pass line:', o: ['Project A meeting all thirteen requirements, and Project B surviving network failure, bad credentials, rate limiting, and a multi-page collection. <b>(part 8; days 17\u201326)</b>', 'Ship a landing page', 'Call one endpoint once', 'Deploy to Heroku'], a: 1, e: 'Designing an API and living with someone else\u2019s are different skills \u2014 each teaches what the other misses. The biggest single project phase: ~10 days.' },
      { q: 'Why is running `curl -v` by hand early the non-negotiable exercise?', o: ['It is the fastest command', 'Almost everyone who finds APIs confusing has only ever seen them through a library that hid the request \u2014 seeing the raw exchange once dissolves most of the confusion permanently', 'It is required by OpenAPI', 'It tests the network'], a: 1, e: 'The exercises are the curriculum. Total realistic pace: ~33 days at one focused hour per day, from M1 to M6.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 \u25b6 What is REST? (The answer that is not \u201can API that returns JSON.\u201d)', a: 'An architectural style \u2014 a set of constraints Roy Fielding identified as the reason the web scales (2000 PhD): client\u2013server, statelessness, cacheability, uniform interface, layered system, code-on-demand. Most \u201cREST APIs\u201d drop hypermedia and keep the valuable resource model + HTTP semantics. (Part 1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 \u25b6 Resource vs representation \u2014 and why is GET /getBook?id=42 an RPC costume?', a: 'The resource is the abstract thing (book, order, weather); what crosses the wire is a representation of it, chosen via Accept. URLs name THINGS, never actions \u2014 verbs already exist as HTTP methods. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 \u25b6 Safe vs idempotent \u2014 and why is a GET that deletes a genuine bug?', a: 'Safe = no server-state change (GET, HEAD). Idempotent = N identical requests have the same effect as one (GET, PUT, DELETE; NOT POST). A GET-that-deletes gets triggered eventually by a crawler or a link preview. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 \u25b6 PUT vs PATCH: omit a field in each. What have you said?', a: 'PUT: \u201cthis is the complete new state\u201d \u2014 the field should be ABSENT, replace the resource. PATCH: \u201ca description of changes\u201d \u2014 omit and you said leave it alone. PUT replaces; PATCH modifies. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 \u25b6 401 vs 403 \u2014 one sentence each.', a: '401 Unauthorized: \u201cI don\u2019t know who you are\u201d \u2014 you didn\u2019t prove identity (the name is a historical misnomer). 403 Forbidden: \u201cI know exactly who you are, and no\u201d \u2014 authenticated but not permitted. Asked in almost every interview. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 \u25b6 Status families, and why 200-with-error is a broken API.', a: '2xx worked, 3xx look elsewhere, 4xx YOU sent something wrong (retrying identically won\u2019t help), 5xx THE SERVER failed (retry may work). The code is for machines \u2014 a 200 body with {"error": "not found"} fools every client, cache, and monitor. (Parts 2, 5)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 \u25b6 The URL design rules in one breath.', a: 'URLs are nouns, methods are verbs (/books, DELETE /books/42); plural collections, consistent; nest for containment, stop at two levels; lowercase, hyphens, no file extensions, no trailing slashes, no verbs anywhere. (Part 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 \u25b6 Path vs query vs header vs body \u2014 where does data belong?', a: 'Path: WHICH resource. Query: HOW a collection is returned (filter/sort/page). Headers: metadata about the request (auth, formats, caching). Body: data being created or updated. Neither secrets in URLs (they hit a dozen logs) nor bodies on GET. (Part 3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 \u25b6 What is wrong with offset pagination, and what replaces it?', a: 'Offset gets progressively slower (every skipped row is generated and discarded) and skips/duplicates when data changes between requests \u2014 \u201crow 41\u201d is not stable. Cursor (keyset) pagination: an opaque \u201cwhere I left off\u201d (sort key + ID tiebreaker) \u2014 fast at any depth, stable under inserts, no page jumping. (Part 6)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 \u25b6 Which \u201charmless\u201d change quietly BREAKS clients \u2014 and the mutual discipline?', a: 'Tightening validation (sloppy-but-accepted data suddenly fails) and adding enum values (clients hard-code switches). Discipline: servers ADD rather than change; clients TOLERATE rather than assume \u2014 ignore unknown fields. That is evolvability in practice. (Part 2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 \u25b6 Cancel an order \u2014 the three legitimate approaches (A, B, C)?', a: 'A: PATCH /orders/42 {"status":"cancelled"} \u2014 when it is really a field. B: POST /orders/42/cancellation \u2014 event as sub-resource, carries who/why/when, idempotency-friendly. C: POST /orders/42/cancel \u2014 explicit action; what Stripe and GitHub do. Defend the choice; clarity beats purity. (Part 4)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 \u25b6 Design a good error response \u2014 what must it carry?', a: 'Right status code (machines branch here); machine-readable codes (\u201crequired\u201d, \u201cinsufficient_funds\u201d) \u2014 never prose clients must parse; all validation failures at once; a request/correlation ID; no internals. RFC 9457 shape: type, title, status, detail, instance, request_id. (Part 5)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 \u25b6 JWTs, honestly \u2014 signed or encrypted, and the biggest weakness?', a: 'SIGNED, not encrypted \u2014 anyone can read the payload; never put secrets in. Signature proves integrity without a DB lookup \u2014 but statelessness means you CANNOT easily revoke: valid until expiry. Mitigation: short-lived access tokens + revocable refresh tokens; validate sig, exp, iss, aud; PIN the algorithm (alg:none / HS256 downgrade forgeries). (Part 7)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 \u25b6 CORS \u2014 who enforces it, what it protects, and what the OPTIONS is?', a: 'Browsers ONLY \u2014 curl, Postman, servers ignore it; it protects USERS from cross-origin reads, not your API from attackers. Non-simple requests trigger an OPTIONS preflight the server must answer with Access-Control-Allow-* headers. Allow-Origin:* cannot combine with credentials \u2014 echo specific, allowlisted origins. (Part 7)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 \u25b6 A POST timed out. How do you make it safe to retry, and how do you stop silent lost updates?', a: 'Idempotency-Key: client-generated per logical operation, kept across retries; server stores the first result and returns it for repeats (24h+ retention, concurrent duplicates \u2192 409). Lost updates: ETag from GET + If-Match on PATCH \u2192 412 on conflict \u2014 explicit, handleable conflict instead of silent overwrites. (Part 11)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 \u25b6 Narrate the full journey of one API call.', a: 'DNS (cached per TTL; stale cache is a real failure mode) \u2192 TCP three-way handshake (one round trip) \u2192 TLS handshake (1\u20132 more; why HTTPS has fixed startup cost and reuse matters) \u2192 request serialized exactly as Part 3\u2019s text \u2192 intermediaries (CDN may end it there; load balancer; gateway \u2014 the layered system in action) \u2192 application: route, auth, authorize, validate, query \u2192 compressed, cached response \u2192 connection kept alive. (Part 10)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 \u25b6 The HTTP version through-line: what did 1.1, 2, and 3 each fix?', a: '1.1: persistent connections (keep-alive) + Host header + caching headers \u2014 but head-of-line blocking, hence ~6 parallel connections. H2: multiplexing + HPACK over TCP \u2014 fixed app-level HOL; TCP-level HOL remained. H3: QUIC over UDP \u2014 independent streams, faster handshake, connection migration. Consequence: bundling and domain sharding are counterproductive on H2/H3. (Part 10)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 \u25b6 When do you choose GraphQL or gRPC over REST \u2014 and what do you give up?', a: 'GraphQL: clients need widely varying subsets of a graph; mobile over-fetching/N+1 pain \u2014 you give up HTTP caching (one POST to /graphql), add query-depth limiting, and rate limiting gets harder. gRPC: internal service-to-service where you control both ends \u2014 you give up browser callability, hand-debuggability, and accept generated-stub coupling (fine inside a boundary). Industry pattern: REST at the edge, gRPC/events internally. (Part 12)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 \u25b6 Model', pass: 'Pass: the five checkpoint questions from Part 2, unaided; name Fielding\u2019s constraints and what statelessness buys. <b>(parts 1\u20132; days 1\u20132)</b>' },
    { id: 'm2', title: 'Milestone 2 \u25b6 HTTP Fluency', pass: 'Pass: read a raw request/response and name every part; deliberately trigger five status codes; explain where any piece of data belongs (path, query, header, body). <b>(part 3; days 3\u20135)</b>' },
    { id: 'm3', title: 'Milestone 3 \u25b6 Design', pass: 'Pass: full URL surface for a non-trivial domain incl. non-CRUD actions with justifications; specify error format, pagination, and a versioning strategy; write valid OpenAPI for it. <b>(parts 4\u20136; days 6\u201311)</b>' },
    { id: 'm4', title: 'Milestone 4 \u25b6 Security', pass: 'Pass: token auth with per-object ownership checks and a proven 401/403 boundary via curl; explain CORS correctly to someone else; state the JWT revocation problem and its mitigation. <b>(part 7; days 12\u201316)</b>' },
    { id: 'm5', title: 'Milestone 5 \u25b6 Build and Consume', pass: 'Pass: Project A meets all thirteen requirements; Project B survives network failure, bad credentials, rate limiting, and a multi-page collection. <b>(part 8; days 17\u201326)</b>' },
    { id: 'm6', title: 'Milestone 6 \u25b6 Depth', pass: 'Pass: debug an unfamiliar failing integration via the Part 9 tree; deliver the Part 10.1 narration; implement idempotency keys and If-Match and say what each prevents; argue for GraphQL or gRPC over REST and name what you lose. <b>(parts 9\u201312; days 27\u201333)</b>' },
  ],

  sim: [
    { t: 800, l: "<span class=\"a\">resolve</span> api.example.com \u2192 IP, via a cascade of DNS caches ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">tcp</span> SYN, SYN-ACK, ACK \u2014 one round trip before any data moves ... <span class=\"ok\">OK</span>" },
    { t: 1400, l: "<span class=\"a\">tls</span> certificates + key agreement \u2014 1\u20132 round trips, why reuse matters ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">send</span> request line, headers, blank line, body \u2014 the text from Part 3 ... <span class=\"ok\">OK</span>" },
    { t: 1300, l: "<span class=\"a\">edge</span> CDN / load balancer / gateway inspect it \u2014 layered system ... <span class=\"ok\">OK</span>" },
    { t: 1000, l: "<span class=\"a\">app</span> route, authenticate, authorize, validate, query ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">reply</span> gzip-compressed, cacheable per Cache-Control ... <span class=\"ok\">OK</span>" },
    { t: 900, l: "<span class=\"a\">reuse</span> connection kept alive \u2014 round trips dominate ... <span class=\"ok\">OK</span>" },
  ],
};