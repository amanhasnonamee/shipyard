// htmlcss pedagogy — authored from htmlcss-curriculum.md (current 2025)
export default {
  title: 'The HTML/CSS Shipyard - HTML & CSS: A First-Principles Curriculum',
  brand: 'THE HTML/CSS SHIPYARD',
  tagline: 'HTML & CSS · the document tree · the cascade · layout',
  storageKey: 'shipyard:htmlcss:v1',
  legacyStorageKeys: [],
  stackLabel: 'THE STACK — your document tree',
  ranks: ['SWABBIE', 'DECKHAND', 'BOATSWAIN', 'FIRST MATE', 'CASCADE CAPTAIN', 'HARBOR MASTER'],
  accent: '#ff6b6b',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum htmlcss-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0–14 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: the cascade decides who wins — and it has a fixed order, every time.</span></div>',
    '<div class="bl">AWAITING CREW — 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">▮</span></div>',
  ],
  h1: 'HTML & CSS, UNDERSTOOD.<br><span class="wh">NOT JUST PLACED.</span>',
  sub: 'This is a first-principles curriculum rebuilt as a mission. Learn the core mental model <b>deeply</b> — the document is a tree, everything is a box, and the cascade settles every argument in a fixed order — reference the rest, and physically <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The HTML/CSS Shipyard is a faithful interactive conversion of the first-principles HTML & CSS curriculum — every concept, table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file — copy it anywhere, it runs offline.',
    rule: 'everything is a box, and the cascade settles every argument in a fixed order.',
  },
  chips: { p10: ['<span class="m">★ <b>INTERVIEW GOLD</b></span>'] },
  grad: {
    title: 'LAYOUT RESOLVED 🎨',
    rule: 'everything is a box, and the cascade settles every argument in a fixed order.',
  },
  console: {
    cmd: '$ open index.html',
    line: 'painted  ·  no layout shift  ·  (yours — every layer earned)',
  },
  simPrompt: '/* .card { display: flex } — watch the cascade settle */',
  egg: { label: 'SPECIFICITY OVERRIDE' },
  deckIn: 'p13',
  milesIn: 'p14',
  simIn: 'p10',
  deckNote: 'If you can deliver these aloud — WHAT → WHY IT EXISTS → ONE CONCRETE CONSEQUENCE — you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof. When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'HTML was invented:', o: ['By Tim Berners-Lee at CERN (1989) — hypertext to share physics papers; CSS followed in 1996 to separate content from presentation', 'By a committee at Netscape in 1995, who bundled markup and visual styling together into one proprietary browser format', 'By the W3C in 1994 as a top-down formal standard, years before any browser existed that could render it', 'As part of HTTP/2 in the mid-2010s, added so servers could push fully structured documents straight to clients'], a: 0, e: 'TBL at CERN, 1989: the document web. CSS arrived 1996 (Lie & Bos) — the separation of meaning (HTML) from appearance (CSS) is the founding idea.' },
      { q: 'HTML is:', o: ['A markup language — it describes document structure and meaning, not behaviour', 'A programming language with loops and conditionals that control how the page behaves at runtime', 'A styling language the browser uses to decide colours, fonts, and spacing on the page', 'A query language used to fetch and filter records out of a structured database'], a: 0, e: 'No logic, no state. Elements describe meaning; browser + CSS decide appearance; JS adds behaviour.' },
      { q: 'The standards context that matters:', o: ['The HTML Living Standard (WHATWG) — the web keeps moving, and browsers implement to it rather than frozen specs', 'HTML is frozen at version 4.01 and has not meaningfully changed at all since browsers first stabilised support back in the 2000s', 'Only the W3C can change HTML, and every new feature needs their sole formal sign-off first', 'Standards are mostly cosmetic — browsers ship whatever features product marketing happens to demand'], a: 0, e: 'HTML is a living standard: features ship in browsers first, get standardised after — the opposite of frozen legacy specs.' },
      { q: 'When a design is “just divs”, the lost layer is:', o: ['MEANING — divs carry no semantics; screen readers, search and future you lose the document’s outline', 'Speed — a page built only from divs renders measurably and consistently slower than one built with proper semantic elements', 'Colour — divs strip out whatever colour information the designer originally specified', 'Cascade — divs quietly break the cascade so specificity rules stop applying correctly'], a: 0, e: 'Every div is a box until you give it meaning; semantics is the layer that survives any visual redesign.' },
    ],
    p2: [
      { q: 'The browser renders pages by:', o: ['Building a DOM TREE first, then styling it — CSS targets nodes of that tree', 'Styling the raw HTML text directly as it streams in, before any tree gets built', 'Rendering every element in source order instantly, with no intermediate structure at all', 'Asking the server for pre-rendered pixels instead of parsing the markup locally'], a: 0, e: 'HTML → parsed into a tree of elements; CSS selectors match nodes; layout and paint follow. Idea 1: the tree comes first.' },
      { q: 'The box model arithmetic:', o: ['content + padding + border (+ margin) — box-sizing: border-box moves padding/border INSIDE the width', 'width includes margin by default, so an element’s own margin never actually adds to its total rendered footprint', 'padding never affects total size unless box-sizing is explicitly switched to content-box', 'border renders for free — it never changes an element’s width or height at all'], a: 0, e: 'With border-box, width = everything up to the border — the only sane default for real layouts.' },
      { q: 'Normal flow is:', o: ['A real layout system — block elements stack, inline flows along the line — and it is usually right; flex/grid are refinements, not replacements', 'A bug to be worked around with absolute positioning everywhere, until flexbox fully takes over', 'Deprecated behaviour that modern browsers only keep around for legacy backward compatibility support', 'Something that only applies to paragraphs and other purely textual inline-level elements'], a: 0, e: 'Idea 3: normal flow handles most pages correctly. Reach for positioning only when flow genuinely can’t.' },
      { q: 'The cascade decides winners in this order:', o: ['Important + origin → specificity → source order — more specific selectors win; ties go to the later rule', 'File order only — whichever stylesheet happens to be loaded last in the document always wins every single conflict', 'Selector length — the selector written with the most characters always overrides the shorter one', 'Alphabetical order — property names earlier in the alphabet take precedence over later ones'], a: 0, e: 'Idea 4: importance/origin first (author !important beats normal), then specificity, then source order. Inline styles beat selectors (except !important).' },
    ],
    p3: [
      { q: 'Semantic HTML is:', o: ['Meaning over appearance — header/main/section/article/nav describe structure; the browser and assistive tech consume the OUTLINE', 'A styling preference — semantic tags simply look tidier in the raw source code to other developers', 'Required only for SEO purposes — search engines need it but users and browsers largely ignore it', 'The same as using divs, since browsers render both of them identically once CSS is applied'], a: 0, e: 'The element map is the document’s outline. Semantic elements also ship free behaviour (landmark roles, default margins).' },
      { q: 'Forms done right require:', o: ['label tied via for/id, a name on every field, correct type + required/pattern validation, aria-describedby for help text', 'Only a submit button — everything else is optional as long as a user can click it', 'Tables for layout — wrapping each field in a table row keeps the form visually aligned', 'JS validation exclusively — skip the HTML attributes and validate everything in script'], a: 0, e: 'Labels are the click target AND the accessibility name. Validation belongs in HTML first, JS as enhancement.' },
      { q: 'Accessibility is mostly:', o: ['A byproduct — correct headings, alt text, focus styles and contrast come free from structure; the audits mostly check the structure you already should have', 'A plugin — install a third-party accessibility widget on the page and let it automatically handle every compliance requirement for you, with no structural changes needed', 'A CSS class — add one special utility class to the body element and assistive tech picks up the rest', 'Only for government sites — private companies are not expected to meet these standards'], a: 0, e: 'Keyboard navigation, readable outline, visible focus: all follow from semantic, well-formed documents.' },
      { q: 'alt text exists for:', o: ['The blind — describe what the image MEANS in context, not what it literally shows', 'SEO keywords — stuff the alt attribute with search terms so the page ranks higher', 'Designers — alt text is really a note left for other designers reviewing the file later', 'Lazy loading — alt text tells the browser when it should defer loading the image'], a: 0, e: 'Meaning, not description: a graph’s alt summarises the finding, not the axes.' },
    ],
    p4: [
      { q: 'The selector strategy that scales:', o: ['Class-based with LOW specificity — avoid IDs for styling; :is()/:where() keep specificity sensible', 'IDs everywhere — an ID selector on every element guarantees your rule always wins the cascade', 'Universal selectors only — style everything through the * selector for total consistency', 'Deep descendant chains — nesting selectors five or six levels deep keeps intent clear and easy for teammates to follow'], a: 0, e: 'Low-specificity classes keep every later rule winnable. Deep chains (div .a .b .c) become specificity debt.' },
      { q: 'Custom properties are:', o: ['Design tokens — --x declared on :root, inherited down the tree, overridable at any level (but not inside media queries)', 'A preprocessor feature — custom properties only exist once Sass or Less compiles the source', 'Runtime variables that JavaScript quietly and automatically recreates completely from scratch on every single page load and reload', 'Deprecated — custom properties were replaced by Sass variables in modern CSS workflows'], a: 0, e: 'This is where modern CSS starts: one token change re-themes a whole app. They compose with calc() and can’t be read by media queries — plan for that.' },
      { q: 'rem vs em:', o: ['rem = root em (stable); em compounds at every nesting level — use rem for spacing/type scale, em for element-relative sizes', 'rem is always the wrong choice for any real production project — stick to em everywhere instead for fully consistent scaling', 'They are identical units — browsers simply resolve both of them to the same fixed pixel value', 'em is an absolute unit fixed permanently at 16 pixels regardless of nesting or context'], a: 0, e: 'em compounds through nested rules and surprises; rem is the predictable scale. Together with clamp() they replace most px maths.' },
      { q: 'Two rules same specificity — who wins?', o: ['Source order: the LATER rule wins', 'The shorter rule wins outright, since browsers favour compact selectors over verbose ones', 'The first rule always wins, no matter where the second, equally specific rule is declared', 'The browser resolves the tie at random on every single page load, by design'], a: 0, e: 'Specificity tie → later in the stylesheet wins. That is why “undo” rules must come after the rules they undo.' },
    ],
    p5: [
      { q: 'Choosing a layout tool:', o: ['Flow for what flow does; FLEXBOX for 1-D arrangement of content; GRID for 2-D container-driven structure; absolute positioning as the escape hatch', 'Grid for everything, always — even a single row of nav links or one inline button deserves its own full grid container with named template areas', 'Flexbox for everything, always — two-dimensional layouts still resolve fine with nested flex rows', 'Tables were never a real layout tool and have no legitimate use in any modern page'], a: 0, e: 'Flex = content-sized, one axis, per-item control. Grid = container-sized, tracks and areas. Each has a mental model; pick by the constraint that actually binds.' },
      { q: 'The flexbox mental model:', o: ['main axis vs cross axis — justify-content places along main, align-items along cross; flex-grow/shrink/basis decide how space is claimed', 'Flex has one axis only, so justify-content and align-items always control the same direction', 'align-items controls the main axis, while justify-content is reserved for the cross axis instead', 'gap only works inside Grid containers; flexbox has no equivalent spacing property at all'], a: 0, e: 'If you can state the axis for each property, flexbox stops surprising you.' },
      { q: 'The grid mental model:', o: ['Tracks (columns/rows) with fr units, named template areas, and alignment — the container owns the layout, not the items', 'Grid items decide their own position by default, independent of whatever the container declares', 'fr is a font-sizing unit, similar to rem, used for scaling type inside a grid container', 'Grid cannot mix differently sized tracks — every column or row must share one fixed width'], a: 0, e: 'grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)) — one line, fluid responsive layout, no media query.' },
      { q: 'The two positioning traps:', o: ['absolute removes the box from flow (its containing block is the nearest positioned ancestor); sticky needs a scroll container and a top value', 'absolute stays fully inside normal flow, so surrounding elements still reserve space around it exactly as before', 'sticky works fine with no top value declared, defaulting quietly to the viewport edge', 'fixed positions relative only to the page’s root element, never to the viewport itself'], a: 0, e: 'Both traps are “works nowhere else, works here” surprises — check the containing block, check the scroll parent.' },
    ],
    p6: [
      { q: 'The responsive premise:', o: ['Respond to the VIEWPORT and the device — design systems that reflow; mobile-first makes constraints explicit', 'Detect the browser with JS and serve a completely different, hand-built layout file for each browser and device combination', 'One desktop layout is fine — modern phones just zoom the page out to make everything fit', 'Media queries are the only tool that matters; every other responsive technique is optional'], a: 0, e: 'Intrinsic sizes, wrapping, clamp() and container queries come BEFORE media queries — media queries fill the gaps, not the plan.' },
      { q: 'The best-first toolkit:', o: ['Intrinsic sizing (min/max-content), flex-wrap, grid auto-fit/minmax, clamp() type, fluid spacing, srcset — media queries LAST', 'Fixed pixel widths on every container, chosen once for a single reference screen size', 'A dedicated media query written for each individual device model on the market', 'iframe resizing scripts that recalculate the whole page layout on every scroll event'], a: 0, e: 'Layout should shrink and grow on its own; breakpoints only patch where intrinsic behaviour can’t.' },
      { q: 'A common responsive mistake:', o: ['Setting min-width in px on containers — breaks any viewport below it; prefer minmax(0, ...) and flexible tracks', 'Making a layout too fluid overall, so it reflows smoothly across literally every viewport width available on any device', 'Using rem units for spacing instead of raw pixels throughout the whole stylesheet', 'Using gap for spacing between flex or grid children instead of individual margins'], a: 0, e: 'min-width: 960px is how desktop-only pages happen. Grid’s minmax(0, 1fr) is the anti-overflow weapon.' },
      { q: 'Images that never overflow:', o: ['max-width: 100%; height: auto — plus width/height attributes (or aspect-ratio) to reserve layout space', 'width: 100vw on every image, regardless of the container it happens to sit inside', 'object-fit applied to span elements instead of the actual img elements it was designed to target and constrain', 'img { display: none } until the whole page has fully finished loading its assets'], a: 0, e: 'Missing dimensions cause CLS (cumulative layout shift) — reserve space, set aspect ratios, lazy-load below the fold.' },
    ],
    p7: [
      { q: 'Systems beat one-offs:', o: ['Tokens (type/colour/space) → components → pages — a small design system beats bespoke pixels every time', 'Every page should be bespoke — shared tokens just slow designers down in the long run', 'One-off styles are faster to ship forever, even as the number of pages keeps growing', 'Design systems kill creativity by locking every single page into one identical, unchangeable visual template forever'], a: 0, e: 'Consistency is a feature: users learn one scale, one rhythm, one vocabulary across the whole product.' },
      { q: 'Typography is the highest visual ROI:', o: ['A modular type scale (e.g. 1.25 ratio), 1.4–1.6 line-height, 45–75ch measure — type alone carries a design', 'More fonts loaded on a page is always better, since variety reads as more polished', 'font-size: 16px everywhere is basically the whole story; type scale and line measure barely matter after that point', 'Line-height is a luxury setting that only matters for long-form editorial articles'], a: 0, e: 'Type is ~90% of the page’s visual weight. Get the scale, spacing and measure right and the design lands.' },
      { q: 'Colour’s one DEEP rule:', o: ['CONTRAST — text must pass 4.5:1 against its background (WCAG AA); hue choice is taste, contrast is law', 'Use every colour in the palette on a single screen so nothing feels flat or boring', 'Black text on a white background only — every other colour combination automatically fails every accessibility audit', 'Contrast checking is a designer luxury that most production teams can skip entirely'], a: 0, e: 'The audit tools check contrast, not taste. If a color pair fails, no visual argument rescues it.' },
      { q: 'Motion defaults:', o: ['150–300ms, ease-out, one or two properties at a time — and honour prefers-reduced-motion', 'Longer animation durations always read as more premium and considered to most users', 'Animate every property you can on every element so the interface always feels alive', 'Motion is forbidden in accessible interfaces and should never appear on any page'], a: 0, e: 'Motion communicates state (which element changed); slow or constant motion just annoys. 300ms of ease-out is the default taste.' },
    ],
    p8: [
      { q: 'Building the full page starts with:', o: ['The semantic STRUCTURE — outline first, then tokens, then sections, then polish; verify in DevTools constantly', 'Colours first — lock the palette down before any markup or layout decisions get made', 'Fonts first — pick the typeface family before writing a single line of semantic HTML', 'A framework — install one first and let its defaults decide the page’s structure'], a: 0, e: 'Structure first means the page is accessible and extensible before it is pretty.' },
      { q: 'CSS order that scales:', o: ['Mobile-first: base styles, then media queries that ADD complexity at wider widths', 'Desktop-first always, with media queries later stripping features away for small screens', 'One giant file with every rule for every screen size mixed together in source order', 'Inline styles everywhere, so each element carries its own rules independent of any sheet'], a: 0, e: 'Mobile-first means the constrained case is the default — extension beats correction.' },
      { q: 'Form validation states must be:', o: ['Visible, labelled, and matching HTML validation — :invalid/:valid plus aria-describedby help text and focus management', 'Silent until submit, so the user only discovers problems after clicking the button', 'Colour-only, since a red or green border communicates the full validation state alone', 'A JS library’s job alone, with no HTML validation attributes involved at all'], a: 0, e: ':​placeholder-shown + :invalid distinguish untouched from wrong — the difference between helpful and hostile forms.' },
      { q: 'The final pass audits:', o: ['Keyboard navigation, contrast (4.5:1), zoom to 200%, Lighthouse/axe — measure, then fix the real findings', 'Only how the page looks in the one browser and screen size used during development', 'Browser compatibility tables, checked once early on, with no further live testing against real production content', 'Nothing — a finished-looking page is ready to ship without any further checks'], a: 0, e: 'The audits are quick; the findings are honest. A keyboard-walkable, high-contrast page is the baseline, not the bonus.' },
    ],
    p9: [
      { q: 'The four DevTools panels that matter:', o: ['Elements (styles + box), Computed (what actually won), Network (what loaded), Performance (what janked) — Computed answers cascade fights instantly', 'Console only — every layout, cascade, source, and network question can always be answered directly and completely from the printed console logs alone', 'Only the Elements tab exists in a real debugging workflow; the rest are rarely opened', 'A dedicated screenshots panel that compares renders across browsers automatically'], a: 0, e: 'Never guess who won a cascade fight — the Computed panel tells you the winner and why.' },
      { q: 'The debugging decision tree:', o: ['Is it a BOX problem, a CASCADE problem, or a SOURCE problem? Check computed styles before touching code', 'Rewrite the file from scratch — a clean slate is usually faster than tracing the bug', 'Ask the design tool what the intended spacing was supposed to be and match the CSS exactly to that value', 'Make random changes and reload the page until something starts looking right'], a: 0, e: 'Box: inspect the box model. Cascade: inspect computed + matched selectors. Source: is it even loaded?' },
      { q: 'An element is smaller than expected. First move:', o: ['Inspect the box model panel — border-box vs content-box and padding explain most sizing mysteries', 'Increase the declared width repeatedly until the element visually looks about the right size on screen', 'Add !important to the width rule so nothing else in the cascade can override it', 'Delete the stylesheet and see whether the browser’s default sizing looks correct'], a: 0, e: 'The box panel shows content/padding/border/margin live — the arithmetic is visible, not guessed.' },
      { q: 'A style will not apply no matter what:', o: ['Check the cascade order: specificity (IDs/classes count), then source order, then inline styles and !important — the Computed panel names the culprit', 'It is almost certainly a browser rendering bug in this particular case, rather than anything actually wrong in your own stylesheet, markup, or build configuration', 'CSS itself is simply broken in this case and there is no reliable way to debug it', 'Add more !important declarations until one of them finally happens to take effect'], a: 0, e: 'Winners are decided by importance → specificity → order. The panel lists which selector won and where it lives.' },
    ],
    p10: [
      { q: 'The rendering pipeline, in order:', o: ['HTML → DOM tree; CSS → style (cascade + resolved values) → layout (boxes measured) → paint → composite', 'Paint happens first in every case, then layout measures the boxes, and style resolves the values last of all', 'The DOM is built, then the page paints immediately, and style is resolved afterward', 'The style stage is optional and browsers can skip straight from DOM to layout'], a: 0, e: 'The interview-grade narration: tokenize → parse → style → layout → paint → composite. Each stage feeds the next.' },
      { q: 'Layout thrash is:', o: ['Reading a layout value right after writing forces a synchronous relayout — batch your DOM reads and writes', 'A paint-only problem that never actually touches layout or measurement work of any kind at all', 'A GPU issue caused by too many composited layers stacking up on one page', 'Something that cannot happen in modern browsers because they batch all reflows automatically'], a: 0, e: 'JS reads like scrollHeight/offsetWidth mid-write-loop force reflow every time — the classic jank source.' },
      { q: 'z-index only works:', o: ['INSIDE the nearest stacking context — transform/filter/opacity create them, so z-index decisions are local, not global', 'Globally, anywhere on the page, regardless of any ancestor’s transform or opacity settings', 'On any element at all, even ones with no positioning or stacking context involved', 'Only on elements given position: fixed; every other position value ignores z-index'], a: 0, e: 'A transformed ancestor traps its children’s z-index. Two “z-index: 9999” fixes fight inside different contexts — the mystery behind most stacking bugs.' },
      { q: 'The parser blocks on:', o: ['CSS (render-blocking) and non-deferred JS — defer/async scripts stop blocking; <script> mid-body blocks parsing until executed', 'Images only — CSS and scripts never delay the parser from moving through the document', 'Web fonts alone, while every stylesheet and classic script loads without blocking anything', 'Nothing anymore — modern browsers parse the full document before loading any resource'], a: 0, e: 'CSS blocks first paint; classic scripts block parsing. async = fire when ready; defer = after parsing, in order. Fonts trigger FOIT unless you plan for it.' },
    ],
    p11: [
      { q: 'The metrics actually graded:', o: ['LCP (largest content painted), INP (interaction latency), CLS (layout shift) — and each has a fix: preload, defer work, reserve space', 'Hits per second — how many requests the server can answer per second is the only metric that actually gets graded', 'FPS only — frame rate during scrolling is the sole signal the audits actually grade', 'File size only — a smaller total download always beats a larger one on every metric'], a: 0, e: 'LCP: image sizes + preload. INP: keep main thread free. CLS: width/height on media + stable fonts.' },
      { q: 'CSS architecture’s real trade-off:', o: ['Consistency + speed of change vs file size + constraint — utilities (Tailwind) vs BEM vs composition; the senior conversation is cost/consistency, not dogma', 'Always reach for utility classes — they are objectively the correct choice for every team, every project, and every possible scale of screen count imaginable', 'Always reach for BEM naming — it is the one approach with no real downside anywhere', 'CSS files themselves are irrelevant once a component framework handles the styling'], a: 0, e: 'Pick the system your team will actually keep consistent; measure the cost over a year, not a week.' },
      { q: 'The accessibility checks that catch most issues:', o: ['axe/Lighthouse scan + keyboard-only walk + focus visibility + label audit — most failures are found by these four', 'Screen-reader poetry — reading the whole page aloud for tone and rhythm catches the majority of real accessibility issues', 'Eye-tracking studies run in a lab are the main way accessibility problems get discovered', 'Colour picking — choosing a nicer palette resolves most reported accessibility failures'], a: 0, e: 'Automated scans catch ~30%; the keyboard walk catches the rest. Both together: most of the way.' },
      { q: 'Performance starts at:', o: ['The critical request chain — fewer, smaller, later — then images, then fonts, then JS payload', 'Whatever performance claims the hosting provider’s marketing page happens to make about server speed', 'Switching to a CDN, which alone resolves most real-world performance problems', 'Choosing a framework, since the framework alone determines the page’s speed'], a: 0, e: 'First paint cares about the chain: HTML → critical CSS → first image. Shipping less, shipping later, wins every time.' },
    ],
    p12: [
      { q: 'Hand-writing CSS wins when:', o: ['The surface is bespoke and small — custom UI, tight design control, few pages; the file is the system', 'The team is large and many different people need to ship consistent screens quickly and fully independently of each other', 'There are 40+ components that all need to share one predictable, generated set of rules', 'Always — utility layers and frameworks never offer any real advantage over hand-written CSS'], a: 0, e: 'Hand-written wins on small, design-heavy surfaces where every pixel is deliberate.' },
      { q: 'A framework (or utility layer) wins when:', o: ['Rapid product iteration — consistency + speed across many screens; tokens keep brand identity anyway', 'Never — hand-written CSS is always the objectively better choice, regardless of team size or delivery timeline', 'Only for marketing sites, since product screens never benefit from a shared utility layer', 'Only once a backend exists, since utility classes cannot function in a static page'], a: 0, e: 'When screens outnumber hours, the framework’s constraint is the point: your team ships consistent fast.' },
      { q: 'The honest trade-off to name:', o: ['Build size and debugging surface vs consistency and speed — and conventions matter more than either tool', 'Bundle size is the only factor worth weighing when picking a CSS approach', 'Debugging is equally easy no matter which CSS approach a team happens to choose', 'Frameworks carry no real cost once a team has learned their conventions'], a: 0, e: 'Whatever you pick, the decision is about your team’s consistency cost over a year — that is the senior framing.' },
      { q: 'What survives every framework change:', o: ['The semantic layer — real elements, a real outline, tokens as custom properties. The DOM is the contract; frameworks come and go around it', 'The utility classes, which stay valid and fully meaningful even years after a framework migration or rewrite', 'The build config, since bundler settings outlast any particular component library', 'The component names, which every future framework is expected to preserve exactly'], a: 0, e: 'Structure is portable; framework-specific markup is not. Your pages’ meaning outlives any stack.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 ▶ The box model — and why border-box is the sane default.', a: 'content + padding + border (+ margin). box-sizing: border-box puts padding/border inside the declared width — no more “width + 32px” arithmetic. (Part 2, Idea 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 ▶ The cascade order — who wins, and when.', a: 'Important + origin first, then specificity, then source order. Inline styles beat selectors (except !important). A tie goes to the later rule. (Part 2, Idea 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 ▶ Specificity in one breath.', a: 'IDs > classes/attrs/pseudo-classes > elements. :is()/:where() let you write complex selectors with LOW specificity — keep yours low so later rules can win. (Part 4.1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 ▶ rem vs em — and the only sane spacing rule.', a: 'rem = root em (predictable); em compounds at every nesting level. Use rem for the type/space scale, em only when something should size relative to ITSELF. (Part 4.3)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 ▶ Flexbox in one axis — which property controls which axis.', a: 'justify-content = MAIN axis placement; align-items = CROSS axis. flex-grow/shrink/basis decide how space is split. State the axis first, then the property. (Part 5.2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 ▶ Grid vs flex — the actual selector.', a: 'Flex: 1-D, content-sized, items decide. Grid: 2-D, container-sized, tracks decide. auto-fit/minmax(240px, 1fr) = fluid responsive without media queries. (Part 5.3)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 ▶ Custom properties — what they are and where they fail.', a: 'Design tokens: declared on :root, inherited, overridable at any level, composable with calc(). They do NOT work inside media queries — precompute what breakpoints need. (Part 4.2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 ▶ The semantic element set that carries the outline.', a: 'header, main, nav, section, article, aside, footer, h1→h6, figure, time, address — the landmarks assistive tech and search use. Every div is a box until you give it meaning. (Part 3.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 ▶ Forms: the four things every field needs.', a: 'A label (for/id), a name, a correct type, and HTML validation (required/pattern) — plus aria-describedby for help text. Labels are click targets AND accessibility names. (Part 3.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 ▶ The colour law that is not taste.', a: 'Contrast: 4.5:1 for normal text, 3:1 for large text (WCAG AA). Hue is taste; contrast is law — every audit tool checks it. (Part 7.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 ▶ The rendering pipeline, named in order.', a: 'HTML → DOM tree; CSS → style (cascade + resolved values) → layout (boxes measured) → paint → composite. The interview-grade narration starts here. (Part 10.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 ▶ Layout thrash — the one-line explanation.', a: 'A JS read after a write forces a SYNCHRONOUS relayout. Batch your DOM reads and writes; one forced reflow a frame is fine, a loop is jank. (Part 10.2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 ▶ Stacking contexts — why z-index stops working.', a: 'z-index only orders elements INSIDE the same stacking context. transform/filter/opacity/position create new ones — so “z-index: 9999” loses to a transformed ancestor’s context. (Part 10.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 ▶ The parser-blocks rules.', a: 'CSS blocks rendering. Classic scripts block parsing — defer runs after parsing in order, async fires when ready. Images and fonts do not block parsing, only paint. (Part 10.4)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 ▶ LCP / INP / CLS — what each measures and its fix.', a: 'LCP: largest content → preload + image sizes. INP: interaction latency → keep the main thread free. CLS: layout shift → reserve space (width/height, stable fonts). (Part 11.1)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 ▶ The CSS architecture trade-off — as a senior frames it.', a: 'Consistency + change-speed vs file size + constraint: utilities (Tailwind), BEM, or composition. The decision is your team’s consistency cost over a year, not tool dogma. (Part 11.2)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 ▶ The responsive default that kills breakpoints.', a: 'Intrinsic sizing + flex-wrap + grid auto-fit/minmax + clamp() — layout that reflows by itself; media queries fill the residual gaps. Mobile-first makes the constrained case the default. (Part 6)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 ▶ When to hand-write CSS and when not to.', a: 'Hand-write: bespoke, small, design-heavy surfaces. Framework/utilities: many screens racing to consistency. Either way the semantic layer and tokens survive the choice. (Part 12)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 ▶ Model', pass: 'Pass: the Part 2 checkpoint aloud — DOM tree, box arithmetic, normal flow, and the cascade order from memory. <b>(Parts 1–2; ~days 1–3)</b>' },
    { id: 'm2', title: 'Milestone 2 ▶ Semantics', pass: 'Pass: write the full outline of a real page (landmarks, headings, forms with labels) without looking at a design. <b>(Part 3; days 4–6)</b>' },
    { id: 'm3', title: 'Milestone 3 ▶ Fundamentals', pass: 'Pass: build a token layer (type/colour/space as custom properties) and settle five specificity puzzles using the Computed panel. <b>(Part 4; days 7–12)</b>' },
    { id: 'm4', title: 'Milestone 4 ▶ Layout', pass: 'Pass: a fluid layout with flex + grid that survives 360px→1440px with no breakpoint below 720px. <b>(Parts 5–6; days 13–16)</b>' },
    { id: 'm5', title: 'Milestone 5 ▶ Craft & project', pass: 'Pass: the full responsive page — semantic structure, type scale, 4.5:1 contrast, reduced-motion honoured, keyboard-navigable. <b>(Parts 7–8; days 17–25)</b>' },
    { id: 'm6', title: 'Milestone 6 ▶ Depth', pass: 'Pass: narrate the rendering pipeline in order; fix a real layout-thrash and a stacking-context bug; argue when you would NOT hand-write CSS. <b>(Parts 9–12; days 26–30)</b>' },
  ],

  sim: [
    { t: 700, l: '<span class="a">cli</span> GET /index.html — 1.1 MB → 78 KB transferred (gzip)' },
    { t: 1000, l: '<span class="a">parse</span> HTML → DOM tree: tokens → nodes — 14ms, one pass' },
    { t: 1100, l: '<span class="a">style</span> cascade + custom props resolved: 9,214 rules → 1,402 matched' },
    { t: 1200, l: '<span class="a">layout</span> boxes measured: flex + grid + clamp() resolved — 31ms' },
    { t: 900, l: '<span class="a">paint</span> layers rasterised — 12 composited layers' },
    { t: 900, l: '<span class="a">composite</span> frame sent to GPU — 60fps steady' },
    { t: 1000, l: '<span class="a">metric</span> LCP 0.9s · INP 12ms · CLS 0.00 — Core Web Vitals: all green' },
    { t: 800, l: '<span class="ok">OK</span> shipped. 4.5:1 everywhere. keyboard-navigable.' },
  ],
};
