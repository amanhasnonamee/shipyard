// rest-api pedagogy — authored from rest-api-curriculum.md
export default {
  title: 'The REST-API Shipyard - REST APIs: A First-Principles Curriculum',
  brand: 'THE REST-API SHIPYARD',
  tagline: 'REST \u00b7 resources & representations \u00b7 HTTP \u00b7 idempotency',
  storageKey: 'shipyard:rest-api:v1',
  legacyStorageKeys: [],
  stackLabel: 'THE STACK — your resource model',
  ranks: ['SWABBIE', 'DECKHAND', 'BOATSWAIN', 'FIRST MATE', 'RESOURCE CAPTAIN', 'HARBOR MASTER'],
  accent: '#14b8a6',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum rest-api-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0\u201314 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: REST is a set of constraints, not a JSON convention. Every \u201crule\u201d is a consequence of a problem someone already hit.</span></div>',
    '<div class="bl">AWAITING CREW \u2014 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">\u258e</span></div>',
  ],
  h1: 'REST, UNDERSTOOD.<br><span class="wh">NOT JUST SNIPPED.</span>',
  sub: 'First-principles curriculum rebuilt as a mission. Learn the resource model, method semantics (safety and idempotency), statelessness, the status families, and the auth model <b>deeply</b> \u2014 reference the rest \u2014 and <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The REST-API Shipyard is a faithful interactive conversion of the first-principles REST APIs curriculum \u2014 every table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file \u2014 copy it anywhere, it runs offline.',
    rule: 'express state through representations, never through verbs in URLs. Hold that, and every \u201cwhy does this API feel wrong?\u201d answers itself.',
  },
  chips: { p2: ['<span class="m">\u2605 <b>INTERVIEW GOLD</b></span>'], p10: ['<span class="m">\u2605 <b>INTERVIEW GOLD</b></span>'] },
  grad: {
    title: 'API SHIPPED 🛰',
    rule: 'express state through representations, never through verbs in URLs. Hold that, and every \u201cwhy does this API feel wrong?\u201d answers itself.',
  },
  console: {
    cmd: '$ curl -sS -o /dev/null -w "%{http_code}" /health',
    line: '200  ·  idempotent  ·  cacheable  ·  (yours — every layer earned)',
  },
  simPrompt: '$ curl -i -X PUT https://api.example.com/orders/42 -H "If-Match: a1b2"',
  egg: { label: 'HATEOAS ENGAGED' },
  deckIn: 'p13',
  milesIn: 'p14',
  simIn: 'p10',
  deckNote: 'If you can deliver these aloud \u2014 with the WHAT \u2192 WHY IT EXISTS \u2192 ONE CONCRETE CONSEQUENCE shape \u2014 you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof, mapped to the curriculum&#39;s day plan (~1 focused hour/day, days 1\u201333). When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'The RPC era was built on a bad bet. What was it?', o: ['Pretending remote calls behave like local function calls, hiding real network failure', 'Betting that XML payloads would always beat binary wire formats', 'Assuming every client and server could redeploy in lockstep', 'Skipping versioning entirely, on the assumption interfaces would never need to change at all'], a: 0, e: 'Tight coupling (generated interface definitions broke clients on every change), a lying abstraction, enormous ceremony \u2014 and infrastructure could not help, because every call was an opaque POST.' },
      { q: 'Roy Fielding\u2019s actual contribution, stated accurately:', o: ['He helped build HTTP, then described the constraints that let the web scale', 'He invented REST first and then built the entire web around it', 'He designed JSON as the required format for web resources', 'He wrote the official specification that every REST API is required to formally satisfy'], a: 0, e: '2000 PhD dissertation: REST = REpresentational State Transfer. Each constraint buys a property: statelessness \u2192 scaling, cacheability \u2192 fewer round trips, uniform interface \u2192 generic infrastructure.' },
      { q: 'Which two constraints do most of the work \u2014 and what does each buy?', o: ['Statelessness \u2192 any server can answer; cacheability \u2192 far fewer round trips', 'Client\u2013server \u2192 nicer UIs; code-on-demand \u2192 shipping JavaScript to clients directly', 'Uniform interface \u2192 consistent naming; layering \u2192 transparent proxies', 'Code-on-demand \u2192 dynamic scripts; layering \u2192 simpler client code'], a: 0, e: 'Statelessness: any server handles any request \u2192 horizontal scaling and failover. Cacheability: \u201cthe single biggest reason the web scales\u201d \u2014 the request never happens.' },
      { q: 'Why does REST accept being chatty and verbose compared with RPC?', o: ['It trades round trips for evolvability, since clients can\u2019t be coordinated', 'Because REST simply predates RPC and never adopted any of its performance tricks', 'Because verbose payloads are simply easier for servers to compress', 'Because JSON encoding is inherently larger than any binary format'], a: 0, e: 'That is why REST wins for public APIs and is often the wrong trade for two microservices inside one cluster.' },
      { q: 'The honest thing worth saying to an interviewer about \u201cREST APIs\u201d:', o: ['Most so-called REST APIs drop hypermedia and just keep the resource model', 'Every API that calls itself RESTful fully satisfies Fielding\u2019s constraints', 'Only early SOAP services failed to meet the real REST definition', 'HATEOAS is mandatory, so an API without it isn\u2019t really an API'], a: 0, e: 'Fielding called this out publicly in 2012. Knowing it \u2014 and what the industry kept \u2014 is a senior answer.' },
    ],
    p2: [
      { q: 'A resource vs a representation:', o: ['Resource is the abstract thing; representation is what crosses the wire', 'They\u2019re two words for exactly the same underlying database row', 'The representation is simply the resource\u2019s raw database record', 'The URL itself is generally considered to be the representation of the resource'], a: 0, e: 'The name says it: REpresentational State Transfer. One resource, many representations via the Accept header; the API is a contract deliberately decoupled from your storage.' },
      { q: 'Why is GET /deleteUser?id=5 a bug, not a style choice?', o: ['GET must be safe \u2014 crawlers can trigger it and silently delete data', 'It\u2019s a bug only because the URL is longer than it needs to be', 'DELETE requests execute noticeably faster than GET requests do', 'Query strings like ?id=5 are technically forbidden by the HTTP spec itself'], a: 0, e: 'URLs name things, never actions. The verb belongs in the HTTP method \u2014 which already exists for exactly that purpose.' },
      { q: 'A POST /payments times out. What has possibly happened, and why can\u2019t the client safely retry?', o: ['The payment may have succeeded already, so a blind retry can double-charge', 'Nothing happened, so the client remains free to retry as many times as it wants', 'POST is idempotent, so retrying the exact same request is always safe', 'Timeouts only occur before the server acts, so retrying is harmless'], a: 0, e: 'The client cannot tell \u201cit didn\u2019t happen\u201d from \u201cit happened but I didn\u2019t hear back.\u201d Networks fail after the server acted but before the response arrived \u2014 the actual hard part of distributed systems.' },
      { q: 'PUT vs PATCH \u2014 omit a field in each and the meaning is:', o: ['PUT: the field is gone; PATCH: the field is simply left unchanged', 'Identical in both cases \u2014 omitted fields are always left untouched', 'PUT leaves the field alone; PATCH replaces the entire resource', 'Both methods reject the request outright when a field is omitted'], a: 0, e: 'PUT = replace wholesale (idempotent); PATCH = partial change (not necessarily idempotent). The most confused pair in APIs.' },
      { q: 'What statelessness forbids \u2014 and what it makes possible:', o: ['Forbids servers holding client session state; permits any server to answer', 'Forbids using a database at all; permits caching every response via Redis instead', 'Forbids checking authorization; permits faster cache invalidation', 'Forbids setting cookies; permits issuing long-lived JWTs instead'], a: 0, e: 'It costs: every request re-sends auth context and repeats some work. Note the precision: \u201cstateless\u201d refers to client session state, not data \u2014 shared stores like Redis are fine.' },
      { q: '401 vs 403 \u2014 the load-bearing distinction:', o: ['401 means we don\u2019t know you; 403 means we know you and say no', '401 means forbidden outright; 403 means simply not authenticated yet', 'They are interchangeable codes that most servers use at random', '401 is reserved for browsers only; 403 is reserved for native apps'], a: 0, e: 'Asked in almost every API interview. And 4xx vs 5xx tells a client whether retrying helps \u2014 retry 5xx, fix 4xx.' },
      { q: 'Which \u201charmless\u201d change quietly BREAKS clients?', o: ['Adding a new field to a response that clients don\u2019t read yet', 'Adding a new optional field to an incoming request payload', 'Tightening validation, so previously-accepted sloppy data now fails', 'Relaxing validation so more request shapes are now accepted'], a: 2, e: 'The backward-compatibility discipline: servers add rather than change; clients tolerate rather than assume. Adding an enum value also breaks clients that hard-coded a switch.' },
    ],
    p3: [
      { q: 'The four parts of every HTTP request and response:', o: ['Start line, headers, a blank line, then the body', 'URL, cookies, query string, and a signed auth token', 'Method, path, protocol version, and a JSON payload', 'Status code, reason phrase, byte count, and timing'], a: 0, e: 'Running `curl -v` against any API once, deliberately, is worth more than three tutorials.' },
      { q: 'Content-Type vs Accept, and the codes for rejection:', o: ['Content-Type is what you send; Accept is what you\u2019ll take back', 'They\u2019re simply two different names the spec uses for the exact same header', 'Accept was deprecated years ago in favor of Content-Type', 'Both headers always mean the payload format is JSON'], a: 0, e: '406 Not Acceptable vs 415 Unsupported Media Type \u2014 the easiest pair to mix up: one is about the response, the other about the request.' },
      { q: 'Where does a piece of data belong? The rule that resolves nearly every case:', o: ['Path names the resource; query filters it; headers hold metadata; body holds data', 'Put everything in the query string, since that keeps every request simple and uniform', 'Put everything in the request body, whatever it represents', 'Put everything in custom headers so the URL stays short'], a: 0, e: 'Never put secrets in the query string (URLs end up in a dozen logs), and never send a body on GET (intermediaries and caches ignore it).' },
      { q: 'Why is a body on GET a bad idea?', o: ['Intermediaries and caches don\u2019t reliably forward GET bodies', 'GET requests simply execute slower whenever a body is attached', 'The HTTP specification makes GET bodies technically illegal', 'Bodies only work when the request comes from a web browser'], a: 0, e: 'Same reason secrets must not live in URLs: intermediaries are part of the contract you cannot control.' },
    ],
    p4: [
      { q: 'URLs identify nouns; the verbs come from HTTP. Which pair is correct?', o: ['POST /books to create, DELETE /books/42 to remove one', 'POST /createBook, DELETE /deleteBook?id=5 for the same job', 'GET /getBook?id=42 to fetch a single book by its id', 'PATCH /updateBook to change any field on any book'], a: 0, e: 'The verb-shaped URL isn\u2019t merely unfashionable \u2014 it discards what HTTP gives for free: caches can\u2019t tell reads from writes, retries become unsafe, and a crawler can destroy data.' },
      { q: 'Nesting rule for resource URLs:', o: ['Nest to show containment, but stop at about two levels deep', 'Nest as many levels deep as your database schema happens to go', 'Never nest resource URLs under any circumstances', 'Only nest URLs that represent read-only views'], a: 0, e: 'Once a resource has its own identity, give it a top-level URL \u2014 /reviews/9/comments or /comments?review_id=9.' },
      { q: 'Cancel an order. The three legitimate approaches in order of RESTfulness:', o: ['PATCH the status field, POST a cancellation event, or POST /cancel', 'Only POST /orders/42/cancel is ever acceptable for this', 'PATCH is always forbidden here, so POST is the only option', 'DELETE the order resource outright, since cancelling effectively removes it entirely'], a: 0, e: 'A when it\u2019s really a field; B when the action has its own history worth recording (honest, idempotency-friendly); C when the operation is a genuine command \u2014 Stripe and GitHub do C. Defending the choice beats purity.' },
      { q: 'The most common tell of an RPC-shaped API:', o: ['Verbs baked into the URL, like /api/getUsersList, plus POST for everything', 'Returning far too many 200 status codes instead of more specific error ones', 'Using snake_case field names instead of camelCase in bodies', 'Offering cursor-based pagination instead of page numbers'], a: 0, e: 'Other tells: /tbl_users database internals, inconsistent pluralization, five-level nesting, and response shapes that change per parameter.' },
    ],
    p5: [
      { q: 'Money and 64-bit IDs in JSON — the safe forms:', o: ['Money as a string or minor units; large IDs also encoded as strings', 'Always encode both money and IDs as plain JSON numbers', 'Base64-encode both money amounts and identifiers before ever sending them', 'Use integers for money and leave large IDs as raw JSON numbers'], a: 0, e: 'Both are real bugs that have cost real money. Timestamps: ISO 8601, UTC, explicit offset — never bare “11/08/2026”.' },
      { q: 'A 200 response containing {"error": "not found"} is:', o: ['A broken API, since every cache and monitor reads 200 as success', 'Perfectly fine, as long as the error shape is documented', 'A pattern that’s especially well suited to mobile client apps', 'Exactly what the RFC 9457 problem-details standard actually recommends doing'], a: 0, e: "“Use the right status code; the body is for humans and debugging.” One of the most common serious API design errors." },
      { q: 'A good error tells the client three things:', o: ['What failed, whether retrying helps, and what to fix', 'The full stack trace, the exact line number, and library version', 'The raw SQL query, the table name, and the internal user ID', 'A single free-form prose message and nothing else structured'], a: 0, e: 'RFC 9457 shape: type, title, status, detail, instance, request_id — plus MACHINE-READABLE codes, never prose clients must parse. Report ALL validation failures at once, and never leak internals.' },
      { q: 'Validation layers and their codes:', o: ['Syntactic → 400/415; structural → 422; business rules → 422/409/403', 'There’s really only one layer, and every failure returns 500', 'All validation really belongs in the UI, so the server never needs to check it', 'Every kind of validation failure should simply return a 400'], a: 0, e: 'Never trust the client — re-enforce every UI rule on the server — and validate with a schema (pydantic, zod, JSON Schema), not hand-written ifs, which drift from the docs.' },
    ],
    p6: [
      { q: 'What’s wrong with offset pagination?', o: ['It slows as the offset grows, and skips or repeats rows as data changes', 'Nothing at all — it’s simply the only pagination style worth implementing', 'It’s fine for large data but breaks caching on the very first page', 'It only works when results are requested in descending order'], a: 0, e: 'Cursor (keyset) pagination is the correct default for large or changing data: an opaque “where I left off” (sort key + ID tiebreaker) that stays fast at any depth and is stable under inserts.' },
      { q: 'The right response metadata for a cursor page:', o: ['The page of data plus a next_cursor and a has_more flag', 'Just a bare array of items with no metadata attached', 'A total count of every row in the table, computed each time', 'An offset number telling the client how many rows to skip'], a: 0, e: 'Never return an unbounded collection — it is small in development and catastrophic in production.' },
      { q: 'The industry-recommended versioning approach:', o: ['A /v1/ path plus a strong bias toward never shipping /v2', 'Always version by request date the way Stripe’s API does it', 'Skip versioning entirely, since a public API should never change', 'Use a custom header exclusively and never touch the URL path'], a: 0, e: 'Whatever you choose — path, header, Accept, date — the discipline matters more: publish a deprecation policy (Deprecation/Sunset headers), give real dates, and retire what you can.' },
      { q: 'Why does documentation drift to wrongness — and what keeps it honest?', o: ['Hand-maintained docs rot fast; generating them from code keeps them honest', 'Because technical writers are generally careless about accuracy', 'OpenAPI is mainly just a visual editor, so it can’t really validate anything', 'Documentation is optional once an API reaches a stable release'], a: 0, e: 'From one OpenAPI document you get interactive docs, generated clients, request validation, mock servers, and contract tests — FastAPI generates it from type annotations automatically.' },
    ],
    p7: [
      { q: 'Authentication vs authorization:', o: ['Authentication is who you are; authorization is what you may do', 'They’re just two different words for the exact same login step', 'Both terms refer only to how API tokens get generated and stored', 'Authorization always happens before authentication can occur'], a: 0, e: 'Constantly conflated — and the 401/403 boundary is exactly where the conflation shows.' },
      { q: 'JWTs, honestly. Order the critical facts:', o: ['Signed, not encrypted; readable by anyone; hard to revoke before expiry', 'Encrypted and unreadable by anyone, and trivially revocable anytime', 'Unsigned by design, which is exactly why they’re safe to store in localStorage', 'Effectively tiny server-side sessions stored entirely in the token'], a: 0, e: 'Never put secrets in a JWT. Revocation problem → short-lived access tokens (5–15 min) plus a revocable, database-backed refresh token. Algorithm pinning stops the alg:none / RS256→HS256 downgrade forgeries.' },
      { q: 'The single most common cause of “my POST doesn’t work” reports:', o: ['A missing or wrong Content-Type header, which returns a 415', 'Simply hitting the wrong URL for the intended endpoint', 'Neither the client nor the endpoint being reachable at all right now', 'The server being down or unreachable at request time'], a: 0, e: 'From the Part 9 debug table for 415: “the most common cause of a ‘my POST doesn’t work’ report.”' },
      { q: 'CORS — what actually protects, and who enforces it?', o: ['It protects users from cross-origin reads, and only browsers enforce it', 'It protects your server from attackers hitting it directly', 'It encrypts traffic between the browser and the API server', 'It fully replaces the need for any separate authentication scheme entirely'], a: 0, e: 'Server grants permission via headers; non-simple requests trigger an OPTIONS preflight the API must answer. Allow-Credentials cannot combine with a wildcard origin — and never reflect arbitrary origins.' },
    ],
    p8: [
      { q: 'The create endpoint’s success response must include:', o: ['A 201 Created status with a Location header for the new resource', 'A plain 200 status code returned along with the newly created object', 'A 204 No Content response with nothing else in the payload', 'A 202 Accepted response with no further details required'], a: 0, e: '201/Location and 204-on-delete are Parts 2–3 basics — the first thing reviewers look for in Project A.' },
      { q: 'The single most common production hang in API clients:', o: ['A missing timeout, so a stuck request can wait forever', 'Parsing the JSON response body after it has already arrived', 'Using HTTPS instead of plain HTTP for the outgoing request', 'Retrying a failed request exactly once before giving up'], a: 0, e: 'Project B’s rules: credentials from env vars; explicit timeout; retry with backoff+jitter ONLY on 5xx/429/network errors — never 4xx (a bad request is still bad on the fourth attempt); honour Retry-After; paginate; cache via conditional requests; log everything except the token.' },
      { q: 'Two concurrent editors save the same record. The protection, and the code it answers with:', o: ['An ETag with If-Match, returning 412 if the version has changed', 'Last writer simply wins, and that’s considered the intended behavior', 'A Redis-based lock the server acquires before every write', 'The client double-checks the record itself before saving'], a: 0, e: 'A small amount of work that converts silent data loss into an explicit, handleable conflict — the same lost-update problem as the SQL curriculum, at the API layer.' },
      { q: 'The most secure choice for tokens in a browser — and why:', o: ['HttpOnly, Secure, SameSite cookies, paired with CSRF defenses', 'localStorage, always, since any JavaScript on the page can access it directly', 'Base64-encoding the token and appending it to the URL', 'sessionStorage, since it clears when the browser tab closes'], a: 0, e: 'JWT storage is a real trade-off, not a solved problem — the professional choice is the cookie, which needs SameSite and CSRF protection on state-changing requests.' },
    ],
    p9: [
      { q: 'The first move when an API call fails:', o: ['Establish where the failure is before guessing why it happened', 'Immediately rewrite the client code from scratch just to be safe', 'Restart the server first, before checking anything else', 'Read through the response body line by line for clues'], a: 0, e: 'The tree covers hangs, the 4xx table (400 bytes, 401 Bearer space, 404 typo or hidden object, 405 Allow header, 415 Content-Type, 422 read the body, 429 Retry-After), 5xx (read LOGS by correlation ID, never the deliberately vague response), and curl-vs-browser (CORS).' },
      { q: '“It works in curl but not in my browser app.” First suspect:', o: ['CORS — check the console and whether preflight failed', 'The server being down just for the browser but not curl', 'An expired or misconfigured HTTPS certificate on the server', 'The browser silently caching an old, stale response'], a: 0, e: 'Second most likely: the browser sends different headers, or cookies aren’t sent because of SameSite or a missing credentials: include.' },
      { q: 'The highest-value test level for an API:', o: ['Integration tests that make real HTTP requests and assert the response', 'Unit tests that only exercise isolated business-logic functions', 'Full end-to-end tests that drive the entire browser UI each time', 'Load tests that measure throughput under heavy, sustained concurrent traffic'], a: 0, e: 'Keep E2E small, and write the tests everyone skips: another user’s object ID (BOLA), no/malformed/expired tokens, and sending {"role":"admin"} to check mass assignment.' },
    ],
    p10: [
      { q: 'The full journey of GET https://api.example.com/v1/books/42 — in order:', o: ['DNS, then TCP and TLS handshakes, intermediaries, the app, a kept-alive reply', 'The app queries the database directly, then immediately returns the finished response', 'TCP first, then DNS resolution, followed by a JSON parse step', 'TLS negotiation, then UDP transport, then a DNS lookup at the end'], a: 0, e: 'Every step is a place latency and failure originate — which is why “why is this endpoint slow?” is answered with measurement, and the answer is frequently DNS, TLS, or a proxy timeout, not your code.' },
      { q: 'Why does connection reuse matter so much?', o: ['A fresh connection pays full TCP and TLS setup cost every request', 'Because reusing connections mainly just saves the client some RAM usage', 'Because HTTP/3 requires connections to be reused to function', 'Because cookies expire whenever a new connection is opened'], a: 0, e: 'The layered-system and keep-alive facts also explain why old advice — bundling every file, domain sharding — is counterproductive on HTTP/2 and HTTP/3.' },
      { q: 'HTTP/2 and HTTP/3 — what each fixed, and what remained:', o: ['H2 fixed app-level blocking but not TCP’s; H3’s QUIC over UDP fixed that too', 'H2 simply added more simultaneous requests; H3 mainly improved caching further', 'H2 introduced TLS to HTTP; H3 introduced DNS resolution', 'H2 added server push only; H3 added client pull only'], a: 0, e: 'The through-line: each version attacked the round-trip and blocking costs of the previous one. “Which problem did each solve?” is the interview question.' },
      { q: 'The two facts that shape API design more than any optimization:', o: ['Round trips dominate cost, and geography is physics you can’t avoid', 'CPU cycles and memory usage are always the real bottleneck', 'Payload size alone determines how fast an API feels to use', 'Compression settings usually matter more than anything else you can tune'], a: 0, e: 'This is precisely the over-fetching/under-fetching pressure that produced GraphQL — and why cacheability is described as the biggest reason the web scales.' },
    ],
    p11: [
      { q: 'Cache-Control public vs private — and the failure if you get it wrong:', o: ['Private is client-only, public allows shared caches — mixing them up can leak data', 'There’s no real difference between the two cache directives', 'Public caching is always perfectly safe no matter what the response actually contains', 'no-store is entirely optional and rarely changes real behavior'], a: 0, e: 'A cache hit is infinitely faster than any optimization — because the request doesn’t happen. Conditional requests (If-None-Match → 304) save bandwidth without staleness.' },
      { q: 'Idempotency keys — how they work:', o: ['The client generates a key; the server stores and replays the first result', 'The server always generates a brand-new key and hands it back on each request', 'They expire within a few minutes, so later retries are unsafe', 'They serve as a replacement for authenticating the request'], a: 0, e: 'The difference between an API that’s safe to retry and one that double-charges customers — the answer to “my POST timed out, what now?”' },
      { q: 'Rate limiting done right:', o: ['A token bucket keyed per user or API key, returning 429 with Retry-After', 'A fixed window counted strictly per IP address for every route', 'A fixed window only, applied the same way to every endpoint', 'Never rate limit authentication endpoints at all, since users need real speed'], a: 0, e: 'From the security checklist too: stricter limits on auth endpoints (credential stuffing), redact secrets in logs, allowlist outbound URLs (SSRF), and never serialize your database model directly (excessive data exposure).' },
      { q: 'The observability minimum that makes a bug report traceable:', o: ['Structured logs with a correlation ID on every line, plus latency percentiles', 'Just an uptime monitor pinging the root endpoint once every single minute or so', 'A single dashboard showing overall request counts per hour', 'Alerting configured to fire on literally every logged event'], a: 0, e: '“Intermittent failures are the hard ones” — correlation IDs and per-request latency are what make them findable at all.' },
    ],
    p12: [
      { q: 'REST is the strong default when:', o: ['Clients are diverse and external, and the domain is resource-shaped', 'Always, regardless of who the clients are or the domain', 'Never, since newer protocols have already fully replaced it in practice', 'Only when the API is being built specifically for a startup'], a: 0, e: 'Being able to ARGUE AGAINST the default is a mark of real understanding.' },
      { q: 'GraphQL is the right call when — and the price you pay:', o: ['Clients need varying slices of a graph, at the cost of easy HTTP caching', 'Whenever a project is new, simply because GraphQL feels more modern overall', 'Never, because GraphQL offers no real advantage over plain REST', 'When a team is scaling out to run many separate physical servers'], a: 0, e: 'A genuine alternative for specific problems — not a replacement for REST.' },
      { q: 'gRPC is the right call when — and the price:', o: ['Internal service-to-service calls you control, at the cost of browser support', 'Whenever you’re exposing a brand-new public-facing API', 'Whenever the primary clients are simply mobile apps sold in the app stores directly', 'Whenever integrating with an unrelated third-party vendor'], a: 0, e: 'The pragmatic industry pattern: REST at the public edge, gRPC or events internally — evolvability where you don’t control clients, efficiency where you do.' },
      { q: 'REST’s real weaknesses, stated without defending them:', o: ['Chatty round trips, over/under-fetching, and no real pagination or error standard', 'Honestly none worth mentioning — REST has no meaningful design weaknesses at all', 'It’s fundamentally unable to support any kind of data streaming', 'JSON parsing itself is unusually slow compared to other formats'], a: 0, e: 'Name these openly — then the alternatives (GraphQL, gRPC, WebSockets, SSE, queues, webhooks) each buy something specific and cost something else.' },
    ],
    p13: [
      { q: '“What is REST?” — the answer that reads senior:', o: ['A set of constraints where statelessness and a uniform interface let the web scale', '“An API that returns JSON instead of XML or plain text”', '“The direct opposite of SOAP, and nothing more specific”', '“A formal standard complete with an official compliance test every API must pass”'], a: 0, e: 'Tier 1 question 1 — and the shape that reads senior in every answer: WHAT it is → WHY it exists → ONE concrete consequence.' },
      { q: '“A client times out on a POST and doesn’t know if it succeeded. What now?”', o: ['Retry with the same idempotency key so the server returns the original result', 'Retry immediately with a completely different brand-new request and hope for the best', 'Switch the request to a GET so it’s safe to repeat freely', 'Simply ask the user to confirm whether it worked or not'], a: 0, e: 'This exact scenario is a Tier 2 differentiator question — the practical heart of the idempotency section.' },
      { q: 'Tier 3’s walk-through question:', o: ['“Walk me through everything that happens when a client calls your endpoint”', '“Which particular JSON parsing library does your team happen to prefer?”', '“What’s your personal favorite HTTP status code and why?”', '“Do you normally test your endpoints using Postman or curl?”'], a: 0, e: 'Also Tier 3: HTTP/2 vs /3 and what each fixed, Fielding’s constraints and what each buys, the HATEOAS honesty answer, BOLA, GraphQL vs gRPC trade-offs, and the resilient-client checklist.' },
      { q: 'The statement that separates people who used APIs from people who understand them:', o: ['Networks can fail after the server acts but before the response arrives', '“I’ve personally called literally thousands of totally different endpoints”', '“I can recite all seventy-some HTTP status codes from memory”', '“I’ve simply never once run into a request that timed out”'], a: 0, e: 'Every theory in this curriculum serves reasoning about retries, timeouts, and duplicate requests — the actual hard part of distributed systems.' },
    ],
    p14: [
      { q: 'Milestone 1 — Model — pass line:', o: ['Answer Part 2’s five checkpoint questions unaided, cold', 'Build and fully deploy one complete working API entirely from scratch', 'Memorize all seventy-plus official HTTP status codes', 'Successfully set up and configure Postman for testing'], a: 0, e: 'Pure understanding, no typing yet — everything later assumes the mental model.' },
      { q: 'Milestone 3 — Design — pass line:', o: ['Design a full URL surface with justified non-CRUD actions, then write valid OpenAPI', 'Copy Stripe’s entire public API surface wholesale and adapt it to a brand-new domain', 'Write a single working endpoint end to end and stop there', 'Read one blog post about REST API design best practices'], a: 0, e: 'Design is where most APIs reveal whether their author understood REST — cheap to get right, expensive to change later.' },
      { q: 'Milestone 5 — Build and consume — pass line:', o: ['Project A meets every requirement; Project B survives real network failure', 'Ship a simple static landing page describing the finished API', 'Successfully call just a single API endpoint exactly one time, nothing else', 'Deploy the finished project to a free hosting platform'], a: 0, e: 'Designing an API and living with someone else’s are different skills — each teaches what the other misses. The biggest single project phase: ~10 days.' },
      { q: 'Why is running `curl -v` by hand early the non-negotiable exercise?', o: ['Seeing the raw request once dissolves most confusion a library normally hides', 'It happens to be the single fastest command-line tool available', 'The OpenAPI specification technically requires using curl directly for this', 'It’s specifically designed to test raw network connectivity speed'], a: 0, e: 'The exercises are the curriculum. Total realistic pace: ~33 days at one focused hour per day, from M1 to M6.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 \u25b6 What is REST? (The answer that is not \u201can API that returns JSON.\u201d)', a: 'An architectural style \u2014 a set of constraints Roy Fielding identified as the reason the web scales (2000 PhD): client\u2013server, statelessness, cacheability, uniform interface, layered system, code-on-demand. Most \u201cREST APIs\u201d drop hypermedia and keep the valuable resource model + HTTP semantics. (Part 1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 \u25b6 Resource vs representation \u2014 and why is GET /getBook?id=42 an RPC costume?', a: 'The resource is the abstract thing (book, order, weather); what crosses the wire is a representation of it, chosen via Accept. URLs name THINGS, never actions \u2014 verbs already exist as HTTP methods. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 \u25b6 Safe vs idempotent \u2014 and why is a GET that deletes a genuine bug?', a: 'Safe = no server-state change (GET, HEAD). Idempotent = N identical requests have the same effect as one (GET, PUT, DELETE; NOT POST). A GET-that-deletes gets triggered eventually by a crawler or a link preview. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 \u25b6 PUT vs PATCH: omit a field in each. What have you said?', a: 'PUT: \u201cthis is the complete new state\u201d \u2014 the field should be ABSENT, replace the resource. PATCH: \u201ca description of changes\u201d \u2014 omit and you said leave it alone. PUT replaces; PATCH modifies. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 \u25b6 401 vs 403 \u2014 one sentence each.', a: '401 Unauthorized: \u201cI don\u2019t know who you are\u201d \u2014 you didn\u2019t prove identity (the name is a historical misnomer). 403 Forbidden: \u201cI know exactly who you are, and no\u201d \u2014 authenticated but not permitted. Asked in almost every interview. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 \u25b6 Status families, and why 200-with-error is a broken API.', a: '2xx worked, 3xx look elsewhere, 4xx YOU sent something wrong (retrying identically won\u2019t help), 5xx THE SERVER failed (retry may work). The code is for machines \u2014 a 200 body with {"error": "not found"} fools every client, cache, and monitor. (Parts 2, 5)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 \u25b6 The URL design rules in one breath.', a: 'URLs are nouns, methods are verbs (/books, DELETE /books/42); plural collections, consistent; nest for containment, stop at two levels; lowercase, hyphens, no file extensions, no trailing slashes, no verbs anywhere. (Part 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 \u25b6 Path vs query vs header vs body \u2014 where does data belong?', a: 'Path: WHICH resource. Query: HOW a collection is returned (filter/sort/page). Headers: metadata about the request (auth, formats, caching). Body: data being created or updated. Neither secrets in URLs (they hit a dozen logs) nor bodies on GET. (Part 3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 \u25b6 What is wrong with offset pagination, and what replaces it?', a: 'Offset gets progressively slower (every skipped row is generated and discarded) and skips/duplicates when data changes between requests \u2014 \u201crow 41\u201d is not stable. Cursor (keyset) pagination: an opaque \u201cwhere I left off\u201d (sort key + ID tiebreaker) \u2014 fast at any depth, stable under inserts, no page jumping. (Part 6)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 \u25b6 Which \u201charmless\u201d change quietly BREAKS clients \u2014 and the mutual discipline?', a: 'Tightening validation (sloppy-but-accepted data suddenly fails) and adding enum values (clients hard-code switches). Discipline: servers ADD rather than change; clients TOLERATE rather than assume \u2014 ignore unknown fields. That is evolvability in practice. (Part 2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 \u25b6 Cancel an order \u2014 the three legitimate approaches (A, B, C)?', a: 'A: PATCH /orders/42 {"status":"cancelled"} \u2014 when it is really a field. B: POST /orders/42/cancellation \u2014 event as sub-resource, carries who/why/when, idempotency-friendly. C: POST /orders/42/cancel \u2014 explicit action; what Stripe and GitHub do. Defend the choice; clarity beats purity. (Part 4)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 \u25b6 Design a good error response \u2014 what must it carry?', a: 'Right status code (machines branch here); machine-readable codes (\u201crequired\u201d, \u201cinsufficient_funds\u201d) \u2014 never prose clients must parse; all validation failures at once; a request/correlation ID; no internals. RFC 9457 shape: type, title, status, detail, instance, request_id. (Part 5)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 \u25b6 JWTs, honestly \u2014 signed or encrypted, and the biggest weakness?', a: 'SIGNED, not encrypted \u2014 anyone can read the payload; never put secrets in. Signature proves integrity without a DB lookup \u2014 but statelessness means you CANNOT easily revoke: valid until expiry. Mitigation: short-lived access tokens + revocable refresh tokens; validate sig, exp, iss, aud; PIN the algorithm (alg:none / HS256 downgrade forgeries). (Part 7)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 \u25b6 CORS \u2014 who enforces it, what it protects, and what the OPTIONS is?', a: 'Browsers ONLY \u2014 curl, Postman, servers ignore it; it protects USERS from cross-origin reads, not your API from attackers. Non-simple requests trigger an OPTIONS preflight the server must answer with Access-Control-Allow-* headers. Allow-Origin:* cannot combine with credentials \u2014 echo specific, allowlisted origins. (Part 7)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 \u25b6 A POST timed out. How do you make it safe to retry, and how do you stop silent lost updates?', a: 'Idempotency-Key: client-generated per logical operation, kept across retries; server stores the first result and returns it for repeats (24h+ retention, concurrent duplicates \u2192 409). Lost updates: ETag from GET + If-Match on PATCH \u2192 412 on conflict \u2014 explicit, handleable conflict instead of silent overwrites. (Part 11)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 \u25b6 Narrate the full journey of one API call.', a: 'DNS (cached per TTL; stale cache is a real failure mode) \u2192 TCP three-way handshake (one round trip) \u2192 TLS handshake (1\u20132 more; why HTTPS has fixed startup cost and reuse matters) \u2192 request serialized exactly as Part 3\u2019s text \u2192 intermediaries (CDN may end it there; load balancer; gateway \u2014 the layered system in action) \u2192 application: route, auth, authorize, validate, query \u2192 compressed, cached response \u2192 connection kept alive. (Part 10)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 \u25b6 The HTTP version through-line: what did 1.1, 2, and 3 each fix?', a: '1.1: persistent connections (keep-alive) + Host header + caching headers \u2014 but head-of-line blocking, hence ~6 parallel connections. H2: multiplexing + HPACK over TCP \u2014 fixed app-level HOL; TCP-level HOL remained. H3: QUIC over UDP \u2014 independent streams, faster handshake, connection migration. Consequence: bundling and domain sharding are counterproductive on H2/H3. (Part 10)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 \u25b6 When do you choose GraphQL or gRPC over REST \u2014 and what do you give up?', a: 'GraphQL: clients need widely varying subsets of a graph; mobile over-fetching/N+1 pain \u2014 you give up HTTP caching (one POST to /graphql), add query-depth limiting, and rate limiting gets harder. gRPC: internal service-to-service where you control both ends \u2014 you give up browser callability, hand-debuggability, and accept generated-stub coupling (fine inside a boundary). Industry pattern: REST at the edge, gRPC/events internally. (Part 12)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 \u25b6 Model', pass: 'Pass: the five checkpoint questions from Part 2, unaided; name Fielding\u2019s constraints and what statelessness buys. <b>(parts 1\u20132; days 1\u20132)</b>' },
    { id: 'm2', title: 'Milestone 2 \u25b6 HTTP Fluency', pass: 'Pass: read a raw request/response and name every part; deliberately trigger five status codes; explain where any piece of data belongs (path, query, header, body). <b>(part 3; days 3\u20135)</b>' },
    { id: 'm3', title: 'Milestone 3 \u25b6 Design', pass: 'Pass: full URL surface for a non-trivial domain incl. non-CRUD actions with justifications; specify error format, pagination, and a versioning strategy; write valid OpenAPI for it. <b>(parts 4\u20136; days 6\u201311)</b>' },
    { id: 'm4', title: 'Milestone 4 \u25b6 Security', pass: 'Pass: token auth with per-object ownership checks and a proven 401/403 boundary via curl; explain CORS correctly to someone else; state the JWT revocation problem and its mitigation. <b>(part 7; days 12\u201316)</b>' },
    { id: 'm5', title: 'Milestone 5 \u25b6 Build and Consume', pass: 'Pass: Project A meets all thirteen requirements; Project B survives network failure, bad credentials, rate limiting, and a multi-page collection. <b>(part 8; days 17\u201326)</b>' },
    { id: 'm6', title: 'Milestone 6 \u25b6 Depth', pass: 'Pass: debug an unfamiliar failing integration via the Part 9 tree; deliver the Part 10.1 narration; implement idempotency keys and If-Match and say what each prevents; argue for GraphQL or gRPC over REST and name what you lose. <b>(parts 9\u201312; days 27\u201333)</b>' },
  ],

  sim: [
    { t: 800, l: "<span class=\"a\">resolve</span> api.example.com \u2192 IP, via a cascade of DNS caches ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">tcp</span> SYN, SYN-ACK, ACK \u2014 one round trip before any data moves ... <span class=\"ok\">OK</span>" },
    { t: 1400, l: "<span class=\"a\">tls</span> certificates + key agreement \u2014 1\u20132 round trips, why reuse matters ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">send</span> request line, headers, blank line, body \u2014 the text from Part 3 ... <span class=\"ok\">OK</span>" },
    { t: 1300, l: "<span class=\"a\">edge</span> CDN / load balancer / gateway inspect it \u2014 layered system ... <span class=\"ok\">OK</span>" },
    { t: 1000, l: "<span class=\"a\">app</span> route, authenticate, authorize, validate, query ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">reply</span> gzip-compressed, cacheable per Cache-Control ... <span class=\"ok\">OK</span>" },
    { t: 900, l: "<span class=\"a\">reuse</span> connection kept alive \u2014 round trips dominate ... <span class=\"ok\">OK</span>" },
  ],
};