// sql pedagogy — authored from sql-curriculum.md
export default {
  title: 'The SQL Shipyard - SQL: A First-Principles Curriculum',
  brand: 'THE SQL SHIPYARD',
  tagline: 'SQL \u00b7 relational model \u00b7 three-valued logic \u00b7 query engine',
  storageKey: 'shipyard:sql:v1',
  legacyStorageKeys: [],
  stackLabel: 'THE STACK — your query plan',
  ranks: ['SWABBIE', 'DECKHAND', 'BOATSWAIN', 'FIRST MATE', 'QUERY CAPTAIN', 'HARBOR MASTER'],
  accent: '#3b82f6',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum sql-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0\u201314 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: SQL reads like a sentence and lies like a spreadsheet. NULL is not a value \u2014 it is the absence of one.</span></div>',
    '<div class="bl">AWAITING CREW \u2014 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">\u258e</span></div>',
  ],
  h1: 'SQL, UNDERSTOOD.<br><span class="wh">NOT JUST QUERIED.</span>',
  sub: 'This is a first-principles curriculum rebuilt as a mission. Learn the relational model <b>deeply</b> \u2014 think in sets, not loops; every query is a pipeline with a fixed evaluation order \u2014 and reference the rest. Along the way you <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The SQL Shipyard is a faithful interactive conversion of the first-principles SQL curriculum \u2014 every concept, table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file \u2014 copy it anywhere, it runs offline.',
    rule: 'think in sets, not loops. Hold that, and every \u201cwhy doesn\u2019t this work?\u201d answers itself from the evaluation pipeline.',
  },
  chips: { p10: ['<span class="m">\u2605 <b>INTERVIEW GOLD</b></span>'] },
  grad: {
    title: 'QUERY PLANNED 🗄',
    rule: 'think in sets, not loops. Hold that, and every \u201cwhy doesn\u2019t this work?\u201d answers itself from the evaluation pipeline.',
  },
  console: {
    cmd: '$ psql -c "EXPLAIN ANALYZE ..."',
    line: 'index scan  ·  0 seq scans  ·  (yours — every layer earned)',
  },
  simPrompt: '-- EXPLAIN ANALYZE SELECT * FROM orders JOIN customers USING (customer_id);',
  egg: { label: 'FULL TABLE SCAN' },
  deckIn: 'p13',
  milesIn: 'p14',
  simIn: 'p10',
  deckNote: 'If you can deliver these aloud \u2014 with the WHAT \u2192 WHY IT EXISTS \u2192 ONE CONCRETE CONSEQUENCE shape \u2014 you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof, mapped to the curriculum&#39;s day plan (~1 focused hour/day). When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'Before relational databases, every query was ______ \u2014 the problem SQL was built to kill.', o: ['A program that navigated pointers', 'A spreadsheet formula', 'A JSON document', 'A stored procedure'], a: 0, e: 'Hierarchical/network databases (IMS, CODASYL) forced navigation code per question. Codd\u2019s 1970 paper made data a mathematical relation and queries declarative.' },
      { q: 'Codd\u2019s core insight, in one sentence:', o: ['Store data as table files with row pointers', 'Model data as relations (sets of tuples) and let a query language describe WHAT you want, leaving HOW to the system', 'Put everything in one giant CSV', 'Use key-value pairs for speed'], a: 1, e: 'Declarative querying + data independence \u2014 the schema is separate from the storage. That separation is the entire point of the 1970 paper.' },
      { q: 'Why does SQL read like an English sentence?', o: ['So lawyers can audit it', 'SEQUEL was designed as Structured English Query Language \u2014 the \u201cEnglish-like\u201d goal is why it reads unlike C', 'SQL was translated from Latin', 'It was a marketing requirement'], a: 1, e: 'Chamberlin and Boyce aimed at a language non-specialists could read. That lineage is why SQL has no loops.' },
      { q: 'The most important addition to SQL since the original standard:', o: ['JSON support in SQL:2016', 'Window functions (SQL:1999) \u2014 they replace whole categories of painful workarounds', 'The LIKE operator', 'The NULL keyword'], a: 1, e: 'Anything written before ~2010 shows painful workarounds for problems windows solve in one line.' },
      { q: 'The NoSQL lesson the industry relearned:', o: ['SQL does not scale to the web', 'Declarative SQL over a well-defined schema is extremely hard to beat \u2014 NoSQL systems now add SQL-ish layers', 'MongoDB beats Postgres for analytics', 'Primary keys are unnecessary'], a: 1, e: 'The 2010s trades (joins, transactions, ad-hoc queries) were often accepted for the wrong reasons; NewSQL and warehouses consolidated back on SQL.' },
    ],
    p2: [
      { q: 'A table is:', o: ['A spreadsheet with positions', 'An unordered set of rows, each with the same named, typed columns \u2014 no inherent order, no current row', 'A list of records in insertion order', 'A JSON array'], a: 1, e: 'Without ORDER BY you get whatever the engine found convenient \u2014 order can change between runs. WHERE defines a subset; it does not walk the data.' },
      { q: 'Why can\u2019t you use a SELECT alias in WHERE but you can in ORDER BY?', o: ['Aliases are forbidden for security', 'WHERE (step 3) runs before SELECT (step 6); ORDER BY (step 8) runs after \u2014 the alias does not exist yet', 'The optimizer forbids it', 'NULL semantics'], a: 1, e: 'Logical order: FROM \u2192 JOIN \u2192 WHERE \u2192 GROUP BY \u2192 HAVING \u2192 SELECT \u2192 DISTINCT \u2192 ORDER BY \u2192 LIMIT. Memorize the pipeline.' },
      { q: 'WHERE city = \u2018IN\u2019 returns 30 rows; WHERE city <> \u2018IN\u2019 returns 50. The table has 100 rows, 20 NULLs. Where are the 20?', o: ['Silently dropped \u2014 NULL yields UNKNOWN, and WHERE keeps only rows where the condition is TRUE', 'The query was wrong', 'They went to HAVING', 'Index misses'], a: 0, e: 'UNKNOWN is discarded exactly like FALSE \u2014 so the two queries never sum to the whole table, and reports get silently corrupted.' },
      { q: 'COUNT(*) vs COUNT(city) on a 100-row table with 20 NULLs:', o: ['100 vs 80', '100 vs 100', '80 vs 100', 'Identical'], a: 0, e: 'COUNT(*) counts rows; COUNT(column) counts non-NULL values only \u2014 a classic interview question. GROUP BY buckets all NULLs together as ONE group.' },
      { q: 'The correct way to test for NULL:', o: ['col = NULL', 'col <> NULL', 'col IS NULL', 'col == NULL'], a: 2, e: 'NULL = NULL is UNKNOWN, not TRUE. = NULL never matches \u2014 a silent bug. Use IS NULL / IS NOT NULL; COALESCE returns the first non-NULL argument.' },
    ],
    p3: [
      { q: 'SELECT * is:', o: ['Always the best habit', 'Fine to explore, bad in saved code: returns unused data, silently changes shape when columns are added, hides dependencies', 'Required for indexes', 'Faster in production'], a: 1, e: 'Name your columns in anything you keep \u2014 SELECT * hides which columns your code actually depends on.' },
      { q: 'AND and OR precedence:', o: ['OR binds tighter than AND', 'AND binds tighter than OR \u2014 WHERE a AND b OR c means (a AND b) OR c', 'Equal, left-associative', 'Undefined in SQL'], a: 1, e: 'Always parenthesize mixed conditions \u2014 wrong-but-plausible results are the real cost.' },
      { q: 'LIKE \u2018A%\u2019 can use an index; LIKE \u2018%A\u2019 cannot because:', o: ['Percent signs disable indexes', 'Indexes are ordered by the START of the value \u2014 a leading wildcard has no starting point to walk', 'Leading wildcards are illegal', 'The optimizer never tries'], a: 1, e: 'Same reason \u201cfind everyone named Priya\u201d fails in a phone book ordered by (last, first). For text search use full-text search, not LIKE \u2018%term%\u2019.' },
      { q: 'Wrapping a column in a function usually:', o: ['Improves selectivity', 'Disables its index \u2014 WHERE YEAR(created_at) = 2026 vs a range check on created_at', 'Forces a bitmap scan', 'Fixes NULLs'], a: 1, e: 'The range version uses the index; the function-wrapped version typically cannot. One of the most common real-world speedups.' },
      { q: 'Money in SQL must be stored as:', o: ['DOUBLE PRECISION for speed', 'NUMERIC(12,2)/DECIMAL \u2014 exact base-10 arithmetic; floats make 0.1 + 0.2 <> 0.3', 'TEXT for display', 'INTEGER cents'], a: 1, e: 'Floats are binary approximations \u2014 fine for measurements, never for money. Types are constraints, and constraints are documentation the database enforces.' },
    ],
    p4: [
      { q: 'A join is conceptually:', o: ['Adding columns to a table', 'Forming combinations of rows from two tables and keeping those satisfying your condition \u2014 a NEW set, wider, and possibly larger', 'Merging two files', 'A subquery'], a: 1, e: 'Row count may grow: an order with 3 line items joined to order_items yields 3 rows \u2014 SUM over order-level columns then triple-counts.' },
      { q: 'The LEFT JOIN that silently became an INNER JOIN:', o: ['Conditions on the optional table in WHERE discard the NULL-padded rows \u2014 put them in ON instead', 'LEFT JOIN always keeps all rows', 'Use FULL OUTER JOIN', 'NULLs are kept'], a: 0, e: 'WHERE evaluates NULL >= date as UNKNOWN and drops it. Rule: conditions on the preserved table \u2192 WHERE; conditions on the optional table \u2192 ON.' },
      { q: 'Forgetting a join condition entirely yields:', o: ['A NULL result', 'A CROSS JOIN \u2014 10,000 \u00d7 10,000 rows = 100,000,000', 'An error', 'A UNION'], a: 1, e: 'The query that never finishes or fills the disk. Sanity habit: COUNT(*) before and after every join.' },
      { q: 'UNION vs UNION ALL:', o: ['The same thing', 'UNION dedupes (costs a sort/hash); UNION ALL keeps duplicates \u2014 faster; prefer it when dedup is not needed', 'UNION ALL is deprecated', 'UNION is faster'], a: 1, e: 'INTERSECT and EXCEPT complete the set algebra \u2014 EXCEPT elegantly answers \u201cwhat\u2019s missing?\u201d when reconciling two sources.' },
      { q: 'Employees to managers is written as:', o: ['A CROSS JOIN', 'A self-join: employees e LEFT JOIN employees m ON m.id = e.manager_id \u2014 LEFT so the CEO is not dropped', 'A UNION', 'Two subqueries'], a: 1, e: 'Self-joins relate rows within one table. Alias every table and qualify every column.' },
    ],
    p5: [
      { q: 'With no GROUP BY, an aggregate query returns:', o: ['One row per table', 'One row \u2014 the whole table is a single group', 'Zero rows', 'An error'], a: 1, e: 'The entire result set is one group. COUNT(*) rows; COUNT(col) non-NULL; COUNT(DISTINCT col) unique non-NULL.' },
      { q: 'Why must every non-aggregated SELECT column appear in GROUP BY?', o: ['Style guide', 'Many values exist per bucket and the database has no basis to choose one \u2014 Postgres errors, MySQL (historically) silently returned an arbitrary value', 'To save memory', 'Because of NULLs'], a: 1, e: 'Silent arbitrary values are worse than errors \u2014 they hide the mistake.' },
      { q: 'WHERE vs HAVING:', o: ['Aliases', 'WHERE filters ROWS before grouping (step 3); HAVING filters GROUPS after aggregation (step 5) \u2014 WHERE cannot see aggregates, HAVING can', 'HAVING is faster for row filters', 'They are synonyms'], a: 1, e: 'A row-level condition in HAVING works but is slower \u2014 you grouped rows you were going to throw away. Filter as early as possible.' },
      { q: 'AVG of per-city averages is not the overall average because:', o: ['AVG is broken', 'Averaging averages ignores different row counts per group \u2014 aggregate from base rows', 'NULLs inflate it', 'Precision loss'], a: 1, e: 'AVG also silently skips NULLs and changes the denominator. If NULL should be zero, use AVG(COALESCE(col, 0)) deliberately.' },
      { q: 'Conditional metrics in one pass:', o: ['COUNT(*) FILTER (WHERE ...) or SUM(CASE WHEN ...) \u2014 far better than three separate queries', 'Three subqueries', 'A UNION ALL', 'A window function'], a: 0, e: 'FILTER (Postgres) or CASE-inside-aggregate computes several metrics in one scan. GROUPING SETS / ROLLUP / CUBE produce subtotals in one query.' },
    ],
    p6: [
      { q: 'The NOT IN trap:', o: ['If the subquery returns even one NULL, NOT IN returns NO rows \u2014 silently. Use NOT EXISTS instead', 'NOT IN is NULL-safe', 'NOT IN is faster than NOT EXISTS', 'NULLs are ignored'], a: 0, e: 'x NOT IN (1, NULL) = x <> 1 AND x <> NULL \u2014 the second half is always UNKNOWN, so nothing can match. One of the highest-value facts in the curriculum.' },
      { q: 'A correlated subquery:', o: ['Runs once per outer row \u2014 optimizers often rewrite them as joins, but when they don\u2019t, large tables hit a performance cliff', 'Is always faster than a join', 'Cannot be indexed', 'Is the same as a CTE'], a: 0, e: 'Prefer a join or CTE when row counts are large. EXISTS stops at the first match; IN builds the full set.' },
      { q: 'Why is a CTE the readability tool?', o: ['CTEs compile to assembly', 'They name intermediate results so a query reads top-to-bottom like the evaluation pipeline, instead of inside-out nesting', 'They replace indexes', 'They run in parallel'], a: 1, e: 'The single biggest improvement for complex SQL. CTEs can reference earlier CTEs; WITH RECURSIVE traverses hierarchies and graphs.' },
      { q: 'GROUP BY vs a window function \u2014 the whole difference:', o: ['Windows are faster', 'GROUP BY collapses rows (10 in \u2192 3 out); a window function keeps every row and adds a computed column (10 in \u2192 10 out)', 'They are identical', 'Windows need partitioning'], a: 1, e: 'SUM(total) OVER (PARTITION BY customer_id) computes per-customer aggregates without collapsing rows \u2014 lifetime value next to every order.' },
      { q: 'For values 10, 10, 9: ROW_NUMBER, RANK, DENSE_RANK give:', o: ['1,2,3 | 1,1,3 | 1,1,2', '1,1,2 | 1,2,3 | 1,1,3', '1,2,3 for all', '1,1,1'], a: 0, e: 'A guaranteed interview question: ROW_NUMBER breaks ties arbitrarily; RANK leaves a gap; DENSE_RANK does not. The \u201clatest row per group\u201d pattern wraps a window in a CTE because WHERE runs before windows are computed.' },
    ],
    p7: [
      { q: 'Why are constraints the database defending your data?', o: ['They save disk', 'Application code is ONE of many paths to the data \u2014 scripts, migrations, admin tools, a console at 2am. Only the database can enforce a rule against all of them', 'They speed up queries', 'They are optional metadata'], a: 1, e: 'Constraints are the last line of defence AND documentation that cannot go out of date. ON DELETE: RESTRICT blocks, CASCADE deletes children (dangerous), SET NULL orphans deliberately.' },
      { q: 'The habit that prevents catastrophe before UPDATE/DELETE:', o: ['Wrap in TRUNCATE', 'Run the same WHERE as a SELECT first and confirm the row count \u2014 a missing WHERE rewrites the table, and there is no undo outside a transaction', 'Add ORDER BY', 'Use CASCADE'], a: 1, e: 'Then wrap destructive changes in an explicit transaction so you can ROLLBACK \u2014 the only undo you get.' },
      { q: '3NF in one habit:', o: ['All columns numeric', 'If you find yourself updating the same fact in more than one place, the schema is wrong \u2014 copies eventually disagree', 'Every table has an index', 'No NULLs allowed'], a: 1, e: '1NF: atomic values. 2NF: no partial-key dependencies. 3NF: no non-key-to-non-key dependencies. Denormalization is legitimate only deliberately, after measuring.' },
      { q: 'Natural vs surrogate keys:', o: ['Natural keys are always better', 'Prefer surrogate keys \u2014 real-world identifiers change, and a changing primary key means updating every referencing row. Enforce real-world uniqueness separately with a UNIQUE constraint', 'Surrogates leak data', 'UUIDs beat integers everywhere'], a: 1, e: 'Auto-increment: compact, index-friendly, ordered. UUID: globally unique, no count leaks, worse locality. Both defensible \u2014 know the trade-off.' },
      { q: 'Checks that belong in the schema:', o: ['CHECK (status IN (...)), CHECK (total >= 0), NOT NULL, UNIQUE on email \u2014 domain rules the database enforces no matter which code path touches it', 'All validation in the app layer', 'No constraints for speed', 'Frontend-only validation'], a: 0, e: 'A CHECK makes an invalid state structurally impossible instead of merely discouraged. The database is the last line of defence \u2014 use it.' },
    ],
    p8: [
      { q: 'unit_price lives on order_items, not products, because:', o: ['It saves a join', 'The price at time of sale is a DIFFERENT fact from the product\u2019s current price \u2014 an old order must keep what it was sold at', 'Product prices are static', 'Normalization forbids it'], a: 1, e: 'Recognizing when one fact is actually two is real data-modelling skill. An order from January must not change when the price changes in June.' },
      { q: 'Revenue by month with empty months still shown:', o: ['Use HAVING', 'A generated date series LEFT JOINed to the data, COALESCE the zeros', 'Use a window function', 'Aggregate in the app'], a: 1, e: 'The empty-group problem: an INNER JOIN would delete zero-revenue months entirely.' },
      { q: 'The reconciliation query \u201corders whose payments don\u2019t sum to the total\u201d needs:', o: ['GROUP BY with HAVING \u2014 with careful NULL handling for unpaid orders', 'A CROSS JOIN', 'DISTINCT', 'An index'], a: 1, e: 'Unpaid orders left-join to no payments \u2014 compare COALESCE(SUM(p.amount), 0) <> o.total so NULL does not hide them.' },
      { q: 'The hardest, most realistic Part 8 query:', o: ['Top 10 products', 'Customer cohort retention: first-order month cohorts, then who ordered again in each later month \u2014 multiple CTEs', 'Month-over-month growth', '7-day average'], a: 1, e: 'Cohort retention exercises grouping, date truncation, and the ROW_NUMBER pattern in combination \u2014 the most realistic analytics query in the ship.' },
      { q: 'The deliberate design choices in the project schema:', o: ['5 tables, FKs, unit_price snapshotted, NULL shipped_at, generated data at scale \u2014 enough that bad queries actually feel slow', 'One denormalized wide table', 'Two tables for speed', 'No indexes'], a: 0, e: 'Deliberate NULLs and data volume are the only way Part 9\u2019s EXPLAIN work becomes real.' },
    ],
    p9: [
      { q: 'The FIRST move for \u201cmy query returns wrong data\u201d:', o: ['EXPLAIN ANALYZE', 'Check the row count \u2014 COUNT(*) at each stage; a join that changed the count is the multiplication bug', 'Add an index', 'Rewrite in a CTE'], a: 1, e: 'Then: simplify until right and rebuild; suspect NULL (<>, WHERE on a LEFT JOINed table, NOT IN); check join grain; check AND/OR precedence; check silent type coercion.' },
      { q: 'EXPLAIN vs EXPLAIN ANALYZE:', o: ['Identical', 'EXPLAIN shows the plan; EXPLAIN ANALYZE RUNS it and shows real timings and row counts next to the estimates', 'ANALYZE is for indexes', 'ANALYZE is the app'], a: 1, e: 'Read plans inside-out and bottom-up \u2014 innermost nodes run first. Careful: ANALYZE executes; wrap UPDATE/DELETE in a transaction you roll back.' },
      { q: 'A Seq Scan on a large table means:', o: ['The index is broken', 'The engine is reading every row \u2014 add an index on the filtered column UNLESS the query genuinely needs most of the table, in which case the scan is correct', 'ANALYZE failed', 'A hash join is needed'], a: 1, e: 'The optimizer is not ignoring your index out of spite \u2014 when you need most rows, sequential page reads beat random index lookups.' },
      { q: 'Estimated rows wildly different from actual rows:', o: ['SQL is broken', 'The optimizer works from bad information \u2014 run ANALYZE to refresh statistics', 'Add an index', 'Use a hint'], a: 1, e: 'Stale statistics after a bulk load is the classic \u201cfast yesterday, slow today\u201d \u2014 the fix is ANALYZE, not a rewrite.' },
      { q: 'An index on (a, b) can serve queries filtering on:', o: ['b alone', 'a, or a AND b \u2014 but generally not b alone (a phone book ordered by last name is useless for finding \u201cPriya\u201d)', 'Neither', 'Everything'], a: 1, e: 'Composite index order is lexicographic. Indexes cost writes \u2014 unused ones are pure overhead. Function-wrapped columns defeat them (LOWER(email) needs an expression index).' },
    ],
    p10: [
      { q: 'The query lifecycle order:', o: ['Optimizer \u2192 parser \u2192 executor', 'Parser \u2192 analyzer \u2192 rewriter \u2192 planner/optimizer \u2192 executor', 'Executor \u2192 planner \u2192 parser', 'Analyzer \u2192 executor'], a: 1, e: 'The rewriter flattens subqueries and pushes predicates down; the optimizer enumerates plans, costs them with statistics, picks one; the executor pulls rows through the plan tree.' },
      { q: 'Rows live in fixed-size pages (\u22488KB); reading one row costs:', o: ['1 byte', 'At least one page read \u2014 wider rows mean fewer per page and more I/O. The buffer pool is why warm runs are faster', 'Nothing at all', 'A full table scan'], a: 1, e: 'Always benchmark warm, not cold \u2014 or you will optimize the wrong thing.' },
      { q: 'Row-oriented vs column-oriented storage:', o: ['Everything is row-oriented now', 'Postgres/MySQL optimize whole rows (OLTP); warehouses (BigQuery, Snowflake, DuckDB) are columnar \u2014 far better compression and scan speed for aggregates', 'Columnar is for OLTP', 'No difference'], a: 1, e: 'Knowing this distinction is a strong interview signal: analytics consolidated on columnar for a reason.' },
      { q: 'The three join algorithms:', o: ['Nested Loop (tiny outer + indexed inner), Hash Join (large unsorted equality joins \u2014 the analytics workhorse), Merge Join (both inputs already sorted)', 'Loop, Recursion, Parallel', 'Index, Scan, Sort', 'Seq, GIN, BRIN'], a: 0, e: 'Seeing Nested Loop on two million-row tables tells you exactly what is missing: an index on the join key.' },
      { q: 'Why do leading wildcards fail and ranges work on a B-tree?', o: ['Magic', 'Leaves are ordered and linked \u2014 ranges find the start and walk; LIKE \u2018%x\u2019 has no starting point in prefix order. Depth is logarithmic: one key among a billion = a handful of reads', 'B-trees are sorted by suffix', 'Indexes are linear'], a: 1, e: 'Composite key order matters for the same reason. Indexes can also satisfy ORDER BY for free.' },
    ],
    p11: [
      { q: 'The four ACID guarantees:', o: ['Atomicity, Consistency, Isolation, Durability \u2014 a crash mid-transfer cannot destroy money', 'Availability, Consistency, Partitioning, Latency', 'All, Some, None, Some', 'Atomic, Basic, Complex, Durable'], a: 0, e: 'Durability comes from write-ahead logging \u2014 the log is flushed before data pages. Wrap multi-statement or destructive changes in an explicit transaction.' },
      { q: 'Postgres\u2019s default isolation level:', o: ['SERIALIZABLE', 'READ COMMITTED \u2014 and Oracle\u2019s too; MySQL/InnoDB defaults to REPEATABLE READ', 'READ UNCOMMITTED', 'REPEATABLE READ'], a: 1, e: 'Knowing your database\u2019s default \u2014 and what it does NOT protect you from \u2014 is a genuine senior distinction.' },
      { q: 'The lost update:', o: ['A network problem', 'Two transactions read, modify, and write the same row \u2014 one overwrites the other silently. SELECT balance \u2192 app math \u2192 UPDATE is unsafe at default levels', 'A deleted row', 'A NULL bug'], a: 0, e: 'Fixes: do arithmetic in SQL (SET balance = balance - 500), lock the row (SELECT ... FOR UPDATE), make the bad state impossible with a constraint, or SERIALIZABLE + retries.' },
      { q: 'A deadlock:', o: ['A table lock that never releases', 'Two transactions each hold what the other needs \u2014 the database detects the cycle and kills one; prevent by acquiring locks in a consistent order and keeping transactions short', 'A NULL comparison', 'A GIL problem'], a: 1, e: 'Never hold a transaction open across user interaction or a network call \u2014 that is how deadlocks and lost updates are born.' },
      { q: 'Parameterized queries exist because:', o: ['They are faster', 'String-concatenating user input is SQL injection \u2014 still the top web vulnerability class, and no escaping is as reliable as parameterization. WHERE id = ? with a bound parameter is safe', 'They are prettier', 'They avoid NULLs'], a: 1, e: 'Non-negotiable: query parameters, least privilege, migrations not manual changes, restored backups, keyset pagination for large offsets, watch N+1, monitor slow queries, store UTC.' },
    ],
    p12: [
      { q: 'A relational database is the right tool when:', o: ['Always', 'Data has structure and relationships; you need ad-hoc queries you did not anticipate; correctness under concurrency matters (money, inventory, bookings); you want the database to enforce integrity', 'Never', 'Only for analytics'], a: 1, e: 'Nearly all business data qualifies. SQL\u2019s real weaknesses: dialect fragmentation, queries hard to test/version, no reuse discipline, clause order vs evaluation order.' },
      { q: 'A graph database (Neo4j) beats SQL when:', o: ['Always', 'Deeply hierarchical or graph-shaped traversal \u2014 \u201cfriends of friends of friends\u201d \u2014 where recursive joins get expensive and awkward', 'For simple key-value caching', 'For JSON documents'], a: 1, e: 'Choose based on access patterns and consistency needs, not on which technology sounds modern.' },
      { q: 'Redis / DynamoDB are a better fit than SQL for:', o: ['Complex reporting', 'Simple key-value lookups at extreme throughput; caching; ephemeral session data \u2014 the relational engine\u2019s overhead buys nothing', 'Transactions', 'Joins'], a: 1, e: 'The 2010s lesson cuts both ways \u2014 many NoSQL adoptions paid in lost joins and transactions for scale never reached.' },
      { q: 'JSONB in Postgres is worth knowing because:', o: ['It replaces indexes', 'It covers a large fraction of \u201cschemaless documents\u201d without giving up joins and transactions \u2014 a document store is not your only choice', 'It is faster than JSON', 'It is a graph engine'], a: 1, e: 'Use JSONB for genuinely irregular attributes \u2014 not as an escape from schema design.' },
      { q: 'Petabyte-scale analytical scans:', o: ['Belong in Postgres', 'Belong on columnar warehouses (BigQuery, Snowflake, DuckDB) \u2014 row storage reads columns you do not need', 'Cannot be done', 'Need Redis'], a: 1, e: 'Row stores optimize whole rows for OLTP; analytics reads few columns across many rows \u2014 that is exactly what columnar storage is built for.' },
    ],
    p13: [
      { q: 'The single most reliable differentiator in a SQL interview:', o: ['Memorized trivia', 'Thinking in sets, not loops \u2014 running query logic through the evaluation pipeline out loud', 'Knowing all vendors', 'Fast typing'], a: 1, e: 'Everything in this curriculum exists to earn that: logical order recited cold, the NULL rules, the join grain questions.' },
      { q: '\u201cDoes this query use my index?\u201d \u2014 answer shape:', o: ['\u201cProbably yes\u201d', 'Check for the three killers: leading wildcard, function-wrapped column, missing composite prefix \u2014 then EXPLAIN to confirm', 'EXPLAIN cannot show it', 'Indexes are automatic'], a: 1, e: 'Deliver the WHAT \u2192 WHY IT EXISTS \u2192 ONE CONCRETE CONSEQUENCE shape and you stand out immediately.' },
      { q: 'The retrieval skill the deck trains:', o: ['Speed reading', 'Answering aloud from memory BEFORE flipping \u2014 the only honest way to test recall', 'Highlighting', 'Rereading'], a: 1, e: 'Rereading feels productive and teaches nothing. Nailed cards dim for a reason.' },
      { q: 'When an interviewer says \u201cmy query returns too few rows\u201d, your first move:', o: ['Blame the database', 'Check the COUNT at each stage \u2014 join grain, then the NULL suspects, from the curriculum\u2019s debugging order', 'Rewrite with a window', 'Add an index'], a: 1, e: '\u201cToo few rows\u201d: join multiplied somewhere or NULL dropped rows. \u201cToo many rows\u201d: join grain widened. That pairing is all interviewers are testing.' },
    ],
    p14: [
      { q: 'The point of six milestones:', o: ['Six badges for cosmetics', 'Six layers of proof that the model is real \u2014 each milestone is a deliverable, not a checkbox', 'Only one matters', 'They replace quizzes'], a: 1, e: 'Milestones are how you know you can DO the thing, not just recognize it \u2014 days 1\u20138 of a focused hour per day.' },
      { q: 'Milestone 1 demands:', o: ['A dashboard', 'The logical order recited cold, three-valued logic checks (NULL riddles), and set-thinking queries on one table', 'An index diagram', 'A join'], a: 1, e: 'Everything after this builds on the pipeline\'s order \u2014 it must be reflex, not recall.' },
      { q: 'The milestone that proves engine-level understanding:', o: ['Milestone 5 \u2014 EXPLAIN ANALYZE three slow queries, diagnose (index missing, statistics stale, function wrap), and fix each with evidence', 'Milestone 2', 'Milestone 4', 'Milestone 1'], a: 1, e: 'Reading a plan inside-out and bottom-up, then making the numbers move, is the line between users of SQL and students of the engine.' },
      { q: 'The capstone asks you to prove production readiness by:', o: ['A certificate', 'Transactions: explain ACID, the lost update, deadlocks, and parameterized queries under mock questioning', 'An ORM migration', 'Reporting'], a: 1, e: 'The final effect: \u201cthink in sets, not loops\u201d now applies to schema, engine, and production behavior at once.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 \u25b6 Recite the logical order of query evaluation. Why can\u2019t WHERE use a SELECT alias?', a: 'FROM \u2192 JOIN \u2192 WHERE \u2192 GROUP BY \u2192 HAVING \u2192 SELECT \u2192 DISTINCT \u2192 ORDER BY \u2192 LIMIT. WHERE (step 3) runs before SELECT (step 6), ORDER BY (step 8) after \u2014 so ORDER BY can use aliases, WHERE cannot. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 \u25b6 A 100-row table has 20 NULL cities; WHERE city <> \u2018IN\u2019 returns 50 of 80 non-null. Where are the rest?', a: 'Dropped \u2014 NULL yields UNKNOWN and WHERE keeps only TRUE. The two queries never sum to the table, silently. Test NULL with IS NULL, not = or <>. (Part 2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 \u25b6 What is a self-join, and why LEFT JOIN for employees-to-manager?', a: 'Joining a table to itself with aliases: e LEFT JOIN m ON m.id = e.manager_id. LEFT keeps rows with no match \u2014 the CEO has no manager and would vanish under INNER. (Part 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 \u25b6 Your LEFT JOIN silently filtered rows to an inner join. The cause and the fix?', a: 'You put a condition on the OPTIONAL table in WHERE \u2014 its NULL-padded rows evaluate UNKNOWN and drop. Move it into ON. Conditions on the preserved table stay in WHERE. (Part 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 \u25b6 What is the NOT IN NULL trap, and the safer alternative?', a: 'x NOT IN (1, NULL) is x <> 1 AND x <> NULL \u2014 always UNKNOWN, so it returns NOTHING, silently. Use NOT EXISTS (or NOT IN ... WHERE col IS NOT NULL). (Part 6)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 \u25b6 WHERE vs HAVING \u2014 when is each evaluated, and which cannot see aggregates?', a: 'WHERE filters rows before grouping (step 3); HAVING filters groups after (step 5). WHERE cannot see aggregates like COUNT; HAVING cannot filter single rows efficiently. (Part 5)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 \u25b6 For values 10, 10, 9, what do ROW_NUMBER, RANK, DENSE_RANK assign?', a: 'ROW_NUMBER: 1,2,3 (tie broken arbitrarily). RANK: 1,1,3 (gap after tie). DENSE_RANK: 1,1,2 (no gap). The classic trinity question. (Part 6)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 \u25b6 Why are constraints the database defending your data \u2014 and what habit pairs with destructive statements?', a: 'The database is the LAST line of defense against every code path \u2014 scripts, consoles, migrations. Pair with: run the same WHERE as a SELECT first, wrap in a transaction you can ROLLBACK. (Part 7)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 \u25b6 Why can LIKE \u2018A%\u2019 use an index but LIKE \u2018%A\u2019 cannot \u2014 and what else kills indexes?', a: 'B-tree leaves are in prefix order with linked ranges \u2014 a leading wildcard has no starting point. Function-wrapped columns (YEAR(created_at)) also defeat the index; use range checks. (Parts 3, 9)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 \u25b6 I have an index on (a, b). Which filters can use it?', a: 'Filters on a alone or a AND b \u2014 generally NOT b alone. Composite index order is lexicographic, like a phone book ordered by last name useless for first-name lookups. (Part 9)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 \u25b6 GROUP BY vs a window function \u2014 the entire difference in one sentence?', a: 'GROUP BY COLLAPSES rows (10 in \u2192 3 out); window functions keep every row and add a computed column (10 in \u2192 10 out) \u2014 so per-customer totals sit next to each order. (Part 6)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 \u25b6 Why is the average of per-city averages wrong \u2014 and how do you compute several metrics in one scan?', a: 'Averaging averages ignores differing group sizes \u2014 aggregate from base rows. Use COUNT(*) FILTER (WHERE ...) or SUM(CASE WHEN ...) for multiple metrics in a single pass. (Part 5)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 \u25b6 EXPLAIN vs EXPLAIN ANALYZE \u2014 which one runs the query, and when do estimates lie?', a: 'EXPLAIN ANALYZE EXECUTES and shows real counts/timings versus estimates (careful with UPDATE/DELETE \u2014 wrap in a rolled-back transaction). When estimates diverge wildly: stale statistics \u2192 ANALYZE. (Part 9)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 \u25b6 Why are CTEs the readability tool, and what recursive power do they hide?', a: 'CTEs name intermediate results so a complex query reads top-to-bottom like the evaluation pipeline instead of inside-out nesting \u2014 and WITH RECURSIVE walks hierarchies and graphs. (Part 6)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 \u25b6 You store money as DOUBLE PRECISION and total 0.1 + 0.2 \u2192 0.30000000000000004. Diagnose.', a: 'Floats are binary approximations \u2014 exact decimal arithmetic is impossible (money, tariffs, sums). Store NUMERIC(12,2)/DECIMAL. Types are constraints the engine enforces. (Part 3)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 \u25b6 Walk the lost update and its three fixes.', a: 'Two transactions read value \u2192 compute in the app \u2192 write; the second overwrites the first SILENTLY at READ COMMITTED. Fixes: do arithmetic in SQL (SET balance = balance - 500), SELECT ... FOR UPDATE, make it impossible (CHECK), or SERIALIZABLE + retry. (Part 11)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 \u25b6 Narrate the query lifecycle, parser to executor.', a: 'Parser (string \u2192 AST) \u2192 analyzer (tables/columns/types) \u2192 rewriter (flatten subqueries, push predicates) \u2192 planner/optimizer (enumerate plans, cost with statistics) \u2192 executor (pull rows through the plan tree). (Part 10)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 \u25b6 When does row-oriented storage lose to columnar \u2014 and why?', a: 'Analytics reads FEW columns across MANY rows \u2014 row stores drag every column (and PAY the I/O) per 8KB page; columnar compresses and scans only the needed columns, trading single-row writes/updates away. (Parts 10, 12)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 \u25b6 The Model of Sets', pass: 'Pass: recite the logical order cold; solve the NULL riddles (COUNT vs COALESCE vs IS NULL); write set-thinking queries on one table unaided. <b>(parts 2\u20133; days 1\u20132)</b>' },
    { id: 'm2', title: 'Milestone 2 \u25b6 Joins Without Fear', pass: 'Pass: explain each join type on a napkin; build the employees self-join; catch a join that multiplied row counts before it hits a report. <b>(part 4; day 2\u20133)</b>' },
    { id: 'm3', title: 'Milestone 3 \u25b6 Aggregation and Windows', pass: 'Pass: GROUP BY rule, WHERE vs HAVING, ROW_NUMBER/RANK/DENSE_RANK, and the latest-per-group pattern \u2014 all from memory. <b>(parts 5\u20136; days 3\u20134)</b>' },
    { id: 'm4', title: 'Milestone 4 \u25b6 A Schema You\u2019d Defend', pass: 'Pass: ship the 5-table shop with constraints and unit_price snapshots; the cohort-retention query runs and its NULL choices are deliberate. <b>(parts 7\u20138; days 4\u20136)</b>' },
    { id: 'm5', title: 'Milestone 5 \u25b6 Talking to the Engine', pass: 'Pass: EXPLAIN ANALYZE three slow queries, diagnose each (index missing, statistics stale, function wrap), fix with evidence the times move. <b>(parts 9\u201310; days 6\u20137)</b>' },
    { id: 'm6', title: 'Milestone 6 \u25b6 Production-Grade SQL', pass: 'Pass: under mock questioning \u2014 ACID defaults, lost-update prevention, deadlocks, parameterized queries, injection hygiene. <b>(parts 11\u201314; days 7\u20138)</b>' },
  ],

  sim: [
    { t: 800, l: "<span class=\"a\">connect</span> session open, shared_buffers warm \u2014 rows live in 8KB pages ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">parse</span> query string \u2192 abstract syntax tree ... <span class=\"ok\">OK</span>" },
    { t: 1400, l: "<span class=\"a\">analyze</span> resolve tables, columns, types \u2014 typos die here ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">rewrite</span> flatten subqueries, push predicates down ... <span class=\"ok\">OK</span>" },
    { t: 1300, l: "<span class=\"a\">plan</span> cost candidate plans against statistics ... <span class=\"ok\">OK</span>" },
    { t: 1000, l: "<span class=\"a\">b-tree</span> walk the index \u2014 keys in prefix order, ranges linked ... <span class=\"ok\">OK</span>" },
    { t: 1200, l: "<span class=\"a\">filter</span> NULL rows evaluate UNKNOWN \u2014 dropped like FALSE ... <span class=\"ok\">OK</span>" },
    { t: 900, l: "<span class=\"a\">execute</span> 1,024 rows returned \u2014 think in sets, not loops ... <span class=\"ok\">OK</span>" },
  ],
};