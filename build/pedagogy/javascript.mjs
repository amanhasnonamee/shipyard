// javascript pedagogy — authored from javascript-curriculum.md (current 2025)
export default {
  title: 'The JavaScript Shipyard - JavaScript: A First-Principles Curriculum',
  brand: 'THE JAVASCRIPT SHIPYARD',
  tagline: 'JavaScript · values & refs · closures · the event loop',
  storageKey: 'shipyard:javascript:v1',
  legacyStorageKeys: [],
  stackLabel: 'THE STACK — your call stack',
  ranks: ['SWABBIE', 'DECKHAND', 'BOATSWAIN', 'FIRST MATE', 'CLOSURE CAPTAIN', 'HARBOR MASTER'],
  accent: '#a78bfa',

  boot: [
    '<div class="bl">$ shipyard boot --curriculum javascript-first-principles</div>',
    '<div class="bl">STAGE 1: LOADING PARTS 0–14 ......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl">STAGE 2: MOUNTING DEPTH TAGS .......... <span style="color:var(--ok)">OK</span></div>',
    '<div class="bl"><span class="warn">WARNING: `this` is decided by the call, not by the definition.</span></div>',
    '<div class="bl">AWAITING CREW — 14 waypoints ahead. <span class="cursor" style="color:var(--accent)">▮</span></div>',
  ],
  h1: 'JAVASCRIPT, UNDERSTOOD.<br><span class="wh">NOT JUST STACKED.</span>',
  sub: 'This is a first-principles curriculum rebuilt as a mission. Learn the core mental model <b>deeply</b> — values vs references, lexical scope, the prototype chain, and the event loop — reference the rest, and physically <b>build the shipyard&#39;s container</b> in the sidebar as you complete parts. Quizzes earn XP. Ranks go from <b>Swabbie</b> to <b>Harbor Master</b>. Finish all 14 parts and the shipyard ships your container.',
  foot: {
    about: 'The JavaScript Shipyard is a faithful interactive conversion of the first-principles JavaScript curriculum — every concept, table, code block and exercise preserved. Progress lives in your browser (localStorage). Built as a single file — copy it anywhere, it runs offline.',
    rule: '`this` is decided by the call, not by the definition. Hold that and half your JS bugs disappear.',
  },
  chips: { p11: ['<span class="m">★ <b>INTERVIEW GOLD</b></span>'] },
  grad: {
    title: 'EVENT LOOP CLEARED ⚡',
    rule: '`this` is decided by the call, not by the definition. Hold that and half your JS bugs disappear.',
  },
  console: {
    cmd: '$ node --stack-trace-limit=0 app.js',
    line: 'event loop idle  ·  microtasks drained  ·  (yours — every layer earned)',
  },
  simPrompt: '// console.log(1); setTimeout(()=>console.log(2)); Promise.resolve().then(()=>console.log(3));',
  egg: { label: 'HOISTED' },
  deckIn: 'p13',
  milesIn: 'p14',
  simIn: 'p11',
  deckNote: 'If you can deliver these aloud — WHAT \u2192 WHY IT EXISTS \u2192 ONE CONCRETE CONSEQUENCE — you are interview-ready. Answer before flipping; nailed cards dim.',
  milesNote: 'Six milestones = six layers of proof. When all six drop, the shipyard ships your container.',

  quiz: {
    p1: [
      { q: 'The governing constraint of JS — the one that explains its history:', o: ['Never break the web — every surviving page must keep working; legacy mistakes stay', 'Maximise raw speed', 'Remove footguns aggressively every release', 'Keep the GIL'], a: 0, e: '\u201cDon\u2019t break the web\u201d: backwards compatibility is sacred, so old mistakes persist — which is why new features arrive via transpilation, not breaking changes.' },
      { q: 'JavaScript was invented:', o: ['By Brendan Eich in 10 days at Netscape, 1995 — borrowing prototypes from Self and closures/first-class functions from Scheme', 'By the W3C committee over five years', 'By Microsoft in 2001', 'As a subset of Java'], a: 0, e: 'Ten days, Netscape, 1995. Prototypes from Self, lambdas from Scheme — that lineage is why the language looks the way it does.' },
      { q: 'What took JavaScript out of the browser?', o: ['Node.js (Ryan Dahl, 2009) — V8 running outside the browser', 'TypeScript in 2012', 'ES6 modules', 'npm becoming mandatory'], a: 0, e: 'Node.js moved JS to servers and build tooling, which changed the ecosystem forever.' },
      { q: 'How do ES6+ features reach the web safely?', o: ['Transpilation — Babel compiles modern syntax down to what older engines understand', 'Breaking browsers', 'A parallel language', 'CDN polyfills only'], a: 0, e: 'The \u201cnever break the web\u201d constraint means new syntax ships compiled, not native — until engines catch up.' },
    ],
    p2: [
      { q: 'Primitives vs objects — the divide:', o: ['7 primitives are values compared by VALUE; everything else is an object compared by REFERENCE', 'Everything is an object', 'Strings are objects', 'Primitives cannot be stored in variables'], a: 0, e: 'string, number, bigint, boolean, symbol, undefined, null — value semantics. Objects (incl. arrays, functions): reference semantics. Half your bugs live at this boundary.' },
      { q: 'let vs const vs var:', o: ['var: function-scoped + hoisted as undefined — the legacy footgun; let/const: block-scoped with a temporal dead zone', 'let and const are identical', 'const prevents all mutation', 'var is best practice in 2025'], a: 0, e: 'Use const by default, let when rebinding. const binds the name; it does not freeze the object.' },
      { q: 'const a = {x:1}; const b = a; b.x = 2; what is a.x?', o: ['2 — b is a second reference to the SAME object; assignment never copies objects', '1 — b is a copy', 'undefined', 'TypeError'], a: 0, e: 'b = a makes two names for one object; mutation through either is visible through both. Clone explicitly (spread) when you need independence.' },
      { q: 'A closure is:', o: ['A function that keeps access to the scope where it was defined, even after the outer function returns', 'A function with no arguments', 'The event loop\u2019s queue', 'A class without state'], a: 0, e: 'Lexical scoping: functions capture surrounding variables. Counters, factories, and modules are all closures.' },
    ],
    p3: [
      { q: '\u20182\u2019 + 2 evaluates to:', o: ['\u201822\u2019 — + favours string concatenation', '4 — + is always numeric', 'NaN', 'TypeError'], a: 0, e: '\u2018+\u2019 is dual-purpose (concat/numeric) which is why it lies; \u2018*\u2019 is numeric-only. The coercion rule that matters: be explicit, use ===.' },
      { q: '== vs ===:', o: ['=== checks type AND value; == coerces first. Prefer === — except rare deliberate cross-type comparisons', '== is faster, always use it', '=== fails on objects', 'Never use either'], a: 0, e: '=== with explicit conversions at boundaries makes coercion a non-issue.' },
      { q: 'Which values are falsy?', o: ['0, \u2018\u2019, null, undefined, NaN, false — arrays and objects are always truthy, even [] and {}', '[] and {} are falsy', '\u2018false\u2019 is falsy', 'null is truthy'], a: 0, e: 'The classic plain-error: [] and {} are truthy. if (arr.length) — not if (arr) — when counting items.' },
      { q: 'The rule that makes coercion a non-issue:', o: ['Convert explicitly (String()/Number()/Boolean()) at boundaries and use === everywhere else', 'Avoid all conversions', 'Use == everywhere', 'Rely on truthiness only'], a: 0, e: 'Explicit conversion at input boundaries + strict equality internally — coercion stops being a bug source.' },
    ],
    p4: [
      { q: 'Arrow vs normal functions:', o: ['Arrows inherit `this` lexically and have no own arguments — ideal callbacks; normal functions get `this` from the call', 'Arrows have their own this', 'Arrows cannot close over scope', 'Arrows are slower'], a: 0, e: 'No own `this`, no arguments object, not constructible. That is precisely why arrows replaced callbacks in modern code.' },
      { q: 'Closures in practice:', o: ['Variable persistence + encapsulation — the module pattern is a closure over private state', 'A memory leak by design', 'A replacement for classes', 'An async mechanism'], a: 0, e: 'A closure keeps its definition scope alive, giving exclusive access to private state — counters, factories, modules.' },
      { q: 'Purity and side effects:', o: ['A pure function \u2192 same inputs, same output, no outer mutation; isolate side effects at the edges of your app', 'Pure means \u201cno errors possible\u201d', 'Side-effect-free code cannot exist', 'Impure code is always a bug'], a: 0, e: 'Side effects (I/O, DOM, timers) are unavoidable — pay for them at the edges; keep the middle pure, tested, predictable.' },
      { q: 'Default parameters and rest:', o: ['Defaults evaluate at CALL time; ...rest collects the tail into a real array', 'Defaults run at definition', 'Rest is still experimental', 'arguments is a modern array'], a: 0, e: 'function f(a, b = 2, ...rest) — defaults on call, rest as a true array (unlike the legacy arguments object).' },
    ],
    p5: [
      { q: 'The prototype chain, concretely:', o: ['obj.prop looks on obj, then its prototype, then up to Object.prototype, then null — DELEGATION, not copying', 'Each object copies its prototype at creation', 'Chains stop at Function.prototype', 'Prototypes are classes'], a: 0, e: 'Lookup delegates up the chain until found or null. Mutating a prototype is visible to every object that delegates to it.' },
      { q: 'JS classes are:', o: ['Sugar over constructor functions + the prototype — the same delegation underneath, same `this` rules', 'A new memory model', 'Static-only types', 'Interfaces'], a: 0, e: 'class is syntax, not a new paradigm: a constructor plus prototype methods. Everything you learned about delegation still applies.' },
      { q: 'When do you not need a class?', o: ['Factories (object literals + closures) cover most modules — no ceremony, no `this` to lose', 'Classes are mandatory in modern JS', 'Object.create(null) is deprecated', 'Factories are slower'], a: 0, e: 'Prefer the simplest thing that holds state — a factory with a closure is often it.' },
      { q: 'obj.method() then const f = obj.method; f() — this in the second call is:', o: ['undefined (strict) — `this` is decided by the CALL; fix with arrow, bind, or call/apply', 'obj', 'the global object', 'the method itself'], a: 0, e: 'obj.method() binds this to obj; a detached reference loses it. That asymmetry explains most `this` bugs.' },
    ],
    p6: [
      { q: 'map, filter, reduce — the three shapes:', o: ['map = 1:1 transform, filter = subset, reduce = fold to any value — the shapes of all data work', 'map mutates in place', 'reduce is deprecated', 'filter returns a new array but map does not'], a: 0, e: 'If you can name any pipeline you write as one of the three shapes, the code writes itself.' },
      { q: 'Which methods mutate and which return new?', o: ['push/sort/reverse/splice MUTATE; map/filter/slice/spread return new — mixing the two is a classic bug source', 'All methods mutate', 'sort is pure', 'push returns a new array'], a: 0, e: 'Know the split at a glance: state-changers (push, sort, reverse, splice) vs producers (map, filter, slice, spread).' },
      { q: 'The immutability pattern:', o: ['Spread: const next = [...arr, item] or {...obj, x: 1} — a new reference, the original untouched', 'Spread copies deeply', 'Immutability is impossible in JS', 'Only Object.assign works'], a: 0, e: 'Spread gives you a new top-level reference cheaply. For nested data you need structuredClone or an immutable library.' },
      { q: 'Optional chaining and destructuring:', o: ['?. short-circuits to undefined instead of throwing; destructuring unpacks objects and arrays into names', '?. throws by default', 'Destructuring mutates the source', 'Both are Babel-only'], a: 0, e: 'const {name, age = 0} = user ?? {} — defaults, safety, and unpacking in one readable line.' },
    ],
    p7: [
      { q: 'Why does async exist at all?', o: ['One thread, one call stack — I/O must not block it; promises/async-await let the stack keep working while I/O waits', 'Browsers are multithreaded by default', 'Async adds threads', 'Promises run in parallel processes'], a: 0, e: 'The browser/Node each have one main thread. While a fetch waits, the call stack must stay free — that is the entire reason for callbacks \u2192 promises \u2192 async/await.' },
      { q: 'Event-loop ordering:', o: ['Sync code \u2192 then microtasks (promises) drain fully \u2192 then macrotasks (timers, I/O) — microtasks always win between macrotasks', 'Microtasks wait for all macrotasks', 'Timers run first', 'Everything is FIFO'], a: 0, e: 'Promise callbacks beat setTimeout — always. that ordering decides real dinnertime UI behaviour.' },
      { q: 'The sequential-await mistake:', o: ['await inside a for-loop SERIALISES requests — use Promise.all for independent fetches', 'Promise.all is slower', 'for-await is mandatory', 'await is synchronous'], a: 0, e: 'Each await parks the loop. Independent work should be started together and awaited together: const xs = await Promise.all(ids.map(fetch)).' },
      { q: 'Errors and timeouts:', o: ['Add a timeout (Promise.race or AbortController) and always catch — unhandled rejections crash the process', 'Timeouts are automatic', 'Rejections are silent by design', 'Promises cannot fail'], a: 0, e: 'A fetch with no timeout can hang forever; AbortController actually cancels the request. Never await without a plan for rejection.' },
    ],
    p8: [
      { q: 'The DOM is:', o: ['A live tree the browser builds from HTML — JS queries and mutates the tree, not the file', 'The HTML source string', 'The same as innerHTML', 'A CSS feature'], a: 0, e: 'document.querySelector etc. operate on the parsed tree the browser maintains; innerHTML re-parses text into nodes.' },
      { q: 'Events:', o: ['Bubble child \u2192 parent — delegate with one listener on a parent for many children, and stay dynamic-safe', 'Descend parent \u2192 child only', 'Need manual wiring per element', 'Cannot be prevented'], a: 0, e: 'Event delegation: one listener handles existing AND future children — the standard pattern for lists and tables.' },
      { q: 'Why do frameworks exist?', o: ['Keeping state and DOM in sync is the hard part — frameworks make the mapping explicit instead of manual string updates', 'Frameworks make JS itself faster', 'Frameworks add threads', 'Frameworks replace the DOM'], a: 0, e: 'Manual updates mean you lose where state lives. Virtual DOM / reactive signals give a single explicit path from state \u2192 DOM.' },
      { q: 'Layout thrash is caused by:', o: ['Interleaved reads and writes — each write marks layout dirty; a read forces synchronous relayout. Batch DOM writes', 'Too many elements', 'CSS animations', 'Using React'], a: 0, e: 'The fix: read all values first, then write. One forced reflow per frame is cheap; a thrashing loop is not.' },
    ],
    p9: [
      { q: 'ESM vs CommonJS:', o: ['ESM is static and tree-shakeable — imports analysed before execution; CJS is dynamic. Choose ESM for new code', 'CJS is deprecated', 'ESM cannot run in Node', 'They are identical'], a: 0, e: 'Static imports let bundlers drop unused code; CJS require() runs at runtime, so tree-shaking fails.' },
      { q: 'State in a vanilla app:', o: ['One render(state) function driven by explicit events — the source of truth lives in plain data', 'In DOM attributes', 'In global variables only', 'In CSS classes'], a: 0, e: 'Even without a framework: state \u2192 render \u2192 events \u2192 state. That discipline is what frameworks formalise.' },
      { q: 'A Node service shape:', o: ['Small handlers, explicit validation, error middleware, composition over frameworks', 'One giant handler', 'No validation — JS types itself', 'Frameworks are mandatory'], a: 0, e: 'Composition + explicit boundaries: validate at the edge, keep handlers thin, let errors bubble to one middleware.' },
      { q: 'When does TypeScript become the right call?', o: ['When surface area and team size make refactor safety worth the ceremony — it is a tooling win, not a runtime one', 'Never', 'Always, from line one', 'Only for browsers'], a: 0, e: 'TS costs ceremony and buys tooling safety. Small scripts: plain JS. Growing codebase: adopt incrementally.' },
    ],
    p10: [
      { q: 'The debugging decision tree starts with:', o: ['Reproducing exactly — then bisecting the failing assumption', 'Reading the source top to bottom', 'Asking the framework', 'Restarting the machine'], a: 0, e: 'A reproducible case + a stated assumption lets you bisect fast. DevTools come second.' },
      { q: 'DevTools patterns that beat console.log:', o: ['Breakpoints, call stack, watchers — plus the debugger statement, Network tab, and Performance panel', 'console.log everywhere', 'Only the Elements tab', 'DevTools cannot debug async'], a: 0, e: 'Breakpoints pause the call stack so you can inspect state at the exact moment — no logging noise.' },
      { q: 'Async bugs are hard because:', o: ['The stack unwinds at each await — use async await tracing in DevTools and fake timers in tests', 'Errors are invisible', 'Promises never throw', 'alert() is required'], a: 0, e: 'Async tracing follows promise chains across awaits; fake timers make timeout/polling logic deterministic in tests.' },
      { q: 'undefined vs NaN as symptoms:', o: ['undefined usually means a missing property or a fallen-through return; NaN usually means coercion — string math or a bad parse', 'NaN means hardware failure', 'undefined means a network error', 'Both are typos'], a: 0, e: 'Read the symptom: undefined \u2192 wrong object shape; NaN \u2192 coercion. Both are cheap to locate once you know the direction.' },
    ],
    p11: [
      { q: 'How V8 runs your code:', o: ['Parse \u2192 AST \u2192 bytecode, then TurboFan JIT compiles hot paths to machine code — with hidden classes and inline caches optimizing property access', 'Interpreted only', 'V8 never optimises', 'Bytecode is machine code'], a: 0, e: 'JS in production is JIT-compiled: hot functions become real machine code. That is why loops are not inherently slow in JS — unlike Python.' },
      { q: 'Memory and leaks:', o: ['GC marks and sweeps heap objects; the classic leaks are detached DOM nodes and closures retaining large scopes', 'JS has no GC', 'JS uses reference counting only', 'Leaks are impossible in modern engines'], a: 0, e: 'A detached node with a listener keeps its subtree alive; closures keep whatever they capture. Heap snapshots find both.' },
      { q: 'The event loop, precisely:', o: ['Macrotasks (timers, I/O, UI events) and microtasks (promises, queueMicrotask) — after each macrotask the microtask queue drains completely', 'Microtasks are macrotasks', 'Only timers exist', 'The loop runs in parallel threads'], a: 0, e: 'Promise callbacks beat setTimeout, and a microtask that schedules another microtask delays the next macrotask indefinitely.' },
      { q: 'ES modules execute:', o: ['Once per URL, with static imports resolved before any code runs — no duplicate execution like CJS cycles', 'On every import call', 'Lazily in parallel', 'In global scope'], a: 0, e: 'The module graph is resolved and instantiated up front; evaluation happens once. That is why ESM cycles work and CJS cycles deadlock.' },
    ],
    p12: [
      { q: 'TypeScript the decision:', o: ['Annotations buy tooling safety at the cost of ceremony — and TS is NOT runtime validation; validate external data at the boundary', 'TS runs in the browser', 'TS replaces tests', 'Types guarantee safety'], a: 0, e: 'TS erases to plain JS; runtime validation still needs explicit checks (zod etc.) on API boundaries.' },
      { q: 'Testing doctrine:', o: ['Test BEHAVIOUR, not implementation — the unit is a behaviour, and tests freeze the contract', 'Test every line for coverage', 'Tests slow you down', 'Skip tests for demos'], a: 0, e: 'Behaviour tests survive refactors; implementation tests just break. Coverage is a hint, not a goal.' },
      { q: 'The security essentials:', o: ['Never inject untrusted strings via innerHTML — build with textContent/createElement; sanitise URLs (javascript:) and CSP headers', 'innerHTML is safe', 'eval is fine for user data', 'CSP is optional'], a: 0, e: 'XSS = trusting strings as code. Escape output, validate input, and let CSP constrain what can execute.' },
      { q: 'When NOT to use JavaScript:', o: ['CPU-bound work that browsers cannot offload, hard real-time, and massive in-process compute — native/WASM or other languages choose themselves', 'JS is always fastest', 'JS is for everything', 'Node cannot serve production traffic'], a: 0, e: 'The honest answer includes the platform: in the browser there is no alternative; on servers, reach for the right tool when the hot path demands it.' },
    ],
  },

  cards: [
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '1 \u25b6 Primitives vs objects — and what assignment actually does.', a: '7 primitives are values (compared by value); objects (arrays, functions included) are references. Assignment never copies — it points two names at one object. (Part 2, Idea 1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '2 \u25b6 let vs const vs var — scoping and hoisting.', a: 'var: function-scoped, hoisted as undefined. let/const: block-scoped with TDZ; const binds the name, does not freeze the object. (Part 2, Idea 1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '3 \u25b6 == vs === and the coercion rule.', a: '=== checks type and value; == coerces. The non-issue rule: convert explicitly (String/Number/Boolean) at boundaries, use === everywhere else. (Part 3.2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '4 \u25b6 The falsy list — and the classic plain-error.', a: '0, \u2018\u2019, null, undefined, NaN, false. [] and {} are TRUTHY — use arr.length, not arr, to test for items. (Part 3.1)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '5 \u25b6 Closures — what they capture and why they exist.', a: 'A function keeps access to the scope where it was defined, even after the outer function returns. Captures variables (not values) — late-binding footgun with loops. (Parts 2, 4.3)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '6 \u25b6 The prototype chain, concretely.', a: 'Property lookup: the object, then its prototype, up to Object.prototype, then null — delegation, not copying. class is sugar over constructor + prototype. (Part 5.2)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '7 \u25b6 `this` — who decides it?', a: 'The CALL: obj.method() binds obj; a detached reference loses it (undefined in strict). Arrows inherit `this` lexically; bind/call/apply override. (Part 2, Idea 4)' },
    { t: 'TIER 1 - EXPECTED OF EVERYONE', q: '8 \u25b6 The event loop in one breath.', a: 'One thread. Sync code runs, then ALL microtasks (promises), then one macrotask (timer/I/O/UI), then microtasks again. Promise callbacks beat setTimeout — always. (Part 2, Idea 5)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '9 \u25b6 map/filter/reduce — the three shapes of data work.', a: 'map = 1:1 transform, filter = subset, reduce = fold to any value. Any pipeline names itself as one of the three — and they never mutate. (Part 6.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '10 \u25b6 Mutating vs non-mutating array methods.', a: 'Mutate: push, sort, reverse, splice. Return new: map, filter, slice, spread. Mixing the two is THE classic array bug. (Part 6.2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '11 \u25b6 Spread and immutability.', a: 'const next = [...arr, item] / {...obj, x:1} — new reference, original untouched. Shallow: nested objects still shared; use structuredClone for depth. (Part 6.2)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '12 \u25b6 Why async exists — the single thread.', o: undefined, a: 'One call stack; I/O must not block it. Callbacks came first, then promises (composable, no pyramid), then async/await (synchronous-looking). (Part 7.1)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '13 \u25b6 The sequential-await mistake.', o: undefined, a: 'await in a loop serialises independent work. Start together, await together: Promise.all(ids.map(fetch)). Add timeouts via AbortController. (Part 7.3)' },
    { t: 'TIER 2 - DIFFERENTIATORS', q: '14 \u25b6 Why frameworks exist.', a: 'State \u2194 DOM sync is the hard part; frameworks make it explicit (virtual DOM / reactive signals) instead of manual string updates. Delegation + render(state) is the vanilla version. (Part 8.3)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '15 \u25b6 How V8 runs your code.', a: 'Parse \u2192 AST \u2192 bytecode, then TurboFan JIT-compiles hot paths to machine code; hidden classes + inline caches speed property access. (Part 11.1)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '16 \u25b6 Memory — and the two classic leaks.', a: 'GC marks/sweeps the heap. Leaks: detached DOM nodes holding subtrees via listeners, and closures retaining large scopes. Heap snapshots find both. (Part 11.2)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '17 \u25b6 Microtasks vs macrotasks, precisely.', a: 'After every macrotask, the microtask queue drains completely. A microtask scheduling another microtask starves timers. queueMicrotask runs before any next timer. (Part 11.3)' },
    { t: 'TIER 3 - SENIOR SIGNAL', q: '18 \u25b6 When is JavaScript the wrong choice?', a: 'CPU-bound work the browser cannot offload, hard real-time, huge in-process compute. In the browser there is no alternative; on the server, pick the right tool. (Part 13)' },
  ],

  miles: [
    { id: 'm1', title: 'Milestone 1 \u25b6 Model', pass: 'Pass: the Part 2 checkpoint aloud — values vs references, let/const/var, a closure, the prototype chain, `this`, and the event loop ordering. <b>(Parts 1\u20132; days ~1\u20133)</b>' },
    { id: 'm2', title: 'Milestone 2 \u25b6 Values', pass: 'Pass: recite the falsy list and coercion rule from memory; rewrite five == programs with === + explicit conversions. <b>(Part 3; days 4\u20136)</b>' },
    { id: 'm3', title: 'Milestone 3 \u25b6 Functions & objects', pass: 'Pass: write a counter closure, a factory, and a class; explain arrow `this` and trace a prototype lookup path by hand. <b>(Parts 4\u20135; days 7\u201312)</b>' },
    { id: 'm4', title: 'Milestone 4 \u25b6 Data & async', pass: 'Pass: transform a dataset with map/filter/reduce without mutation; build a parallel fetch with Promise.all and timeouts. <b>(Parts 6\u20137; days 13\u201316)</b>' },
    { id: 'm5', title: 'Milestone 5 \u25b6 Projects', pass: 'Pass: a vanilla browser app (delegated events + render(state), no framework, no build step) and a Node service with explicit validation. <b>(Parts 8\u20139; days 17\u201325)</b>' },
    { id: 'm6', title: 'Milestone 6 \u25b6 Depth', pass: 'Pass: reproduce the event-loop order on paper; find a real leak in DevTools (heap snapshot); argue two cases where JS is the wrong tool. <b>(Parts 10\u201313; days 26\u201330)</b>' },
  ],

  sim: [
    { t: 700, l: '<span class="a">cli</span> GET /api/orders \u2192 fetch() from a browser tab' },
    { t: 1000, l: '<span class="a">sync</span> call stack runs execute() — 2ms, stack empties' },
    { t: 1100, l: '<span class="a">micro</span> promise.then drains: render badge \u2192 queue flushed before ANY timer' },
    { t: 1200, l: '<span class="a">io</span> network 120ms — the stack stays free; other tasks keep running' },
    { t: 1000, l: '<span class="a">macro</span> setTimeout(0) waits in the macrotask queue — after microtasks' },
    { t: 1000, l: '<span class="a">concurrent</span> 3 fetches via Promise.all — overlapped, not serialised' },
    { t: 900, l: '<span class="a">render</span> paint committed at 60fps — zero layout thrash' },
    { t: 800, l: '<span class="ok">OK</span> orders table updated. one thread; nothing blocked.' },
  ],
};